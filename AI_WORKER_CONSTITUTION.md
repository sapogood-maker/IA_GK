# AI_WORKER_CONSTITUTION.md — Goalkeeper AI Worker

**Constituição arquitetural oficial do serviço `goalkeeper_ai_worker`, parte do monorepo `IA_GK`.**

> Documento vivo. A revisão original (antes da primeira linha de código) promoveu toda decisão então registrada como "risco em aberto" ou "nota" a arquitetura principal ou a um ADR explícito. **Sprint W2.1** (pós-Sprint W2, `ARCHITECTURE_REVIEW_W2.md`) sincronizou a Seção 1 e a Seção 12 com a estrutura real implementada (`contracts/`, `infrastructure/`, `core/`). **Sprint W4.1** (pós-Sprint W4, `SPRINT_W4_1_REPORT.md`) fez a mesma sincronização para o Pipeline completo (W3) e a camada de Inferência (W4): `orchestrator/`, `pipeline/stages/`, `state/`, `workspace/`, `events/` deixaram de estar vazios, e o módulo `inference/` — não previsto com esse nome na versão original — passou a ser a arquitetura oficial de IA plugável.
>
> **A partir da Sprint W6, não há mais sprints de sincronização (Wx.1).** Documentação passou a fazer parte do Definition of Done de cada sprint — toda alteração arquitetural é registrada aqui durante a própria sprint que a introduz, nunca depois. A revisão da W6 incorporou o que ficou pendente da W5 (`worker/video/`) e o que a W6 introduziu (`BasicVisionEngine`, `frame_ops.py`, configuração de pré-processamento de frame). A revisão da W7 incorporou `inference/processors/` (`FrameProcessor`, `ColorProcessor`/`ResizeProcessor`/`ROIProcessor`/`StatisticsProcessor`, `PipelineProcessor`) — `BasicVisionEngine` deixou de transformar frame diretamente e passou a orquestrar essa pipeline de Processors. A revisão da W8 incorporou `inference/detectors/` (`Detector`, `YOLODetector`, Registry e `factory.py` próprios) e `YOLOProcessor` — o primeiro modelo real de detecção, isolado atrás de uma abstração própria desde o primeiro dia. A revisão da W9 incorporou `inference/trackers/` (`Tracker`, `ByteTrackTracker`, Registry e `factory.py` próprios) e `TrackingProcessor` — o primeiro Tracker real, mais o par `reset()` (`FrameProcessor`/`Tracker`/`PipelineProcessor`) que corrige um vazamento de estado entre Jobs sequenciais, descoberto durante a implementação. A revisão da W10 incorporou `inference/events/` (`SceneAnalyzer`, `BasicSceneAnalyzer`, Registry e `factory.py` próprios) e `SceneAnalysisProcessor` — a primeira camada de interpretação de cena, ainda sem nenhuma regra de negócio de futebol; reaproveitou a plumbing de `reset()` da W9 sem precisar recriá-la, e corrigiu de curso a expectativa antiga (W4.1) de que um "Event Registry técnico" nasceria dentro de `worker/events/` — em vez disso, materializou-se em `worker/inference/events/`. A revisão da W11 incorporou `inference/world/` (`WorldModel`, `BasicWorldModel`, `Trajectory`/`History`/`compute_motion`, Registry e `factory.py` próprios) e `WorldModelProcessor` — a ÚLTIMA camada genérica da arquitetura: uma "fotografia" consistente do mundo (posição, trajetória, velocidade, aceleração, ciclo de vida do objeto), sem regra de negócio alguma. Reaproveitou a plumbing de `reset()` da W9 pela terceira vez consecutiva, e estendeu `SceneAnalysisResult` (W10) com `objects: list[SceneObjectSnapshot]` — achado necessário, já que `SceneEvent` sozinho não carrega bbox/posição e o World Model só pode consumir `SceneAnalysisResult`. A revisão da W12 incorporou `worker/domain/` (`FootballWorld`, `FootballWorldBuilder`, `entities/`, `geometry/`) e `FootballDomainProcessor` — a PRIMEIRA camada específica de futebol, mas deliberadamente ainda sem análise, IA, regra de negócio ou heurística: apenas transforma `WorldState` em entidades ricas do domínio (goleiro, bola, jogadores, gols, campo, direção do jogo). Único módulo desta arquitetura sem Registry/factory (implementação única e canônica). Reaproveitou a plumbing de `reset()` da W9 pela quarta vez, desta vez aplicada proativamente desde a primeira versão do código, e introduziu `Vector.from_polar()` para converter a cinemática polar do World Model em vetores cartesianos do domínio. A revisão da W13 incorporou `worker/analyzers/` (`Analyzer`, `GoalkeeperPresenceAnalyzer`, `results.py`, Registry e `factory.py` próprios) e `AnalyzerProcessor` — a primeira camada que RESPONDE perguntas sobre o domínio, mas ainda sem julgamento/avaliação real: `GoalkeeperPresenceAnalyzer` é totalmente determinístico (existe goleiro? quantos? qual track_id? visível? posição/bbox atuais?). Sétima família de Registry, e a primeira em que várias implementações coexistem simultaneamente (`WORKER_ANALYZERS`, lista, ao invés de uma única implementação ativa por vez). Reaproveitou a plumbing de `reset()` da W9 pela quinta vez, e estendeu `TrackedFootballEntity` (W12) com `bbox: Region` — achado necessário, já que nenhuma entidade de domínio carregava bounding box até então. A revisão da W14 incorporou `worker/analyzers/goal_geometry.py` (`GoalGeometryAnalyzer`, `GoalGeometryResult`, `GoalZone`) — segundo Analyzer real, também sem julgamento/avaliação: puramente geométrico (centro/postes/dimensões/regiões-zonas do gol). Confirmou na prática, pela primeira vez, que dois Analyzers rodam simultaneamente sem qualquer mudança estrutural, e que um Analyzer pode compor o resultado de outro (via instanciação interna e chamada direta a `.analyze()`) sem exigir nenhum canal de comunicação novo entre Analyzers. Nenhum achado arquitetural foi necessário — `Goal`/`Field` (W12) já carregavam tudo que o novo Analyzer precisava. A revisão da W15 incorporou `worker/analyzers/goalkeeper_position.py` (`GoalkeeperPositionAnalyzer`, `GoalkeeperPositionResult`) — terceiro Analyzer real, primeiro a de fato COMPOR outro (instancia `GoalGeometryAnalyzer` internamente), validando em produção o padrão previsto na W14: distância/deslocamentos/ângulo/áreas/terços entre goleiro e gol, tudo medição pura, nenhum julgamento. Confirmado que a composição funciona mesmo sem o Analyzer composto estar ativo em `WORKER_ANALYZERS`. Nenhum achado arquitetural foi necessário. A revisão da W16 incorporou `worker/analyzers/ball_position.py` (`BallPositionAnalyzer`, `BallPositionResult`) — quarto Analyzer real, segundo a compor `GoalGeometryAnalyzer` (mesmo padrão exato da W15, aplicado à bola em vez do goleiro): distância/deslocamentos/ângulo/áreas/zona do gol entre bola e gol, tudo medição pura. Extraiu `estimate_goal_and_penalty_areas()` de `goalkeeper_position.py` para um novo módulo `field_areas.py`, reutilizado por ambos os Analyzers — refatoração comportamento-preservando, não um achado de contrato. A revisão da W17 incorporou `worker/analyzers/goalkeeper_ball_alignment.py` (`GoalkeeperBallAlignmentAnalyzer`, `GoalkeeperBallAlignmentResult`) — quinto Analyzer real, primeiro RELACIONAL: compõe TRÊS Analyzers internamente (`GoalGeometryAnalyzer`/`GoalkeeperPositionAnalyzer`/`BallPositionAnalyzer`), combinando apenas seus resultados já produzidos (ecoando distâncias/ângulos já calculados, em vez de recalculá-los) e computando como genuinamente novo só a relação goleiro↔bola e o alinhamento do goleiro com a reta bola→gol (projeção geométrica). Primeira confirmação de que o padrão de composição escala para múltiplos Analyzers simultâneos dentro de um só. A revisão da W18 incorporou `worker/analyzers/ball_motion.py` (`BallMotionAnalyzer`, `BallMotionResult`, `BallMotionContext`) — sexto Analyzer real, **primeiro STATEFUL** da Analyzer API: mede deslocamento/velocidade/direção/aceleração da bola comparando com o frame anterior, usando `AnalyzerContext` (reservada desde a W13) pela primeira vez de forma real. Confirmou que `AnalyzerProcessor.reset()`, existente desde a W13 e nunca modificada, já delegava corretamente a `analyzer.reset()` mesmo antes de qualquer Analyzer precisar dele de verdade. Nunca extrapola através de uma lacuna de observação (bola desaparecida ou `track_id` trocado reinicia a continuidade). A revisão da W19 incorporou `worker/analyzers/shot.py` (`ShotAnalyzer`, `ShotAnalysisResult`, `ShotAnalyzerContext`) — sétimo Analyzer real, primeiro de EVENTOS e o primeiro a tomar uma DECISÃO (`shot_detected`), ainda 100% determinística e parametrizável, não uma avaliação de qualidade. Compõe três Analyzers (`BallMotionAnalyzer`/`BallPositionAnalyzer`/`GoalGeometryAnalyzer`), combinando só resultados já produzidos. Segundo Analyzer STATEFUL, mesmo padrão da W18. Revelou concretamente, na validação manual, o descompasso de escala entre geometria placeholder e posições reais em pixel (Risco 34) — documentado honestamente. A revisão da W20 incorporou `worker/analyzers/ball_trajectory.py` (`BallTrajectoryAnalyzer`, `BallTrajectoryResult`, `BallTrajectoryContext`) — oitavo Analyzer real, volta a ser puramente descritivo (não um evento binário como a W19): modela a FORMA da trajetória observada (comprimento do caminho, direção dominante, consistência de direção, mudanças de direção, linearidade), tudo derivado só dos frames já observados, nunca extrapolado. Compõe três Analyzers (`BallMotionAnalyzer`/`BallPositionAnalyzer`/`GoalGeometryAnalyzer`, este último só para `confidence`). Terceiro Analyzer STATEFUL, mesmo padrão da W18/W19. Por instrução explícita, não corrigiu o Risco 34 (confirmou que não se agrava) e introduziu o Risco 35 (`trajectory_points` sem limite) — ambos documentados honestamente. A revisão da W21 incorporou `worker/analyzers/play_situation.py` (`PlaySituationAnalyzer`, `PlaySituationResult`, `PlaySituation`) — nono Analyzer real, primeiro COGNITIVO: classifica o estado observado da jogada (`situation`/`sub_state`) combinando QUATRO Analyzers já existentes (`ShotAnalyzer`/`BallTrajectoryAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`GoalGeometryAnalyzer`), sem manter nenhum estado próprio. Continua sendo uma classificação determinística, nunca um julgamento de qualidade. Revelou, na implementação, que `ShotAnalyzer.towards_goal` pode ser `True` para uma bola parada (Risco 36) — corrigido com um guard explícito no próprio `PlaySituationAnalyzer`, documentado honestamente. A revisão da W22 incorporou `worker/analyzers/goalkeeper_decision.py` (`GoalkeeperDecisionAnalyzer`, `GoalkeeperDecisionResult`, `GoalkeeperDecision`, `GoalkeeperDecisionContext`) — décimo Analyzer real, primeiro ESPECÍFICO DO GOLEIRO e quarto STATEFUL: classifica a decisão observada que o goleiro aparenta estar executando, combinando CINCO Analyzers já existentes (`PlaySituationAnalyzer`/`GoalkeeperPositionAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`BallTrajectoryAnalyzer`/`ShotAnalyzer`) e rastreando, pela primeira vez, o movimento do PRÓPRIO goleiro entre frames (informação genuinamente nova, já que nenhum Analyzer composto fazia isso). Continua sendo uma classificação determinística, nunca um julgamento de qualidade. Introduziu o Risco 37 (`PREPARE_DIVE` é um proxy de velocidade, não uma detecção real de postura) — documentado honestamente. A revisão da W23 incorporou `worker/analyzers/goalkeeper_decision_evaluation.py` (`GoalkeeperDecisionEvaluationAnalyzer`, `GoalkeeperDecisionEvaluationResult`, `GoalkeeperDecisionEvaluation`) e o novo módulo genérico `worker/analyzers/rules.py` (`Rule`/`RuleOutcome`/`evaluate_rules`) — décimo primeiro Analyzer real, primeiro de AVALIAÇÃO e primeira peça da camada de Explainability: responde se a decisão observada do goleiro (W22) foi compatível com a situação observada (W21), via um mecanismo explícito e auditável de Rule Evaluation (6 regras documentadas, cada resultado sempre acompanhado de explicação textual). Continua NÃO avaliando o resultado da jogada (nem defesa, nem gol). Corrigiu, durante a implementação (antes da validação manual), uma leitura incorreta de `ShotAnalysisResult.direction_vector` (normalizado, não cru) via teste falho. Introduziu o Risco 38 (a maioria das regras de conteúdo são auditorias garantidas por construção, `INCOMPATIBLE` puro só alcançável hoje via teste direto da agregação) — documentado honestamente. A revisão da W24 incorporou `worker/analyzers/play_outcome.py` (`PlayOutcomeAnalyzer`, `PlayOutcomeResult`, `PlayOutcome`, `PlayOutcomeContext`) — décimo segundo Analyzer real, encerra a cadeia de observação (Observação→Situação→Decisão→Avaliação→Resultado): responde qual foi o resultado observado da jogada, combinando CINCO Analyzers já existentes e mantendo estado próprio (quinto Analyzer STATEFUL) para memorizar a última posição conhecida da bola/do goleiro e reconhecer `LOST_TRACK`. Validado manualmente com DOIS Jobs consecutivos no mesmo processo, confirmando ausência de vazamento de estado. Descobriu, durante a implementação, que `GoalGeometryResult.left_post`/`right_post` (W14) nunca foram genuinamente consumidos até agora e não correspondem à real separação lateral dos postes (Risco 39) - `CROSSBAR` permanece deliberadamente nunca produzido. **A revisão da W25** incorpora `worker/analyzers/goalkeeper_performance_evaluation.py` (`GoalkeeperPerformanceEvaluationAnalyzer`, `GoalkeeperPerformanceEvaluationResult`, `GoalkeeperPerformanceEvaluation`) — décimo terceiro Analyzer real, encerra a cadeia de avaliação (Situação→Decisão→Avaliação da Decisão→Resultado→Avaliação de Desempenho): responde como foi o desempenho observado do goleiro, combinando EXCLUSIVAMENTE `GoalkeeperDecisionEvaluation` (W23) e `PlayOutcome` (W24) numa matriz determinística 3x3, via 8 novas regras aplicadas ao MESMO mecanismo de Rule Evaluation da W23 (`worker.analyzers.rules`) - nenhum segundo mecanismo de regras criado. Confirma que o desenho da W23 é genuinamente reutilizável por um Analyzer completamente diferente.

## Premissas e escopo

- **Monorepo (ADR-009):** o Worker vive na pasta `goalkeeper_ai_worker/`, dentro do mesmo repositório Git de `IA_GK` (`backend_fastapi/`, `frontend_flutter/`, `goalkeeper_ai_worker/` como pastas de topo irmãs). Decisão puramente organizacional/de versionamento — **nenhuma responsabilidade de componente muda**. O Worker continua sendo tratado como se estivesse em outro repositório: runtime, dependências, código Python, banco de dados e lógica de negócio nunca são compartilhados com `backend_fastapi/` (ver Frozen Architecture, ao final deste documento).
- O Worker **consome** a infraestrutura já construída no backend (Sprints 5-7): autenticação por API Key (`X-Worker-Api-Key`), URLs assinadas de download/upload, Redis Streams (stream `processing_jobs`), e o modelo de dados `Analysis`/`Event`/`Metric`/`Artifact`/`Report`. **Nenhuma dessas peças é alterada aqui.**
- Hardware atual: 1 máquina com GPU AMD RX 7900 XTX (ROCm). Horizonte de 2-3 anos: múltiplas máquinas, múltiplos vendors de GPU, múltiplos modelos de IA coexistindo, dezenas a centenas de vídeos por clube por mês.
- Este documento assume que uma peça específica do backend ainda não existe (o endpoint de submissão de resultados) e trata isso como dependência externa explícita (ver ADR-008), não como algo a implementar agora.

---

## Terminologia Oficial (Glossário)

Termos usados de forma consistente em todo o documento — evitar reintroduzir sinônimos para os mesmos conceitos.

| Termo | Definição |
|---|---|
| **Video** | Asset original enviado pelo usuário via backend. Identificado por `video_id`, estável durante toda a vida do vídeo, mesmo com múltiplas análises. |
| **Job** | Uma tentativa de processamento de um Video. Identificado por `job_id`. Um Video pode ter vários Jobs ao longo do tempo (reprocessamento), mas no máximo **um Job ativo por vez** (ADR-001). |
| **Correlation ID** | `video_id`, formalizado como identificador primário de correlação (Seção 3). |
| **Pipeline** | Sequência ordenada de Stages que processam um Job. Possui versão própria — **Pipeline Version** (Seção 8, ADR-006). |
| **Stage** | Unidade atômica de trabalho dentro do Pipeline (ex.: Download, Inferência, Submissão). Cada Stage tem timeout e classe de retry próprios (Seção 5). |
| **Plugin** | Implementação concreta e substituível de uma família de capacidade (modelo de detecção, tracker, calculadora de métrica, detector de evento, gerador de artefato, gerador de relatório). Todo Plugin tem nome, família e versão, e implementa a Interface de Plugin da sua família. |
| **Plugin Registry** | Componente único que resolve, para uma família de Plugin, qual implementação concreta está ativa, a partir de configuração (Seção 6, ADR-005). |
| **Model Registry** | Especialização do Plugin Registry para modelos de IA (detecção, pose, segmentação, classificação). |
| **Analysis** | Resultado versionado de um Job bem-sucedido sobre um Video. **Nunca sobrescrita** — cada execução bem-sucedida gera uma nova versão. |
| **Event / Metric / Artifact / Report** | Entidades do modelo de dados já definido no backend (Sprint 5), associadas a uma Analysis. |
| **Worker** | O serviço definido por este documento. **Stateless entre Jobs** — não guarda estado de negócio entre execuções; o único estado local é o checkpoint efêmero de um Job em andamento (Seção 5). |
| **Worker Instance** | Um processo Worker em execução, identificado por `worker_instance_id` (o campo já usado como `worker_id` nos testes do backend — mesmo conceito). |
| **Worker Version** | Versão do software do Worker (build/release) — distinta da instância. |
| **Schema Version** | Versão do contrato de dados usado na submissão de resultados ao backend (Seção 9). |

---

## 1. Arquitetura Interna do Worker

Arquitetura hexagonal (ports & adapters): um núcleo de orquestração que não conhece detalhes de infraestrutura, cercado por Plugins e adaptadores substituíveis via Registry.

| Módulo | Status | Responsabilidade | O que NÃO faz |
|---|---|---|---|
| `orchestrator` | ✓ Implementado (W3) | `WorkerOrchestrator`: executa o Pipeline (`process_job`), decide a transição entre Stages, garante `Cleanup`/`ReleaseLock` em bloco `finally`, consome a fila (`run_forever`) | Não decide o conteúdo de nenhum Stage; não conhece motores de inferência específicos |
| `pipeline` | ✓ Implementado (W3/W4) | `stages/`: os 10 Stages ativos hoje — `ReceiveJobStage`, `ValidateJobStage`, `AcquireLockStage`, `PrepareWorkspaceStage`, `DownloadVideoStage`, `InferenceStage`, `UploadArtifactStage`, `UpdateStatusStage`, `CleanupStage`, `ReleaseLockStage` (Seção 2) | Não orquestra a sequência (isso é do `orchestrator`); nenhum Stage conhece OpenCV/YOLO/PyTorch |
| `inference` | ✓ Implementado (W4/W6/W7/W8/W9/W10/W11/W13) — **não previsto com este nome na versão original da Constituição** | Único lugar onde código de visão computacional pode existir: `InferenceEngine` (contrato, Seção 6.1), `BasicVisionEngine` (motor **padrão**, orquestrador fino), `inference/processors/` (`FrameProcessor`s independentes: `ColorProcessor`/`ResizeProcessor`/`ROIProcessor`/`StatisticsProcessor` transformam o frame, `YOLOProcessor` só detecta, `TrackingProcessor` só associa, `SceneAnalysisProcessor` só interpreta, `WorldModelProcessor` só mantém estado, `FootballDomainProcessor` só modela domínio, `AnalyzerProcessor` (Sprint W13, classe definida em `worker.analyzers.processor` — módulo irmão, registrada aqui) só analisa, `PipelineProcessor`, Registry próprio), `inference/detectors/` (Sprint W8 — API de Detecção: `Detector`, `YOLODetector`), `inference/trackers/` (Sprint W9 — API de Tracking: `Tracker`, `ByteTrackTracker`), `inference/events/` (Sprint W10 — API de Eventos de Cena: `SceneAnalyzer`, `BasicSceneAnalyzer`), `inference/world/` (Sprint W11 — **World Model**: `WorldModel`, `BasicWorldModel`, Registry e `factory.py` próprios — a ÚLTIMA camada genérica), `FakeInferenceEngine` (mantido só para testes), `frame_ops.py` (funções puras, chamadas pelos Processors correspondentes), `registry.py` (nome→classe de **motores**) e `engine.py` (`create_engine`, resolve por `WORKER_INFERENCE_ENGINE`) | `Pipeline`/`Orchestrator` nunca importam nada daqui além do contrato `InferenceEngine`; nunca abre um arquivo de vídeo diretamente — sempre via `video/`; nenhum Processor conhece outro Processor; nenhum Processor conhece Ultralytics/pesos/modelos — só os contratos `Detector`/`Tracker`/`SceneAnalyzer`/`WorldModel`; `Tracker` nunca conhece YOLO/Ultralytics — só `DetectionResult`; `SceneAnalyzer` nunca conhece ByteTrack/YOLO — só `TrackingResult`; `WorldModel` nunca conhece YOLO/ByteTrack/OpenCV/Redis/Backend/R2 — só `SceneAnalysisResult` |
| `video` | ✓ Implementado (W5) — **módulo irmão de `inference/`, nunca dentro dele** | `VideoReader` (abre/fecha/valida, via `cv2.VideoCapture` só como biblioteca de leitura), `FrameProvider` (leitura sequencial), `FrameIterator` (protocolo padrão de iterador), `Frame`/`FrameMetadata`/`VideoProperties` (Seção 6.1) | Nenhuma linha de `cv2.dnn`, YOLO, modelo, GPU ou qualquer decisão de pré-processamento (resize/ROI/skip) — isso é responsabilidade da camada de inferência que o consome |
| `domain` | ✓ Implementado (W12) — **pacote de topo, irmão de `inference/`, não um submódulo dele** | Football Domain Model (Seção 6.4): `FootballWorld`/`FootballWorldBuilder` (transforma `WorldState` em entidades de futebol), `entities/` (`Goalkeeper`/`Player`/`Ball`/`Goal`/`Field`), `geometry/` (`Coordinate`/`Vector`/`Region`/`Direction`, utilitários puros reutilizáveis por analisadores). Sem Registry/`factory.py` (deliberado — `FootballWorldBuilder` é a única implementação do domínio, não uma família de Plugin) | Conhece apenas `WorldState`/`ObjectState` (`inference/world/`); nunca `Detector`/`Tracker`/`SceneAnalyzer`, OpenCV, YOLO, ByteTrack, Redis, Backend ou R2; nenhuma decisão/heurística/regra de negócio de futebol vive aqui |
| `analyzers` | ✓ Implementado (W13, NOVO) — **pacote de topo, irmão de `domain/` e `inference/`, não um submódulo de nenhum dos dois** | Analyzer API (Seção 6.5): `Analyzer` (contrato `analyze(FootballWorld) -> AnalysisResult`), `GoalkeeperPresenceAnalyzer` (primeira implementação — perguntas puramente factuais/determinísticas sobre presença do goleiro), `results.py` (`AnalysisResult`/`AnalyzerMetadata`/`AnalysisStatistics`, tipos fortemente tipados), Registry e `factory.py` próprios (sétima família de Plugin — diferente de Detector/Tracker/SceneAnalyzer/WorldModel, aqui VÁRIAS implementações coexistem simultaneamente, via `WORKER_ANALYZERS`). `AnalyzerProcessor` (`processor.py`) é a ponte com a pipeline, registrada em `inference/processors/registry.py` | Conhece apenas `FootballWorld` (`worker.domain.football_world`); nunca `WorldState`/`SceneAnalysisResult`/`TrackingResult`/`DetectionResult`, OpenCV, YOLO, ByteTrack, Redis, Backend ou R2; `GoalkeeperPresenceAnalyzer` não implementa heurística, regra de futebol, avaliação ou julgamento — só fatos determinísticos; a primeira análise com julgamento real (`ShotAnalyzer`/`SaveAnalyzer`/`DiveAnalyzer`/`GoalAnalyzer`) fica para W14+ |
| `contracts` | ✓ Implementado (W2) | Tipos que representam exclusivamente os contratos públicos do Backend — REST (`backend_api.py`) e mensagem do Redis Stream (`queue_message.py`) | Não contém lógica, não importa `app/schemas/schemas.py` do backend (Boundary Enforcement) |
| `infrastructure/redis` | ✓ Implementado (W2) | Cliente Redis, consumer group do stream `processing_jobs` (ack/claim/retry de mensagens) e Lock distribuído por `video_id` (Seção 4, ADR-001/003) | Não decide o que fazer com o Job nem política de negócio, só entrega/confirma mensagens e coordena acesso |
| `infrastructure/backend_client` | ✓ Implementado (W2) | Cliente HTTP do Worker API: camada genérica de requisição + detalhes do Job, atualização de status, URLs assinadas, Contrato Backend↔Worker (Seção 9) | Não acessa Postgres/R2 diretamente |
| `infrastructure/storage` | ✓ Implementado (W3) | Download/upload REAL via URL assinada (`r2_client.py`) | Não decide quando baixar/subir — quem chama é `DownloadVideoStage`/`UploadArtifactStage` |
| `gpu` | Vazio — aspiracional | Abstração de compute (Seção 7) | Ainda nenhuma linha de código; nenhum Plugin específico será conhecido por aqui |
| `registry` (genérico, cross-família) | Vazio — **o padrão já foi provado**, não o módulo | Seção 6 previa um único mecanismo de Plugin Registry de topo, reaproveitado por todas as famílias. O padrão (nome→classe, registrar/resolver, ativação por configuração) já está provado em `inference/registry.py`, mas ainda escopado só a motores de inferência | Extrair um `worker/registry/` genérico compartilhado é decisão para quando uma **segunda** família (Tracker Registry, W6+) precisar do mesmo mecanismo — evita generalizar a partir de uma única instância |
| `models/` | Vazio — mantido por compatibilidade textual | Seção 6 original previa Plugins de modelo (detecção/pose/segmentação/classificação) aqui, resolvidos por um Model Registry separado. **Na prática (W4), essa responsabilidade convergiu para `inference/`** — motores de inferência (incluindo futuros motores de detecção reais) vivem lá, não aqui | Esta pasta não deve receber código; ver `inference/` |
| `tracking/` | Vazio — **igual a `models/`, decisão já tomada** | Previsto aqui na versão original para Plugins de tracking (Tracker Registry) | **Na prática (W9), essa responsabilidade convergiu para `inference/trackers/`** — mesmo padrão de `models/`→`inference/` (W4): a família de Plugin vive dentro de `inference/`, esta pasta de topo não recebe código |
| `metrics/` | Vazio — aspiracional (W6+) | Plugins de métricas (Metric Registry) | Idem |
| `events/events.py` (eventos internos) | ✓ Implementado (W3), com 1 tipo órfão desde a W4 | `JobStarted`/`VideoDownloaded`/`UploadFinished`/`JobCompleted`/`JobFailed` — eventos de ciclo de vida do Job realmente emitidos hoje, só logging por enquanto (Seção 6.2). `ArtifactGenerated` continua definido mas **não é mais emitido** por ninguém desde que `GenerateArtifactStage` foi removida (W4) — pequeno código morto, sinalizado aqui, não corrigido (sem mudança funcional permitida) | **Não é** `inference/events/` (linha abaixo) — mesmo nome de pasta (`events`) em dois níveis diferentes da árvore, conceitos inteiramente distintos. A ideia original desta revisão (W4.1) de acomodar um futuro "Event Registry técnico" como submódulo AQUI DENTRO de `worker/events/` **não se concretizou assim** — a W10 implementou a API de Eventos de Cena em `worker/inference/events/` em vez disso (mais perto da camada de inferência que a consome). `worker/events/` segue só com `events.py` (ciclo de vida do Job) |
| `artifacts/` | Vazio — aspiracional (W6+) | Plugins de artefatos (Artifact Registry) | Idem |
| `report/` | Vazio — aspiracional | Plugins de relatório (Report Registry) | Não persiste nada — só agrega |
| `submission` | Vazio — aspiracional (depende de ADR-008, backend) | Monta e envia o payload do Contrato Backend↔Worker em duas fases idempotentes (Seção 9) | Não gera dados, só os empacota e envia |
| `state` | ✓ Implementado (W3) | `PipelineState` (Seção 6.2) — todo o estado de um Job em processamento, trocado entre Stages | Não é banco de dados de produção; não é o checkpoint durável entre máquinas (ainda não existe, ver Riscos) |
| `workspace` | ✓ Implementado (W3) | `WorkspaceManager` (Seção 6.2) — cria/limpa o diretório de trabalho temporário por Job, sempre via `tempfile.mkdtemp` | Não decide o que gravar ali — só oferece o espaço; não usa o diretório `goalkeeper_ai_worker/workspace/` da Seção 12 (ver nota lá) |
| `observability` | Parcial — logging pronto (W1), resto aspiracional | Logging estruturado (Correlation ID obrigatório) já funciona; métricas internas, heartbeat, health check ainda não existem | Não decide política de alerta |
| `core` | ✓ Implementado (W1) | Ciclo de vida do processo (inicialização, shutdown gracioso) e hierarquia de exceções própria (`WorkerError` → `ConfigurationError`/`QueueConnectionError`/`BackendUnavailableError`/`BackendRequestError`/`StorageError`/`PipelineError`) | Não contém lógica de negócio nem de infraestrutura externa |
| `config` | ✓ Implementado (W1-W4, crescendo por sprint) | Toda configuração via `.env` (`WorkerSettings`) — inclui `WORKER_INFERENCE_ENGINE` desde a W4 | Não contém lógica de negócio |

**Regra de dependência (Frozen):** `orchestrator`, `pipeline` só dependem de **interfaces**. Adaptadores concretos (`inference/fake_engine.py`, e futuramente `inference/opencv_engine.py`) implementam essas interfaces e são conectados pelo Registry da própria família (hoje: `inference/registry.py`), nunca por import direto do núcleo.

---

## 2. Pipeline e Estágios (Stages)

Cada Stage recebe o `PipelineState` (Seção 6.2) e devolve o `PipelineState` atualizado — nunca dezenas de parâmetros soltos. Nenhum Stage acessa estado global.

### 2.1 Fluxo real, implementado (W3/W4)

```
Redis (consumer group) → WorkerOrchestrator.process_job
  → ReceiveJobStage → ValidateJobStage → AcquireLockStage → PrepareWorkspaceStage
  → DownloadVideoStage → InferenceStage (chama InferenceEngine.process) → UploadArtifactStage
  → UpdateStatusStage → [bloco finally] CleanupStage → ReleaseLockStage → ACK
```

1. **Recepção do Job** (`ReceiveJobStage`) — o `orchestrator` já reivindicou a mensagem (`job_id`, `video_id`) do consumer group Redis; esta Stage busca os detalhes do Job via `backend_client`.
2. **Validação** (`ValidateJobStage`) — confirma que o `video_id` da mensagem bate com o do Job e que ele não está em estado terminal (`COMPLETED`/`FAILED`/`CANCELLED`). Falha aqui é sempre permanente.
3. **Aquisição do Lock** (`AcquireLockStage`) — adquire o Lock distribuído do vídeo (Seção 4) — nenhuma etapa seguinte roda sem o lock.
4. **Preparação do Workspace** (`PrepareWorkspaceStage`) — cria o diretório de trabalho temporário via `WorkspaceManager` (Seção 6.2).
5. **Download** (`DownloadVideoStage`) — obtém a URL assinada e baixa o vídeo original para o workspace.
6. **Inferência** (`InferenceStage`) — delega inteiramente a `InferenceEngine.process(state)` (Seção 6.1). Hoje: `FakeInferenceEngine` (sem IA real) — lê o vídeo baixado, gera um `InferenceResult` mínimo, salva-o como artefato JSON no workspace.
7. **Upload** (`UploadArtifactStage`) — envia os bytes do artefato ao R2 via URL assinada de upload (esquema `artifacts/{video_id}/{job_id}/{filename}`).
8. **Atualização de Status** (`UpdateStatusStage`) — marca o Job como `COMPLETED` (progresso 100%) junto ao backend.
9. **Limpeza** (`CleanupStage`, bloco `finally`) — remove o workspace, se ele chegou a existir — roda mesmo se qualquer etapa anterior falhar.
10. **Liberação do Lock** (`ReleaseLockStage`, bloco `finally`) — libera o Lock, se ele chegou a ser adquirido — roda mesmo se qualquer etapa anterior falhar.

Falha em qualquer etapa 1-8 (exceção `WorkerError`) marca o Job como `FAILED` e ainda assim executa 9-10. Não há retry automático, checkpoint local durável, timeout por Stage nem cancelamento em andamento nesta implementação — ver Seção 5 (política ainda aspiracional) e Riscos Arquiteturais.

### 2.2 Visão original, ainda não implementada (pós-W5)

A sequência completa de 14 etapas originalmente prevista nesta Constituição continua sendo o destino de longo prazo, mas **"Inferência" já deixou de ser aspiracional** — é `InferenceStage`/`InferenceEngine`, real desde a W4. O que falta:

- **Pré-processamento real** de vídeo (normalização de frame rate/resolução) — depende de `VideoReader`/`FrameProvider`/`FrameIterator` (Seção 15, Sprint W5).
- **Tracking**, **Pós-processamento**, **Métricas** (Metric Registry), **Eventos técnicos** (Event Registry — distinto dos eventos internos de ciclo de vida, Seção 6.2), **Artefatos** (Artifact Registry, plural — hoje há só um artefato fixo por Job), **Relatório** (Report Registry) — todos aspiracionais, sem código, sequenciados para depois da W5 (número de sprint a definir quando a infraestrutura de vídeo estiver pronta).
- **Submissão de Resultados** (duas fases, ADR-002/008) — depende do endpoint do backend que ainda não existe (ADR-008).
- **Conclusão formal só após a Submissão** — hoje `UpdateStatusStage` já marca `COMPLETED`, mas sem o conceito de Submissão prévia (porque `Analysis`/`Event`/`Metric` ainda não são gerados).

---

## 3. Correlation ID e Rastreabilidade

**`video_id` é o Correlation ID oficial** — presente obrigatoriamente em todo log, métrica interna e payload de submissão, tanto no Worker quanto no backend. Ele é estável entre reprocessamentos (múltiplos Jobs do mesmo vídeo), o que um `job_id` isolado não garante, e não exige criar um identificador novo, pois já existe em todo o sistema desde o upload.

**`job_id` representa apenas uma tentativa específica de processamento** — um identificador secundário e descartável, nunca reutilizado como chave de correlação de longo prazo. Toda linha de log deve conter os dois: `video_id` (correlação primária) e `job_id` (tentativa atual).

---

## 4. Exclusão Mútua por Vídeo (Lock Distribuído)

Mecanismo oficial (ADR-001) para impedir que dois Workers processem o mesmo Video simultaneamente:

- Lock distribuído no Redis, chave derivada de `video_id`, adquirido como a **primeira ação** do Stage "Recepção do Job" (Seção 2) — nenhum download, nenhuma inferência, nenhuma escrita começa sem o lock adquirido.
- TTL curto com renovação periódica automática atrelada ao heartbeat do Worker (ADR-003) — um Worker que para de reportar liveness perde o lock automaticamente, permitindo que outro Worker assuma.
- Liberado explicitamente ao final do Stage "Conclusão" (sucesso ou falha permanente) e, de forma graciosa, em caso de cancelamento (Seção 5).
- É a defesa primária. Uma defesa complementar no backend (rejeitar criação de um novo Job enquanto há um ativo para o mesmo vídeo) é recomendada como evolução futura, coordenada como mudança de backend — não implementada neste repositório.

---

## 5. Política de Timeout, Retry, Cancelamento e Retomada

| Situação | Ação oficial |
|---|---|
| Falha transitória num Stage retryable (rede, timeout de I/O) | Retry com backoff exponencial, limite de N tentativas configurável por Stage; checkpoint do Stage não avança até sucesso |
| Timeout de um Stage (excedeu o orçamento máximo de tempo declarado para aquele Stage) | Aborta a tentativa atual do Stage e aplica a mesma política de retry acima — o Job inteiro não é abortado só por um Stage estourar o tempo, a menos que o limite de tentativas também se esgote |
| Falha permanente (vídeo corrompido, erro de modelo não recuperável, validação reprovada) | Job vai para `FAILED` com `error_message` descritivo; mensagem confirmada (`ACK`) na fila — não é reprocessada automaticamente; Lock liberado |
| Cancelamento externo (`CANCELLED` solicitado via backend, por ação humana) | O Worker consulta o status do Job junto ao `backend_client` nos pontos de checagem entre Stages (ADR-004); ao detectar `CANCELLED`, interrompe no próximo ponto seguro, libera o Lock, **não** executa a Submissão de Resultados |
| Crash do processo Worker | Mensagem não confirmada permanece pendente no consumer group; após timeout, outro Worker reivindica via `XCLAIM`; se houver checkpoint local (mesma máquina), retoma do último Stage concluído; senão recomeça do início (risco aceito, ver Riscos Arquiteturais) |
| Esgotamento do limite de retries num Stage retryable | Job vai para `FAILED`, tratado como falha permanente a partir desse ponto — não fica retentando indefinidamente |

Cancelamento é sempre **gracioso**: nunca ocorre no meio de uma operação não interrompível com segurança (ex.: nunca no meio do envio da Fase 2 da Submissão — o Worker termina a fase em andamento antes de respeitar um cancelamento pendente).

---

## 6. Arquitetura de Plugins (Plugin Registry Único)

**Um único contrato de Plugin, um único mecanismo de Registry**, reaproveitado por todas as famílias — evita seis padrões ad hoc ligeiramente diferentes. **Status (W12): o princípio está provado na prática seis vezes** (`inference/registry.py` para motores, desde a W4; `inference/processors/registry.py` para Processors, desde a W7; `inference/detectors/registry.py` para Detectors, desde a W8; `inference/trackers/registry.py` para Trackers, desde a W9; `inference/events/registry.py` para SceneAnalyzers, desde a W10; `inference/world/registry.py` para WorldModels, desde a W11) — **o módulo genérico de topo ainda não foi extraído**, ver nota na Seção 1, linha `registry`, e Risco 10 (Seção 14). **Não-exemplo deliberado (W12):** `worker/domain/` (`FootballWorldBuilder`) **não** ganhou uma sétima família de Registry — é uma implementação única e canônica do domínio de futebol, não um Plugin substituível (ver Seção 6.4); o padrão de Registry só se aplica onde múltiplas implementações concorrentes fazem sentido.

- **Contrato de Plugin:** nome, família, versão, interface de entrada/saída própria da família, ciclo de vida (`init` → `execute` → `teardown`).
- **Plugin Registry:** resolve, para uma família, qual Plugin está ativo (por versão), a partir de configuração. Descoberta automática de Plugins existentes + ativação explícita por configuração (ADR-005) — nenhum Plugin roda sem estar habilitado.
- **Independência entre famílias:** Plugins de famílias diferentes (detecção, pose, segmentação, classificação, tracking, métricas, eventos, artefatos, relatório) não têm dependência de código entre si — só compartilham o contrato de dados produzido/consumido em cada transição do Pipeline (Seção 2). Adicionar uma família nova (ex.: um sétimo tipo de modelo) não altera as demais.

**Especializações do Plugin Registry:**

| Registry | Família | Status | Exemplos de Plugin |
|---|---|---|---|
| **Model Registry** (implementado como `inference/registry.py`) | Motores de inferência | ✓ Implementado (W4/W6) — `FakeInferenceEngine` e `BasicVisionEngine` registrados hoje | YOLO, RT-DETR, Grounding DINO, SAM2, modelo próprio (futuros) |
| **Processor Registry** (implementado como `inference/processors/registry.py`) | Processors de frame (dentro do motor `BasicVisionEngine`) | ✓ Implementado (W7/W8/W9) — `ColorProcessor`/`ResizeProcessor`/`ROIProcessor`/`StatisticsProcessor`/`YOLOProcessor`/`TrackingProcessor` registrados hoje; dict ordenado, ordem de registro = ordem de execução | Futuro `PoseProcessor` (Seção 16) |
| **Detector Registry** (implementado como `inference/detectors/registry.py`) | Detectors de objetos (usados por `YOLOProcessor`, resolvidos via `factory.py`) | ✓ Implementado (W8) — `YOLODetector` registrado hoje | RT-DETR, Grounding DINO, OWLv2 (futuros, Seção 16) |
| **Tracker Registry** (implementado como `inference/trackers/registry.py`) | Trackers de objetos (usados por `TrackingProcessor`, resolvidos via `factory.py`) | ✓ Implementado (W9) — `ByteTrackTracker` registrado hoje | BoT-SORT, DeepSORT, StrongSORT, OC-SORT (futuros, Seção 16) |
| **Scene Analyzer Registry** (implementado como `inference/events/registry.py`) | Analisadores de cena (usados por `SceneAnalysisProcessor`, resolvidos via `factory.py`) | ✓ Implementado (W10) — `BasicSceneAnalyzer` registrado hoje | Futuros analisadores genéricos alternativos (Seção 16) |
| **World Model Registry** (implementado como `inference/world/registry.py`) | World Models (usados por `WorldModelProcessor`, resolvidos via `factory.py`) | ✓ Implementado (W11) — `BasicWorldModel` registrado hoje | Futuros World Models alternativos (Seção 16) |
| **Metric Registry** | Métricas | Aspiracional (W6+) | Cada calculadora de métrica |
| **Event Registry** (técnico, específico de futebol) | Eventos técnicos de futebol | Aspiracional (W6+) — **distinto** do `Scene Analyzer Registry` acima: este seria uma camada de análise específica (Defesa/Saída/Reposição/1x1/Cruzamento) que **consumiria** `SceneEvent`s genéricos como entrada, não os produz | Defesa, Saída, Reposição, 1x1, Cruzamento |
| **Artifact Registry** | Artefatos | Aspiracional (W6+) | thumbnail, heatmap, timeline, json, vídeo anotado, clipes, csv, parquet |
| **Report Registry** | Relatório | Aspiracional | Relatório técnico detalhado, resumo para treinador, outros formatos de agregação |

Trocar um Plugin (ex.: `FakeInferenceEngine` por um motor real de OpenCV/YOLO) é escrever uma nova classe conforme o contrato da família + registrá-la + apontar o Registry para ela via configuração — zero mudança em `orchestrator`, `pipeline`, ou em qualquer outra família. Já comprovado por teste automatizado (`tests/inference/test_registry.py::test_swapping_the_active_engine_requires_only_registration_and_config`).

---

## 6.1 Camada de Inferência (Implementação Real — Sprint W4/W6/W7/W8/W9/W10/W11/W12/W13/W14/W15/W16/W17/W18/W19/W20/W21/W22/W23/W24/W25/W26/W27)

Módulo `inference/` — único lugar onde código de visão computacional pode existir.

- **`InferenceEngine`** (`base.py`) — classe abstrata (ABC), o contrato único: `name`, `version`, `async def process(state) -> state`. Nenhuma Stage do Pipeline conhece OpenCV/YOLO/ByteTrack/PyTorch — só este contrato. **Não mudou desde a W4** — trocar de motor, reestruturar por completo o que acontece *dentro* dele (W7), introduzir detecção real (W8), tracking real (W9), interpretação de cena (W10) ou o World Model (W11) nunca exigiu alterar esta interface.
- **`BasicVisionEngine`** (`basic_vision_engine.py`, versão `2.0.0` desde a W7) — **motor padrão** (`WORKER_INFERENCE_ENGINE=basic_vision`). É um **orquestrador fino**: abre o vídeo (`video.VideoReader`), itera frame a frame (`video.FrameProvider`/`FrameIterator`), decide **apenas** se cada frame entra na pipeline (frame-skipping — `WORKER_FRAME_SKIP`), reseta a `PipelineProcessor` no início de cada Job (`self._pipeline.reset()` — ver nota de estado entre Jobs, abaixo) e delega **toda** transformação/detecção/tracking/interpretação/estado-do-mundo a essa `PipelineProcessor` (ver `inference/processors/`, abaixo). Ele mesmo não chama `cv2.cvtColor`/`cv2.resize`/recorte/Ultralytics/ByteTrack algum — só monta o `InferenceResult` final e o artefato JSON, mesclando `context.to_dict()` (métricas por Processor), `pipeline.processor_names` (ordem executada), `context.detections_to_dict()` (W8), `context.tracking_results_to_dict()` (W9), `context.scene_events_to_dict()` (W10), o último `context.world_states` (W11), o último `context.football_worlds` (W12) e, desde a W13, `context.analysis_results` (o ÚLTIMO resultado de cada Analyzer ativo) ao `to_dict()` do resultado. **Nenhum código de detecção/tracking/interpretação/estado-do-mundo/domínio/análise vive aqui** — o motor só lê `context.detections`/`context.tracking_results`/`context.scene_analysis_results`/`context.world_states`/`context.football_worlds`/`context.analysis_results` de volta, exatamente como já lia `context.stats` desde a W7.
- **`inference/processors/`** (Sprint W7/W8/W9/W10/W11) — onde toda transformação/detecção/tracking/interpretação/manutenção-de-estado de frame realmente acontece; nenhuma fica concentrada em `BasicVisionEngine`.
  - **`base.py`** — `FrameProcessor` (ABC): contrato único `process(frame, metadata, context) -> (frame, metadata, context)` + `is_enabled(settings)` (classmethod) + `reset()` (Sprint W9, concreto com default no-op — ver nota de estado entre Jobs). **Nenhum Processor conhece outro Processor.** `ProcessorStats`/`ProcessorContext` — acumulam tempo de execução e frames processados por Processor (`context.record`), resultados de detecção (W8), tracking (W9), análise de cena (W10) e, desde a W11, estados do mundo (`context.add_world_state`) — genéricos o bastante para qualquer Processor futuro.
  - **`color_processor.py`**/**`resize_processor.py`**/**`roi_processor.py`**/**`statistics_processor.py`** — inalterados desde a W7/W8 (conversão de cor, resize, ROI, observabilidade — ver revisões anteriores desta Constituição).
  - **`yolo_processor.py`** (`YOLOProcessor`, `name="yolo"`, Sprint W8) — **não transforma a imagem, só detecta**: chama `Detector.detect(frame)`, acumula o `DetectionResult` no contexto. **Nenhuma linha de Ultralytics aqui**. `is_enabled` reflete `bool(settings.detector)`.
  - **`tracking_processor.py`** (`TrackingProcessor`, `name="tracking"`, Sprint W9) — **não detecta nem transforma, só associa**: lê `context.detections[-1]`, chama `Tracker.track(detections)`, acumula o `TrackingResult`. **Nenhuma linha de ByteTrack aqui**. `is_enabled` reflete `settings.tracking_enabled and bool(settings.tracker)`. `reset()` delega ao Tracker.
  - **`scene_analysis_processor.py`** (`SceneAnalysisProcessor`, `name="scene_analysis"`, Sprint W10) — **não detecta, rastreia nem transforma, só interpreta**: lê `context.tracking_results[-1]`, chama `SceneAnalyzer.analyze(tracking_result)`, acumula o `SceneAnalysisResult`. **Nenhuma linha de ByteTrack/YOLO/regra de negócio de futebol aqui**. `is_enabled` reflete `settings.scene_analysis_enabled and bool(settings.scene_analyzer)`. `reset()` delega ao SceneAnalyzer.
  - **`world_model_processor.py`** (`WorldModelProcessor`, `name="world_model"`, Sprint W11) — **não detecta, rastreia, interpreta cena nem transforma a imagem, só mantém estado**: lê `context.scene_analysis_results[-1]` (o `SceneAnalysisResult` que `SceneAnalysisProcessor` acabou de produzir para o MESMO frame, mais cedo na mesma execução da pipeline), chama `WorldModel.update(scene_result)` (resolvido por `world.factory.create_world_model`), acumula o `WorldState` no contexto (`context.add_world_state`). Se nenhuma análise de cena rodou neste frame (`context.scene_analysis_results` vazio — ex.: `SceneAnalysisProcessor` desabilitado), é um no-op seguro, não levanta exceção. **Nenhuma linha de YOLO/ByteTrack/OpenCV/regra de negócio de futebol aqui** — só o contrato `WorldModel`. `is_enabled` reflete `settings.world_model_enabled and bool(settings.world_model)` — dois interruptores independentes, mesmo padrão de W9/W10. `reset()` delega a `self._world_model.reset()`.
  - **`football_domain_processor.py`** (`FootballDomainProcessor`, `name="football_domain"`, Sprint W12) — **não detecta, rastreia, interpreta cena, mantém estado genérico nem transforma a imagem, só transforma domínio**: lê `context.world_states[-1]` (o `WorldState` que `WorldModelProcessor` acabou de produzir no MESMO frame), chama `FootballWorldBuilder.build(world_state)` (`worker.domain.world_builder` — ver Seção 6.4), acumula o `FootballWorld` no contexto (`context.add_football_world`). No-op seguro se `context.world_states` estiver vazio. **Nenhuma regra de negócio de futebol aqui** — só a ponte entre `domain/` e o pipeline. `is_enabled` reflete só `settings.football_domain_enabled` — **um** interruptor, não dois (não há "qual builder" para escolher, ver Seção 6.4). `reset()` delega a `self._builder.reset()`.
  - **`worker.analyzers.processor.AnalyzerProcessor`** (`name="analyzer"`, Sprint W13, NOVO — classe vive em `worker/analyzers/processor.py`, módulo IRMÃO de `inference/`, importada aqui só para registro) — **não detecta, rastreia, interpreta cena, mantém estado genérico, transforma domínio nem transforma a imagem, só analisa**: lê `context.football_worlds[-1]` (o `FootballWorld` que `FootballDomainProcessor` acabou de produzir no MESMO frame) e, para CADA Analyzer ativo (resolvidos por `worker.analyzers.factory.create_analyzer`, a partir de `WORKER_ANALYZERS` — uma LISTA, diferente de todo `WORKER_X` anterior), chama `Analyzer.analyze(football_world)`, acumulando cada `AnalysisResult` no contexto (`context.add_analysis_result`). No-op seguro se `context.football_worlds` estiver vazio. **Nenhuma linha de YOLO/ByteTrack/OpenCV/regra de negócio de futebol aqui** — só os contratos `Analyzer`/`FootballWorld`. `is_enabled` reflete `bool(settings.analyzer_names)` — **um** único campo de lista basta como interruptor (vazio = desabilitado; diferente de Detector/Tracker/SceneAnalyzer/WorldModel, que precisam de DOIS interruptores porque só uma implementação fica ativa por vez — aqui, várias coexistem). `reset()` delega `reset()` a cada Analyzer ativo.
  - **`registry.py`** — Registry independente do de `inference/registry.py` (engines), `inference/detectors/registry.py` (Detectors), `inference/trackers/registry.py` (Trackers), `inference/events/registry.py` (SceneAnalyzers), `inference/world/registry.py` (WorldModels) e `worker/analyzers/registry.py` (Analyzers): `register_processor`/`get_processor_class`/`available_processors()`. A ordem de registro **define a ordem de execução por padrão** (`color → resize → roi → statistics → yolo → tracking → scene_analysis → world_model → football_domain → analyzer`) — por isso usa um `dict` comum (preserva ordem de inserção). `AnalyzerProcessor` é registrado por último de propósito — analisa o `FootballWorld` que o Football Domain Model acabou de produzir no mesmo frame.
  - **`pipeline.py`** — `PipelineProcessor`: única responsabilidade é **executar** a sequência de Processors habilitados; expõe `processor_names`, `from_settings(settings)` e, desde a W9, `reset()` — chama `processor.reset()` de cada Processor da pipeline, sem saber QUAL deles tem estado (ver nota de estado entre Jobs, abaixo).
- **`inference/detectors/`** (Sprint W8) — a **API de Detecção**: `Detector` (`detect(frame) -> DetectionResult`). `YOLODetector` é a primeira implementação. Ver revisão da W8.
- **`inference/trackers/`** (Sprint W9) — a **API de Tracking**: `Tracker` (`track(DetectionResult) -> TrackingResult`, + `reset()`). `ByteTrackTracker` é a primeira implementação. Ver revisão da W9.
- **`inference/events/`** (Sprint W10) — a **API de Eventos de Cena**: `SceneAnalyzer` (`analyze(TrackingResult) -> SceneAnalysisResult`, + `reset()`). `BasicSceneAnalyzer` é a primeira implementação. **Estendida na W11** com `SceneObjectSnapshot` (ver abaixo). Ver revisão da W10 para os demais detalhes.
- **`inference/world/`** (Sprint W11, NOVO) — o **World Model**: abstração `WorldModel`, independente de qualquer algoritmo/framework concreto. `BasicWorldModel` é apenas a primeira implementação — a partir daqui, futuras análises específicas de futebol (`GoalkeeperAnalyzer`, `BallAnalyzer`, `SaveAnalyzer`, `ShotAnalyzer`, `DiveAnalyzer`, `GoalAnalyzer`, Sprint W12+) consumirão **exclusivamente** `WorldState`, sem depender diretamente de `Detector`, `Tracker`, `SceneAnalyzer`, OpenCV, YOLO ou ByteTrack (ver Seção 16). **Esta é a ÚLTIMA camada genérica da arquitetura** — o World Model não implementa regra de futebol, não detecta, não rastreia, não toma decisão; só mantém uma fotografia consistente do mundo.
  - **`base.py`** — `WorldModel` (ABC): contrato único `update(scene_result: SceneAnalysisResult) -> WorldState` + `reset()` (concreto, default no-op — inerentemente stateful, mesmo motivo de `Tracker.reset()`/`SceneAnalyzer.reset()`). `WorldModel` **conhece apenas o contrato `SceneAnalysisResult`** (`worker.inference.events.types`) — nunca `Detector`, `Tracker`, OpenCV, YOLO, ByteTrack, Redis, Backend ou R2.
  - **`types.py`** — `ObjectId`/`ClassLabel`/`Confidence` (`NewType`), `BoundingBox`, `Position` (centro x/y), `Motion` (displacement/speed/direction_degrees/acceleration — **apenas matemática, nenhuma interpretação**; unidades em pixels/"por frame", já que o World Model não recebe fps, só `frame_index`). **Deliberadamente independentes** dos tipos de `trackers/types.py`/`detectors/types.py` (mesma pequena duplicação já praticada entre as camadas anteriores) — `world/` nunca importa de `trackers/`/`detectors/`, só de `events/types.py` (o único contrato de entrada permitido).
  - **`history.py`** — `History[T]`: buffer circular genérico (`collections.deque(maxlen=N)`), nunca cresce além de `max_size`. Reutilizado por `Trajectory` (posições) e pelo próprio `WorldModel` (eventos recentes) — uma única implementação de "buffer limitado", evita duplicar a lógica de descarte em dois lugares.
  - **`trajectory.py`** — `Trajectory`: histórico limitado de posições de um objeto, composição sobre `History` (nunca duplica a lógica de buffer). Tamanho configurável via `WORKER_WORLD_MAX_TRAJECTORY`.
  - **`motion.py`** — `compute_motion(previous_position, current_position, previous_motion) -> Motion`: função pura, calcula deslocamento (distância euclidiana), velocidade (= deslocamento, unidade "por frame"), direção (ângulo em graus, `atan2`) e aceleração (variação de velocidade desde a chamada anterior). Sem posição anterior, devolve cinemática zerada.
  - **`object_state.py`** — `ObjectState`: estado completo de um objeto do mundo — track_id/label/confidence/bbox/previous_bbox/position/motion/trajectory (lista de `Position`, já achatada — não é a `Trajectory` viva)/age/frames_visible/frames_hidden/active/first_seen_frame/last_seen_frame + `time_in_scene_frames` (propriedade calculada, em FRAMES — não em segundos, já que o World Model não conhece fps; documentado honestamente, mesmo padrão da nota sobre `OBJECT_ENTERED_FRAME`/`OBJECT_LEFT_FRAME` na W10). **Nenhuma lógica específica de goleiro.**
  - **`world_state.py`** — `WorldStatistics` (object_count/active_tracks/lost_tracks/new_tracks/average_speed — reflete o estado ATUAL, não cumulativo como `TrackingStatistics`/`SceneStatistics`, já que `WorldState` é uma fotografia, não um log), `WorldState` (frame_index/active_objects/lost_objects/new_objects/recent_events/statistics + `to_dict()`) — **o único tipo que os analisadores futuros de futebol poderão consumir**.
  - **`context.py`** — `WorldModelContext`: `ObjectState` mais recente conhecido de cada `track_id` — memória interna MUTÁVEL de uma instância de `WorldModel`, **não é** o `ProcessorContext` do pipeline; análogo ao que `SceneAnalysisContext` é para `SceneAnalyzer`. `reset()` limpa `objects`.
  - **`exceptions.py`** — `WorldModelError` → `WorldModelInitializationError`/`WorldModelExecutionError`, ambas de `WorkerError`.
  - **`registry.py`** — `register_world_model`/`get_world_model_class`/`available_world_models()`. Registra `"basic"` hoje.
  - **`factory.py`** — `create_world_model(nome, settings)`: espelha `events/factory.py`; envolve qualquer falha de inicialização numa `WorldModelInitializationError`.
  - **`world_model.py`** (`BasicWorldModel`) — primeira implementação real. **Princípio de implementação:** nunca muta um `ObjectState` existente — sempre constrói uma instância NOVA e substitui a entrada em `WorldModelContext`, garantindo que um `WorldState` já devolvido continue sendo uma fotografia fiel daquele instante mesmo que o WorldModel siga avançando internamente (evita o "vazamento de mutabilidade" que aconteceria se `active_objects`/`lost_objects` compartilhassem referências vivas). A cada `update()`: objetos presentes em `scene_result.objects` mas desconhecidos → `new_objects` (age=1); presentes e já conhecidos → `active_objects` (age incrementado, `Motion` recomputado via `compute_motion`, ponto adicionado à `Trajectory`); conhecidos mas ausentes → `lost_objects` (`frames_hidden` incrementado, `active=False`); reaparecendo → volta a `active_objects` (`frames_hidden` zerado). `WORKER_WORLD_MAX_OBJECTS` limita o total de objetos retidos (ativos + perdidos) — ao exceder, remove os objetos **perdidos** mais antigos primeiro (nunca um ativo); `<=0` desativa o limite. `reset()` limpa `WorldModelContext` + o histórico de eventos recentes.
  - **`SceneObjectSnapshot`** (adicionado a `events/types.py` NESTA sprint, não em `world/`) — **achado arquitetural necessário**: `SceneEvent` (W10) descreve só TRANSIÇÕES (começou/parou/ficou oculto), nunca carrega bbox/confidence. Como `WorldModel.update()` só pode receber `SceneAnalysisResult` (nunca `TrackingResult` diretamente — regra explícita desta sprint), faltava um canal para os dados posicionais chegarem ao World Model. Resolvido estendendo `SceneAnalysisResult` com um novo campo `objects: list[SceneObjectSnapshot]` (track_id/label/confidence/bbox — populado por `BasicSceneAnalyzer.analyze()` a partir do `TrackingResult` que ele já recebia) — extensão aditiva, `default_factory=list`, não quebra nenhum contrato/teste anterior da W10.
- **`FakeInferenceEngine`** (`fake_engine.py`) — implementação placeholder, mantida **exclusivamente para testes**.
- **`frame_ops.py`** (Sprint W6) — funções puras, chamadas pelos Processors correspondentes (nunca duplicadas).
- **`registry.py`** (motores) — `register_engine`/`get_engine_class`/`available_engines()`. **Distinto** dos Registries de Processors, Detectors, Trackers, SceneAnalyzers, WorldModels e Analyzers — sete Registries paralelos, um por camada de plugin.
- **`engine.py`** — `create_engine(nome, settings)`. Todo motor registrado é construído como `engine_class(settings)` (convenção uniforme de construtor — mesma convenção adotada por Processors, Detectors, Trackers, SceneAnalyzers, WorldModels e Analyzers).
- **`types.py`** — `Detection`, `FrameMetadata`, `RegionOfInterest`, `InferenceMetadata`, `InferenceResult`. **Não depende de `inference/processors/`, `inference/detectors/`, `inference/trackers/`, `inference/events/` nem `inference/world/`** — a direção de dependência é sempre das camadas mais concretas para esta (a base), nunca o inverso.
- **`exceptions.py`** — `InferenceError` → `EngineInitializationError`/`InferenceExecutionError`. **Distinta** de `detectors/exceptions.py`, `trackers/exceptions.py`, `events/exceptions.py` e `world/exceptions.py` (`WorldModelError`) — cada família de Plugin com sua própria árvore de exceções, todas descendendo de `WorkerError`.

**Estado entre Jobs (achado arquitetural da Sprint W9, reaplicado sem mudanças na W10/W11, e proativamente na W12/W13):** `WorkerOrchestrator.__init__` constrói `InferenceStage(create_engine(...))` **uma única vez**, para todo o ciclo de vida do processo do Worker — `BasicVisionEngine`/`PipelineProcessor`/cada `FrameProcessor` (incluindo `TrackingProcessor`/`ByteTrackTracker`, `SceneAnalysisProcessor`/`BasicSceneAnalyzer`, `WorldModelProcessor`/`BasicWorldModel`, `FootballDomainProcessor`/`FootballWorldBuilder` e, desde a W13, `AnalyzerProcessor`/cada `Analyzer` ativo) são, portanto, **reaproveitados entre Jobs sequenciais**, não recriados por Job. **Corrigido, uma única vez, de forma genérica**, na W9 — com o par `FrameProcessor.reset()`/`X.reset()` + `PipelineProcessor.reset()` + `BasicVisionEngine.process()` chamando `self._pipeline.reset()` no início de cada Job. Nenhuma sprint desde então **precisou reinventar esse mecanismo** — cada uma só implementou `XProcessor.reset()`/`X.reset()` reaproveitando a plumbing já existente (quinta confirmação consecutiva na W13: `AnalyzerProcessor.reset()` delega a `analyzer.reset()` de cada Analyzer ativo; `GoalkeeperPresenceAnalyzer` nem precisa sobrescrever, já que é sem estado). Validado por teste automatizado (`test_engine_resets_tracker_state_between_jobs`, W9; `test_engine_resets_scene_analyzer_state_between_jobs`, W10; `test_engine_resets_world_model_state_between_jobs`, W11; `test_engine_resets_football_domain_state_between_jobs`, W12; `test_engine_resets_analyzer_state_between_jobs`, W13).

**Configuração (todas opcionais):** `WORKER_FRAME_SKIP`, `WORKER_ENABLE_RESIZE`/`WORKER_TARGET_WIDTH`/`WORKER_TARGET_HEIGHT`, `WORKER_ENABLE_ROI`/`WORKER_ROI_X`/`WORKER_ROI_Y`/`WORKER_ROI_WIDTH`/`WORKER_ROI_HEIGHT`, `WORKER_ENABLE_COLOR_PROCESSOR`, `WORKER_ENABLE_STATISTICS_PROCESSOR`, `WORKER_DETECTOR`/`WORKER_MODEL_PATH`/`WORKER_CONFIDENCE_THRESHOLD`/`WORKER_IOU_THRESHOLD`, `WORKER_TRACKER`/`WORKER_TRACKING_ENABLED`/`WORKER_TRACK_MIN_CONFIDENCE`/`WORKER_TRACK_MAX_AGE`/`WORKER_TRACK_MIN_HITS`, `WORKER_SCENE_ANALYZER`/`WORKER_SCENE_ANALYSIS_ENABLED`/`WORKER_SCENE_MOTION_THRESHOLD_PX`/`WORKER_SCENE_OCCLUSION_IOU_THRESHOLD`, `WORKER_WORLD_MODEL`/`WORKER_WORLD_MODEL_ENABLED`/`WORKER_WORLD_HISTORY_SIZE`/`WORKER_WORLD_MAX_TRAJECTORY`/`WORKER_WORLD_MAX_OBJECTS`, `WORKER_FOOTBALL_DOMAIN_ENABLED` (um único interruptor, sem par "qual implementação" — Seção 6.4), `WORKER_ANALYZERS` (Sprint W13 — lista separada por vírgula, vazio = desabilitado, ver Seção 6.5) — todas lidas por `WorkerSettings`, cada uma consultada pelo `is_enabled()` do Processor correspondente (ou, no caso do frame-skip, diretamente pelo engine).

**Artefato JSON:** ao `to_dict()` de `InferenceResult`, `BasicVisionEngine` acrescenta: `"processors"`/`"processor_order"` (W7), `"detection_results"` (W8), `"tracking_results"`/`"tracking_engine"`/`"tracking_statistics"`/`"tracking_time_ms"` (W9), `"scene_events"`/`"scene_statistics"`/`"scene_processing_time_ms"` (W10), `"world_state"`/`"world_statistics"`/`"object_count"`/`"active_tracks"`/`"lost_tracks"`/`"average_speed"`/`"processing_time_ms"` (W11), `"football_world"`/`"football_domain_time_ms"` (W12) e, desde a W13: `"analysis_results"` (dict `analyzer_name → AnalysisResult.to_dict()`, o ÚLTIMO resultado de CADA Analyzer ativo), `"analysis_statistics"` (`AnalysisStatistics.to_dict()` — `analyzers_run`/`results_count`) e `"analyzer_processing_time_ms"` (lido de `context.stats["analyzer"].total_time_ms` — **nome deliberadamente prefixado**, não reaproveita o `"processing_time_ms"` genérico da W11, que já pertence ao World Model). Desde a W14: `"goal_geometry_result"` (alias de conveniência de `analysis_results["goal_geometry"]`). Desde a W15: `"goalkeeper_position_result"` (alias de `analysis_results["goalkeeper_position"]`). Desde a W16: `"ball_position_result"` (alias de `analysis_results["ball_position"]`). Desde a W17: `"goalkeeper_ball_alignment_result"` (alias de `analysis_results["goalkeeper_ball_alignment"]`). Desde a W18: `"ball_motion_result"` (alias de `analysis_results["ball_motion"]`). Desde a W19: `"shot_analysis_result"` (alias de `analysis_results["shot"]`). Desde a W20: `"ball_trajectory_result"` (alias de `analysis_results["ball_trajectory"]`). Desde a W21: `"play_situation_result"` (alias de `analysis_results["play_situation"]`). Desde a W22: `"goalkeeper_decision_result"` (alias de `analysis_results["goalkeeper_decision"]`). Desde a W23: `"goalkeeper_decision_evaluation_result"` (alias de `analysis_results["goalkeeper_decision_evaluation"]`). Desde a W24: `"play_outcome_result"` (alias de `analysis_results["play_outcome"]`). Desde a W25: `"goalkeeper_performance_evaluation_result"` (alias de `analysis_results["goalkeeper_performance_evaluation"]`). Desde a W26: `"goalkeeper_coaching_result"` (alias de `analysis_results["goalkeeper_coaching"]`). Desde a W27: `"goalkeeper_analysis_report"` (alias de `analysis_results["goalkeeper_analysis_report"]` — **o CONTRATO OFICIAL de saída do Worker**, ver Seção 6.5). Um Processor/Analyzer desabilitado simplesmente não aparece em nenhuma das chaves.

**Integrar um novo Processor de transformação (ex.: um futuro `PoseProcessor`):** escrever `XProcessor(FrameProcessor)` em `inference/processors/`, registrá-lo, habilitá-lo por `is_enabled()`/config — **sem alterar** `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator` ou `VideoReader`.

**Trocar o Detector/Tracker/SceneAnalyzer ativo:** mesmo padrão de sempre — escrever uma nova classe que implemente o contrato da família, registrá-la, apontar a variável `WORKER_*` correspondente — sem alterar Processor, `PipelineProcessor`, `BasicVisionEngine`, `VideoReader`, `WorkerOrchestrator`, Redis, Backend ou R2. Comprovado por teste automatizado em cada família (`tests/inference/{detectors,trackers,events}/test_factory.py`).

**Trocar o WorldModel ativo:** escrever `XWorldModel(WorldModel)` em `inference/world/`, registrá-lo em `world/registry.py` (`register_world_model("x", XWorldModel)`), apontar `WORKER_WORLD_MODEL=x` — **sem alterar** `WorldModelProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `VideoReader`, `WorkerOrchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, Redis, Backend ou R2. Comprovado por teste automatizado (`tests/inference/world/test_factory.py`).

**Construir um novo Analyzer (ex.: `GoalkeeperPositionAnalyzer`/`BallAnalyzer`/`ShotAnalyzer`/`SaveAnalyzer`/`DiveAnalyzer`/`GoalAnalyzer`, Sprint W14+):** escrever `XAnalyzer(Analyzer)` em `worker/analyzers/`, registrá-lo em `analyzers/registry.py` (`register_analyzer("x", XAnalyzer)`), incluir `"x"` em `WORKER_ANALYZERS` — **sem alterar** `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel`, `FootballDomainProcessor`, Redis, Backend ou R2. Consome **exclusivamente** `FootballWorld` — **sem depender diretamente** de `WorldState`, `SceneAnalysisResult`, `TrackingResult`, `DetectionResult`, OpenCV, YOLO ou ByteTrack (ver Seção 16). Comprovado por teste automatizado (`tests/analyzers/test_factory.py`, `tests/analyzers/test_registry.py`). **A W13 ainda não é a sprint da regra de negócio real** — `GoalkeeperPresenceAnalyzer` responde só fatos determinísticos (existe/quantos/qual track_id/visível/posição/bbox), nenhuma heurística, nenhuma avaliação, nenhum julgamento; a primeira análise com julgamento (defesa boa/ruim, chute no gol, mergulho) fica para a W14+.

## 6.2 Estado do Pipeline e Workspace (Implementação Real — Sprint W3)

- **`PipelineState`** (`state/pipeline_state.py`) — todo o estado de um Job em processamento, passado de Stage em Stage: `job_id`, `video_id`, `message_id`, `started_at`, `job` (`JobDetails`), `workspace_dir`, `download_path`, `artifact_path`, `inference_result`, `lock_acquired`, `status`, `finished_at`, `errors`.
- **`WorkspaceManager`** (`workspace/manager.py`) — `create(job_id)`/`cleanup(dir)`, sempre via `tempfile.mkdtemp` (nunca caminho hardcoded), garantindo isolamento entre Jobs mesmo na mesma máquina. Usa o diretório temporário do sistema operacional, **não** o `goalkeeper_ai_worker/workspace/` citado na Seção 12 — decisão deliberada para cumprir literalmente "sempre tempfile" (`SPRINT_W3_REPORT.md`).
- **Eventos internos** (`events/events.py`) — `JobStarted` (em `ReceiveJobStage`), `VideoDownloaded` (em `DownloadVideoStage`), `UploadFinished` (em `UploadArtifactStage`), `JobCompleted`/`JobFailed` (no `orchestrator`, conforme o desfecho) são os realmente emitidos hoje. `ArtifactGenerated` continua definida mas órfã — era específica da extinta `GenerateArtifactStage` (ver Seção 1). Só logging por enquanto; `video_id`/`job_id` sempre presentes (Correlation ID, Seção 3). **Correção de curso (Sprint W10):** a expectativa original (fechada na revisão W4.1) de acomodar um futuro Event Registry técnico como submódulo AQUI DENTRO de `worker/events/` **não se concretizou assim** — a API de Eventos de Cena (Seção 6.1) foi implementada em `worker/inference/events/`, mais perto da camada de inferência que a consome. `worker/events/` permanece só com `events.py`.

## 6.3 Infraestrutura de Leitura de Vídeo (Implementação Real — Sprint W5)

Módulo `video/` — **irmão** de `inference/`, nunca dentro dele. Resolve a questão que a Seção 15 original deixou em aberto ("onde `VideoReader` mora"): aqui, separado da camada de IA, reutilizável por qualquer motor presente ou futuro.

- **`VideoReader`** (`reader.py`) — abre (`cv2.VideoCapture`, só como biblioteca de leitura — nunca `cv2.dnn`), valida (rejeita `frame_count`/`fps`/dimensões inválidos), expõe `VideoProperties`; suporta `with VideoReader(path) as reader:`.
- **`FrameProvider`** (`provider.py`) — leitura sequencial (`read_next()`); distingue fim normal do vídeo (`None`) de falha real de leitura antes do fim esperado (`FrameReadError`).
- **`FrameIterator`** (`iterator.py`) — protocolo padrão de iterador do Python (`__iter__`/`__next__`, `StopIteration` no fim).
- **`Frame`** (`frame.py`) — a imagem (`numpy.ndarray`, BGR nativo do OpenCV) + `FrameMetadata`.
- **`FrameMetadata`** (`metadata.py`) — posição do frame (`frame_index`/`timestamp_seconds`/`position_seconds`) + propriedades do vídeo de origem, denormalizadas para o `Frame` ser um registro autocontido.
- **`VideoProperties`** (`types.py`) — `fps`/`width`/`height`/`frame_count`/`duration_seconds`, lidas uma única vez na abertura.
- **`exceptions.py`** — `VideoError` → `VideoOpenError` (arquivo inexistente/ilegível) / `InvalidVideoError` (metadados inválidos) / `FrameReadError` (falha de leitura antes do fim esperado), todas de `WorkerError`.

`DownloadVideoStage` salva o vídeo sem extensão (`input_video`) — confirmado empiricamente (Sprint W5) que o OpenCV abre esses arquivos via detecção de conteúdo, então nenhuma mudança foi necessária ali.

---

## 6.4 Football Domain Model (Implementação Real — Sprint W12)

Módulo `worker/domain/` — **pacote de topo, IRMÃO de `inference/`, não um submódulo dele.** Marca a fronteira real entre "infraestrutura de visão computacional genérica" (`inference/` — Detector/Tracker/SceneAnalyzer/WorldModel, todos concluídos até a W11) e "modelagem do domínio de futebol" (`domain/`, a partir da W12). Transforma `WorldState` (`worker.inference.world.world_state`, o último tipo genérico) em `FootballWorld` (goleiro(s), bola(s), jogadores, balizas, campo). **Esta sprint não implementa análise, IA ou regra de negócio** — só os conceitos fundamentais do domínio; nenhuma decisão é tomada, nenhuma interpretação é realizada, nenhuma heurística existe.

- **`football_world.py`** — `FootballWorld`: a fotografia do domínio de futebol num dado frame (`frame_index`/`goalkeepers`/`players`/`balls`/`goals`/`field` + `to_dict()`). **O único tipo que os analisadores futuros de futebol poderão consumir** (`GoalkeeperAnalyzer`/`BallAnalyzer`/`SaveAnalyzer`/`ShotAnalyzer`/`DiveAnalyzer`/`GoalAnalyzer`, Sprint W13+) — nenhum deles conhecerá `WorldState` diretamente (ver Seção 16). `goalkeepers`/`players`/`balls` são sempre LISTAS (nunca um único opcional) — desambiguar "qual É o goleiro/a bola de verdade" entre candidatos exigiria uma heurística, explicitamente fora de escopo desta sprint.
- **`world_builder.py`** — `FootballWorldBuilder`: única responsabilidade, `build(world_state: WorldState) -> FootballWorld`. **A única "decisão" desta sprint — e não é uma heurística comportamental:** despacha cada `ObjectState` (de `world_state.active_objects`) para `Goalkeeper`/`Ball`/`Player` comparando `ObjectState.label` (já atribuído pelo Detector, camadas abaixo) contra três conjuntos fixos de rótulos conhecidos — um despacho estrutural por tipo, não uma inferência sobre o comportamento do objeto. **Honestidade documentada:** com o modelo padrão de hoje (YOLO11n, treinado em COCO), o único rótulo de pessoa que existe é `"person"` — por isso, na prática, todo mundo aparece como `Player` e `FootballWorld.goalkeepers` fica vazio até que um modelo treinado especificamente para este domínio (com uma classe `"goalkeeper"` própria) seja usado; confirmado na validação manual desta sprint. Constrói `Field`/`Goal` uma única vez por Job (via `FootballDomainContext`) e os reaproveita em cada frame — não recria a cada chamada. `reset()` limpa esse cache — **aplicado PROATIVAMENTE nesta sprint**, sem precisar redescobrir a lição da W9 (diferente de W10/W11, que reaproveitaram a plumbing já existente; a W12 é a primeira vez que uma camada nasce JÁ ciente do problema de estado entre Jobs).
- **`context.py`** — `FootballDomainContext`: `field`/`goals` memorizados entre chamadas de uma mesma instância de `FootballWorldBuilder` — **não é** o `ProcessorContext` do pipeline; análogo ao que `WorldModelContext`/`SceneAnalysisContext` são para as camadas anteriores.
- **`types.py`** — `EntityId`/`ClassLabel`/`Confidence` (`NewType`, mesma pequena duplicação já praticada entre `inference/detectors`/`trackers`/`events`/`world`), `TrackedFootballEntity` (a base compartilhada de `Goalkeeper`/`Ball`/`Player` — os três têm exatamente a mesma forma nesta sprint, mas são classes DISTINTAS de propósito, para que analisadores futuros exijam o tipo certo via assinatura, não uma união genérica).
- **`exceptions.py`** — `FootballDomainError` → `FootballWorldBuildError`, ambas de `WorkerError`.
- **`entities/`** — `Goalkeeper`/`Player`/`Ball` (`goalkeeper.py`/`player.py`/`ball.py`, todos subclasses de `TrackedFootballEntity` — conversão 1:1 dos dados já computados por `ObjectState`, apenas trocando de representação: `Position`→`Coordinate`, `Motion`→`Vector`; nenhum dado novo é inferido), `Goal`/`Field` (`goal.py`/`field.py` — entidades ESTÁTICAS, não rastreadas frame a frame; `Field.default()`/`Goal.default_pair()` produzem geometria PLACEHOLDER normalizada (0.0-1.0), **documentada honestamente como não calibrada com o vídeo real** — sem informação de câmera/marcações do campo disponível nesta camada; `direction` sempre `Direction.UNKNOWN` por padrão, nenhuma heurística tenta inferi-la).
- **`geometry/`** — utilitários puros, reutilizáveis por todos os analisadores futuros: `Coordinate`/`distance()` (`coordinate.py`), `Vector`/`angle_between()`/`Vector.from_polar()` (conversão polar↔cartesiana, usada para transformar `Motion.speed`/`direction_degrees` do World Model num vetor de velocidade do domínio — só conversão de representação) (`vector.py`), `Region` (retângulo com `contains()`/`center`) (`region.py`), `Direction` (enum `left_to_right`/`right_to_left`/`unknown`, distinto do ângulo contínuo de `Vector` — direção de ATAQUE categórica, não o movimento instantâneo de um objeto) (`direction.py`).

**Ausência deliberada de Registry/factory:** diferente de Detector/Tracker/SceneAnalyzer/WorldModel (cada um uma família de Plugin substituível, com Registry próprio), `FootballWorldBuilder` é a ÚNICA implementação do domínio — não uma família de algoritmos intercambiáveis, e sim a estrutura definitiva de como o Worker representa futebol. Por isso `worker/domain/` não tem `registry.py`/`factory.py`, e `WorkerSettings.football_domain_enabled` é um único interruptor (sem "qual implementação").

**`FootballDomainProcessor`** (`inference/processors/football_domain_processor.py`) — Processor que faz a ponte entre `domain/` e o pipeline (vive em `processors/`, não em `domain/`, mesmo padrão de todo Processor anterior): lê `context.world_states[-1]` (o `WorldState` que `WorldModelProcessor` acabou de produzir no MESMO frame), chama `FootballWorldBuilder.build()`, acumula o `FootballWorld` no contexto. No-op se não houver World Model no frame. `is_enabled` reflete só `settings.football_domain_enabled` (um interruptor, não dois — não há "qual builder" para escolher). `reset()` delega ao builder. Registrado logo antes do `AnalyzerProcessor` na pipeline (`color → resize → roi → statistics → yolo → tracking → scene_analysis → world_model → football_domain → analyzer`, ver Seção 6.5).

**Artefato JSON:** `"football_world"` (o ÚLTIMO `FootballWorld` — singular, mesma convenção de `"world_state"`) e `"football_domain_time_ms"` (lido de `context.stats["football_domain"].total_time_ms`).

**Boundary Enforcement desta sprint (confirmado por grep, `SPRINT_W12_REPORT.md`):** todo `import` real de `worker/domain/**/*.py` é só stdlib (`math`/`dataclasses`/`enum`/`typing`) + `worker.config.settings` + `worker.core.exceptions` + `worker.inference.world.{object_state,world_state}` (o único ponto de entrada permitido) + módulos internos de `worker.domain.*`. Nenhum `import` de `worker.inference.detectors`/`trackers`/`events`, `cv2`, `ultralytics`, `redis`, `worker.infrastructure.*`. Toda menção a `Detector`/`Tracker`/`SceneAnalyzer`/`YOLO`/`ByteTrack`/`OpenCV` em `domain/` é só docstring, explicando o que NÃO é conhecido.

---

## 6.5 Analyzer API (Implementação Real — Sprint W13/W14/W15/W16/W17/W18/W19/W20/W21/W22/W23/W24/W25/W26/W27)

Módulo `worker/analyzers/` — **pacote de topo, IRMÃO de `worker/domain/` e de `inference/`, não um submódulo de nenhum dos dois.** Marca a fronteira real entre "modelar o domínio" (`domain/`, W12 — só estrutura, nenhuma pergunta respondida) e "analisar o domínio" (`analyzers/`, a partir da W13 — responde perguntas sobre o `FootballWorld`). `GoalkeeperPresenceAnalyzer` (W13), `GoalGeometryAnalyzer` (W14), `GoalkeeperPositionAnalyzer` (W15), `BallPositionAnalyzer` (W16), `GoalkeeperBallAlignmentAnalyzer` (W17), `BallMotionAnalyzer` (W18) são puramente factuais/geométricos/de medição — nenhuma decisão. **`ShotAnalyzer` (W19) é o primeiro a tomar uma DECISÃO** (`shot_detected: bool`), mas via critérios 100% determinísticos e parametrizáveis — NUNCA detecta gol, NUNCA avalia defesa/qualidade, NUNCA é uma heurística opaca ou modelo treinado. **`BallTrajectoryAnalyzer` (W20) volta a ser puramente descritivo** — modela a FORMA da trajetória observada (não um evento binário como o `ShotAnalyzer`), sem julgar qualidade/perigo/precisão. **`PlaySituationAnalyzer` (W21) é o primeiro Analyzer COGNITIVO** — classifica o estado OBSERVADO da jogada atual (`PlaySituation`, fortemente tipado), combinando exclusivamente resultados já produzidos por quatro Analyzers existentes. **`GoalkeeperDecisionAnalyzer` (W22) é o primeiro Analyzer ESPECÍFICO DO GOLEIRO** — classifica a decisão OBSERVADA que o goleiro aparenta estar executando (`GoalkeeperDecision`, fortemente tipado); ainda assim NUNCA avalia se a decisão foi correta, NUNCA dá nota, NUNCA julga desempenho ou resultado da jogada — é uma CLASSIFICAÇÃO de comportamento, não uma opinião. **`GoalkeeperDecisionEvaluationAnalyzer` (W23) é o primeiro Analyzer de AVALIAÇÃO** — responde apenas "a decisão observada foi COMPATÍVEL com a situação observada?" (`GoalkeeperDecisionEvaluation`, fortemente tipado), via um mecanismo explícito, genérico e auditável de Rule Evaluation (`worker.analyzers.rules`, ver abaixo); ainda assim NUNCA avalia o RESULTADO da jogada (não determina defesa/gol), NUNCA julga desempenho — é uma verificação de CONSISTÊNCIA lógica entre camadas já produzidas, nunca uma reinterpretação delas. **`PlayOutcomeAnalyzer` (W24) encerra a cadeia de observação** — responde apenas "qual foi o resultado OBSERVADO da jogada?" (`PlayOutcome`, fortemente tipado); ainda assim NUNCA avalia o goleiro, NUNCA atribui nota, NUNCA julga a qualidade da decisão — é a última camada puramente OBSERVACIONAL, sem nenhum julgamento de mérito. **`GoalkeeperPerformanceEvaluationAnalyzer` (W25) encerra a cadeia de avaliação** — responde apenas "como foi o desempenho OBSERVADO do goleiro nesta jogada?" (`GoalkeeperPerformanceEvaluation`, fortemente tipado), combinando EXCLUSIVAMENTE `GoalkeeperDecisionEvaluation` (W23) e `PlayOutcome` (W24) via a MESMA infraestrutura de Rule Evaluation da W23; ainda assim NUNCA gera recomendação, NUNCA faz coaching, NUNCA sugere melhoria — é a classificação FINAL da jogada. **`GoalkeeperCoachingAnalyzer` (W26) é o primeiro Analyzer de COACHING** — responde apenas "qual orientação técnica pode ser extraída desta jogada?" (`GoalkeeperCoaching`, fortemente tipado), interpretando `GoalkeeperPerformanceEvaluation` (W25) combinado com `GoalkeeperDecisionEvaluationResult.rules_failed` (W23, para identificar QUAL desvio específico motivou a orientação), `GoalkeeperDecision` (W22) e `PlayOutcome` (W24) via a MESMA infraestrutura de Rule Evaluation da W23/W25; ainda assim NUNCA gera linguagem natural, NUNCA produz relatório final, NUNCA sugere um plano de treino — é uma classificação determinística de ORIENTAÇÃO, não um texto de coaching. **`GoalkeeperAnalysisReportAnalyzer` (W27) encerra oficialmente o MVP arquitetural** — NÃO produz nenhuma conclusão nova, NÃO recalcula nada, NÃO executa nenhuma regra nova (nem reutiliza `worker.analyzers.rules` diretamente - é agregação pura, não avaliação): apenas AGREGA os seis resultados já produzidos por `PlaySituationAnalyzer`/`GoalkeeperDecisionAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer`/`GoalkeeperPerformanceEvaluationAnalyzer`/`GoalkeeperCoachingAnalyzer` num único `GoalkeeperAnalysisReport` — **o CONTRATO OFICIAL de saída do Worker** (ver "Contrato Oficial de Saída" abaixo). Primeira sprint da cadeia cognitiva (W21-W27) que NÃO introduz nenhum `Enum` novo em `types.py` — não há nenhuma classificação nova a tipar, só consolidação de classificações já existentes.

**A cadeia completa desta arquitetura (W13-W27, MVP arquitetural encerrado):** Observação (W13-W20, factos/geometria/cinemática/eventos/trajetória, sem decisão) → Situação (W21, `PlaySituationAnalyzer`, classificação do estado observado) → Decisão (W22, `GoalkeeperDecisionAnalyzer`, classificação do comportamento do goleiro) → Avaliação da Decisão (W23, `GoalkeeperDecisionEvaluationAnalyzer`, compatibilidade decisão-situação via Rule Evaluation) → Resultado (W24, `PlayOutcomeAnalyzer`, resultado observado da jogada) → Avaliação de Desempenho (W25, `GoalkeeperPerformanceEvaluationAnalyzer`, cruza Avaliação da Decisão + Resultado numa matriz determinística) → Coaching (W26, `GoalkeeperCoachingAnalyzer`, extrai uma orientação técnica fortemente tipada a partir da cadeia completa) → Relatório Consolidado (W27, `GoalkeeperAnalysisReportAnalyzer`, agrega os seis resultados anteriores num único `GoalkeeperAnalysisReport`, o contrato oficial). Cada camada só COMBINA a anterior, nunca a modifica ou reinterpreta. Itens de evolução pós-MVP (Pose Estimation, calibração automática, múltiplas câmeras, tracking 3D, física avançada da bola, Machine Learning para avaliação, feedback em linguagem natural, métricas estatísticas entre sessões) estão registrados, sem sprint definida, na Seção 16.

- **`base.py`** — `Analyzer` (ABC): contrato único `analyze(football_world: FootballWorld) -> AnalysisResult` + `reset()`. **Não mudou desde a W13** — a W19 confirmou que o mesmo contrato de UM argumento basta até para um Analyzer que compõe TRÊS outros (dois deles já compostos por outros) e mantém seu próprio estado adicional; a W20 confirma o mesmo para um Analyzer que acumula uma SEQUÊNCIA de posições ao longo de N frames; a W21 confirma o mesmo para um Analyzer que compõe QUATRO outros sem manter nenhum estado próprio; a W22 confirma o mesmo para um Analyzer que compõe CINCO outros E mantém seu próprio estado adicional; a W23 confirma o mesmo para um Analyzer que compõe SEIS outros (incluindo três já stateful) sem manter nenhum estado próprio; a W24 confirma o mesmo para um Analyzer que compõe CINCO outros E mantém seu próprio estado, lendo também `FootballWorld` diretamente; a W25 confirma o mesmo para um Analyzer que compõe QUATRO outros (dois deles já stateful, transitivamente) sem manter nenhum estado próprio, e que delega sua lógica de classificação ao MESMO mecanismo de regras genérico já usado pela W23 (`worker.analyzers.rules`), sem criar um segundo mecanismo; a W26 confirma o mesmo para um Analyzer que compõe QUATRO outros (um deles, `GoalkeeperPerformanceEvaluationAnalyzer`, já compondo outros quatro transitivamente) e que lê diretamente `rules_passed`/`rules_failed` de um Analyzer composto (Explainability da W23) como entrada das suas PRÓPRIAS regras, sem recalcular nenhuma lógica de compatibilidade; a W27 confirma o mesmo para um Analyzer que compõe SEIS outros simultaneamente (o maior número de composição direta até aqui) sem executar NENHUMA regra própria - o primeiro Analyzer cognitivo cujo `analyze()` é agregação pura, sem nenhum `if`/árvore de decisão de conteúdo.
- **`results.py`** — `AnalyzerMetadata`, `AnalysisResult` (base), `GoalkeeperPresenceResult` (W13), `GoalGeometryResult` (W14), `GoalkeeperPositionResult` (W15), `BallPositionResult` (W16), `GoalkeeperBallAlignmentResult` (W17), `BallMotionResult` (W18), `ShotAnalysisResult` (W19), `BallTrajectoryResult` (W20), `PlaySituationResult` (W21), `GoalkeeperDecisionResult` (W22), `GoalkeeperDecisionEvaluationResult` (W23), `PlayOutcomeResult` (W24), `GoalkeeperPerformanceEvaluationResult(AnalysisResult)` (performance/decision_evaluation/play_outcome/rules_evaluated/rules_passed/rules_failed/summary/confidence, W25), `GoalkeeperCoachingResult(AnalysisResult)` (coaching/performance/decision_evaluation/decision/outcome/rules_evaluated/rules_passed/rules_failed/summary/confidence, W26), `GoalkeeperAnalysisReport(AnalysisResult)` (play_situation/goalkeeper_decision/decision_evaluation/play_outcome/performance_evaluation/coaching — os seis sub-resultados TIPADOS e INTEGRAIS — mais confidence_summary/artifacts/analysis_version/worker_version/generated_at, W27), `AnalysisStatistics`.
- **`goalkeeper_presence.py`**/**`goal_geometry.py`**/**`goalkeeper_position.py`**/**`ball_position.py`**/**`goalkeeper_ball_alignment.py`**/**`ball_motion.py`**/**`shot.py`**/**`ball_trajectory.py`**/**`play_situation.py`**/**`goalkeeper_decision.py`**/**`goalkeeper_decision_evaluation.py`**/**`play_outcome.py`** — implementações 1ª a 12ª (W13-W24), ver Seção 15 para o histórico completo de cada uma.
- **`rules.py`** (W23) — mecanismo de **Rule Evaluation**: `Rule[T]` (`id`/`description`/`condition: Callable[[T], bool | None]`), `RuleOutcome` (`rule_id`/`description`/`passed`/`explanation`), `evaluate_rules(rules, context) -> list[RuleOutcome]`. `condition` devolve `True` (satisfeita) / `False` (violada) / `None` (não aplicável a este caso - não conta nem como sucesso nem como falha). Não pertence a nenhum Analyzer específico - reutilizável por qualquer Analyzer de avaliação futuro. `PlayOutcomeAnalyzer` (W24) não reutilizou este mecanismo (usa `supporting_evidence: list[str]`); **`GoalkeeperPerformanceEvaluationAnalyzer` (W25) e `GoalkeeperCoachingAnalyzer` (W26) VOLTAM a reutilizá-lo** - por instrução explícita em ambas, nenhum segundo mecanismo de regras foi criado, apenas novas `Rule`s específicas (8 de desempenho na W25, 8 de coaching na W26). **`GoalkeeperAnalysisReportAnalyzer` (W27) NÃO reutiliza este mecanismo** - não é uma sprint de avaliação, é de agregação pura; zero `Rule`s novas. Ver "Rule Evaluation e Explainability" abaixo.
- **`goalkeeper_performance_evaluation.py`** — `GoalkeeperPerformanceEvaluationAnalyzer` (W25): décima terceira implementação, **encerra a cadeia de avaliação**, sem `AnalyzerContext` próprio (combinador puro, mesmo padrão de `PlaySituationAnalyzer`/W21 e `GoalkeeperDecisionEvaluationAnalyzer`/W23). Compõe QUATRO Analyzers internamente — `PlaySituationAnalyzer`, `GoalkeeperDecisionAnalyzer`, `GoalkeeperDecisionEvaluationAnalyzer`, `PlayOutcomeAnalyzer` — sendo que `PlaySituationAnalyzer`/`GoalkeeperDecisionAnalyzer` contribuem só como sinais adicionais de `confidence` (mesmo princípio da W20/W23); as 8 `Rule`s de conteúdo usam EXCLUSIVAMENTE `GoalkeeperDecisionEvaluationResult.evaluation`/`PlayOutcomeResult.outcome`, por instrução explícita desta sprint. `performance` (`GoalkeeperPerformanceEvaluation`, sempre um valor, nunca `None`) vem de duas regras de gate + uma matriz determinística 3x3 (ver tabela abaixo). `summary` é uma string ESTRUTURADA (não linguagem natural, por instrução explícita) - concatena `performance`/`decision_evaluation`/`play_outcome`/regras contribuintes.
- **`goalkeeper_coaching.py`** — `GoalkeeperCoachingAnalyzer` (W26, NOVO): décima quarta implementação, **primeiro Analyzer de COACHING**, sem `AnalyzerContext` próprio (quarto combinador puro, depois de `PlaySituationAnalyzer`/W21, `GoalkeeperDecisionEvaluationAnalyzer`/W23 e `GoalkeeperPerformanceEvaluationAnalyzer`/W25). Compõe QUATRO Analyzers internamente — `GoalkeeperPerformanceEvaluationAnalyzer`, `GoalkeeperDecisionEvaluationAnalyzer`, `PlayOutcomeAnalyzer`, `GoalkeeperDecisionAnalyzer` — todos usados como fonte de conteúdo (diferente da W25, onde dois dos quatro compostos só contribuíam para `confidence`). As 8 `Rule`s de conteúdo desta sprint leem `performance` (W25), `decision` (W22) e `outcome` (W24) diretamente, e TRÊS delas (`committed_without_shot`/`reacted_passively_to_shot`/`dived_wrong_direction`) leem `GoalkeeperDecisionEvaluationResult.rules_passed`/`rules_failed` (Explainability da W23) para identificar QUAL desvio específico de compatibilidade motivou a orientação, sem reimplementar nenhuma lógica de `GoalkeeperDecisionEvaluationAnalyzer`. `coaching` (`GoalkeeperCoaching`, sempre um valor, nunca `None`) vem de duas regras de gate (espelhando os gates da W25) + um caso de REFORÇO (`performance` `EXCELLENT`/`GOOD`) + cinco regras específicas em ordem de prioridade fixa + um fallback residual (`IMPROVE_POSITIONING`). `summary` é uma string ESTRUTURADA (mesmo princípio da W25) - concatena `coaching`/`performance`/`decision`/`outcome`/regras contribuintes.
- **`play_outcome.py`** — ver revisão da W24 (Seção 15).
- **`goalkeeper_analysis_report.py`** — `GoalkeeperAnalysisReportAnalyzer` (W27, NOVO): décima quinta implementação, **encerra oficialmente o MVP arquitetural**, sem `AnalyzerContext` próprio (quinto combinador puro, depois de `PlaySituationAnalyzer`/W21, `GoalkeeperDecisionEvaluationAnalyzer`/W23, `GoalkeeperPerformanceEvaluationAnalyzer`/W25 e `GoalkeeperCoachingAnalyzer`/W26). Compõe SEIS Analyzers internamente — `PlaySituationAnalyzer`, `GoalkeeperDecisionAnalyzer`, `GoalkeeperDecisionEvaluationAnalyzer`, `PlayOutcomeAnalyzer`, `GoalkeeperPerformanceEvaluationAnalyzer`, `GoalkeeperCoachingAnalyzer` — todos ecoados INTEGRALMENTE em `GoalkeeperAnalysisReport`, nenhum recalculado, nenhuma regra nova executada. `confidence_summary` consolida (nunca recalcula) as seis `confidence`s + `overall` (`min()` das seis, só quando todas disponíveis). `artifacts` é um espelho de conveniência dos mesmos seis sub-resultados, indexado por nome de Analyzer (mesma convenção de `analysis_results` desde a W13). `analysis_version`/`worker_version`/`generated_at` são metadados do RELATÓRIO em si (distintos de `AnalyzerMetadata`) — `analysis_version` é a versão do ESQUEMA do relatório (`"1.0.0"`, mesmo princípio de `WORKER_PROTOCOL_VERSION` vs. `worker.__version__`, W2), `worker_version` ecoa `worker.__version__`, `generated_at` é um timestamp ISO 8601 real (UTC) — o único valor desta sprint que não é determinístico frame-a-frame, mas reflete um fato real, não uma inferência fabricada.
- **`field_areas.py`** (W16) — `estimate_goal_and_penalty_areas()`.
- **`context.py`** — `AnalyzerContext`: usada pela primeira vez na W18 (`BallMotionContext`), pela segunda na W19 (`ShotAnalyzerContext`), pela terceira na W20 (`BallTrajectoryContext`), pela quarta na W22 (`GoalkeeperDecisionContext`) e pela quinta na W24 (`PlayOutcomeContext`). **Não ganhou uma subclasse na W21, W23, W25, W26 nem W27** (`PlaySituationAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`GoalkeeperPerformanceEvaluationAnalyzer`/`GoalkeeperCoachingAnalyzer`/`GoalkeeperAnalysisReportAnalyzer` são combinadores puros).
- **`types.py`** — `AnalyzerName`/`AnalyzerVersion`, `GoalZone` (W14), `PlaySituation(str, Enum)` (W21), `GoalkeeperDecision(str, Enum)` (W22), `GoalkeeperDecisionEvaluation(str, Enum)` (W23), `PlayOutcome(str, Enum)` (W24), `GoalkeeperPerformanceEvaluation(str, Enum)` (W25 — `UNKNOWN`/`INSUFFICIENT_INFORMATION`/`EXCELLENT`/`GOOD`/`ADEQUATE`/`POOR`/`CRITICAL`), `GoalkeeperCoaching(str, Enum)` (W26 — `UNKNOWN`/`INSUFFICIENT_INFORMATION`/`NO_FEEDBACK`/`KEEP_POSITION`/`IMPROVE_POSITIONING`/`MOVE_EARLIER`/`MOVE_LATER`/`ATTACK_BALL`/`STAY_PATIENT`/`RECOVER_FASTER`). **Sem alteração na W27** — primeira sprint cognitiva (W21-W27) sem nenhum `Enum` novo.
- **`exceptions.py`** — `AnalyzerError` → `AnalyzerInitializationError`/`AnalyzerExecutionError`.
- **`registry.py`**/**`factory.py`** — **Sétima família de Registry paralela.** W14-W27 registraram cada novo Analyzer **sem nenhuma mudança** em `registry.py` além da nova linha — confirmação prática do padrão, agora repetida catorze vezes.
- **`processor.py`** — `AnalyzerProcessor(FrameProcessor)`: **confirmado pela décima quarta vez (W14-W27): zero mudança nesta classe** — inclusive para um Analyzer que compõe SEIS outros sem executar nenhuma regra própria, apenas ecoando resultados já produzidos.

### Contrato Oficial de Saída do Worker (Seção 6.5, W27)

A partir da W27, **`GoalkeeperAnalysisReport` é o CONTRATO OFICIAL de saída do Worker.** Todo consumidor externo (o Backend FastAPI, e qualquer integração futura) deve depender de `analysis_results["goalkeeper_analysis_report"]`/`"goalkeeper_analysis_report"` (alias de conveniência no artefato) — **nunca** de resultados individuais (`analysis_results["play_situation"]`, `analysis_results["goalkeeper_coaching"]`, etc.) diretamente, mesmo que estes continuem presentes e válidos no artefato bruto por retrocompatibilidade (nenhum deles foi removido ou descontinuado).

Esta decisão não introduz nenhuma restrição TÉCNICA nova — `WORKER_ANALYZERS` continua uma lista, todos os quinze Analyzers continuam podendo ser habilitados/desabilitados independentemente, `GoalkeeperAnalysisReportAnalyzer` continua sendo só mais um Analyzer registrado como qualquer outro. É uma decisão de **CONTRATO/GOVERNANÇA**: qual chave do artefato os consumidores externos devem tratar como estável e suportada a longo prazo. `analysis_version` (`"1.0.0"`) versiona esse contrato especificamente — mudanças na FORMA de `GoalkeeperAnalysisReport` devem incrementar `analysis_version`, independentemente de `worker_version` (a versão do software) ou de `AnalyzerVersion` (a versão de um Analyzer individual).

**Nada foi removido ou obscurecido para chegar a este contrato** — `GoalkeeperAnalysisReport` preserva INTEGRALMENTE cada um dos seis sub-resultados (via `to_dict()` de cada um, nunca reconstruído), incluindo toda a Explainability já produzida (`rules_evaluated`/`rules_passed`/`rules_failed`/`explanations`/`summary`/`supporting_evidence`) - ver princípio de "nunca remover informação produzida por camadas anteriores", também aplicado desde a W23.

### Rule Evaluation e Explainability (Seção 6.5, W23/W25/W26)

A W23 inaugurou a camada de **Explainability** do Worker: nenhuma conclusão de `GoalkeeperDecisionEvaluationAnalyzer`/`GoalkeeperPerformanceEvaluationAnalyzer`/`GoalkeeperCoachingAnalyzer` (nem de nenhum Analyzer de avaliação/coaching futuro que reutilize `worker.analyzers.rules`) pode ser um estado isolado sem justificativa. **A W25 confirmou que o mecanismo é genuinamente reutilizável, e a W26 confirma pela SEGUNDA vez** - nenhuma segunda implementação de `Rule`/`RuleOutcome`/`evaluate_rules` foi criada; a W26 só definiu 8 `Rule`s NOVAS, específicas de coaching, sobre um contexto (`_CoachingContext`) diferente dos da W23/W25 - e, pela primeira vez, algumas dessas regras leem a Explainability de OUTRO Analyzer (`rules_passed`/`rules_failed` de `GoalkeeperDecisionEvaluationResult`, W23) como parte da própria entrada, em vez de só um valor agregado (`evaluation`). O mecanismo:

```python
@dataclass(frozen=True)
class Rule(Generic[T]):
    id: str
    description: str
    condition: Callable[[T], bool | None]

@dataclass(frozen=True)
class RuleOutcome:
    rule_id: str
    description: str
    passed: bool | None       # True=satisfeita, False=violada, None=nao aplicavel
    explanation: str
```

`evaluate_rules(rules, context)` aplica cada `Rule.condition` (uma função PURA, sem efeito colateral) ao mesmo `context`, na MESMA ordem, devolvendo exatamente um `RuleOutcome` por `Rule` — nunca descarta, reordena ou resume uma regra silenciosamente. `GoalkeeperDecisionEvaluationResult` expõe essa auditoria completa: `rules_evaluated` (todos os `id`s, mesmo os não aplicáveis), `rules_passed`/`rules_failed` (só os `id`s com `True`/`False`), `explanations` (uma frase legível por regra, sempre presente).

As 6 regras desta sprint (`GoalkeeperDecisionEvaluationAnalyzer`), todas puras funções de `_EvaluationContext`:

| `Rule.id` | Aplicável quando | Verifica |
|---|---|---|
| `actors_visible` | sempre | bola E goleiro visíveis (senão, nada pode ser avaliado) |
| `decision_established` | atores visíveis | `GoalkeeperDecision != UNKNOWN` (histórico suficiente, W22) |
| `shot_prompts_active_response` | `situation == SHOT_DETECTED` | decisão é uma reação ativa (`PREPARE_DIVE`/`DIVE_LEFT`/`DIVE_RIGHT`) |
| `no_dive_without_shot` | decisão é um mergulho | `situation == SHOT_DETECTED` (checagem cruzada de consistência com o W22) |
| `recover_follows_shot_ending` | decisão é `RECOVER_POSITION` | `situation != SHOT_DETECTED` (checagem cruzada de consistência com o W22) |
| `dive_direction_matches_ball_direction` | decisão é um mergulho E ambos os sinais laterais (goleiro/bola) são fortes o bastante (`WORKER_GOALKEEPER_EVALUATION_MIN_LATERAL_SIGNAL`) | o lado do mergulho coincide com o lado da direção lateral observada da bola |

**Achado arquitetural importante desta sprint:** `shot_prompts_active_response`, `no_dive_without_shot` e `recover_follows_shot_ending` são, na prática, GARANTIDAS verdadeiras sempre que aplicáveis — porque `GoalkeeperDecisionAnalyzer` (W22) já só produz `DIVE_LEFT`/`DIVE_RIGHT`/`PREPARE_DIVE` quando `shot_detected=True`, e só produz `RECOVER_POSITION` quando `shot_detected=False`. Essas três regras são intencionalmente AUDITORIAS DE CONSISTÊNCIA CRUZADA entre Analyzers independentes (útil para capturar uma futura regressão em qualquer um dos dois lados), não checagens que hoje podem falhar sozinhas. Na prática, com o conjunto de regras atual, um resultado real (via composição completa) só alcança `INCOMPATIBLE` puro através de `dive_direction_matches_ball_direction` falhando SOZINHA sem nenhuma das três acima aplicável simultaneamente — o que não ocorre com os dados hoje disponíveis (um mergulho sempre implica as três acima aplicáveis e verdadeiras). Um mergulho com direção incompatível hoje produz `PARTIALLY_COMPATIBLE` (a maioria das regras aplicáveis concorda, uma diverge) - `INCOMPATIBLE` puro é alcançável e testado diretamente na função de agregação (`_classify`), mas se tornará naturalmente alcançável via composição real assim que uma regra independente o suficiente for adicionada numa sprint futura. Documentado honestamente, não escondido.

**Agregação num veredito único (`GoalkeeperDecisionEvaluationAnalyzer._classify`)** - determinística, sem heurística opaca:

| Prioridade | `GoalkeeperDecisionEvaluation` | Condição |
|---|---|---|
| 1 (mais alta) | `INSUFFICIENT_INFORMATION` | `actors_visible` falhou |
| 2 | `UNKNOWN` | `decision_established` falhou (atores visíveis, mas decisão ainda não estabelecida) |
| 3 | `COMPATIBLE` | nenhuma regra de conteúdo aplicável, OU todas as aplicáveis satisfeitas |
| 4 | `INCOMPATIBLE` | todas as regras de conteúdo aplicáveis foram violadas |
| 5 (mais baixa) | `PARTIALLY_COMPATIBLE` | mistura de regras de conteúdo aplicáveis satisfeitas e violadas |

**As 8 regras da W25 (`GoalkeeperPerformanceEvaluationAnalyzer`), todas puras funções de `_PerformanceContext` (`decision_evaluation`/`play_outcome`, exclusivamente):**

| `Rule.id` | Aplicável quando | Verifica |
|---|---|---|
| `actors_and_geometry_available` | sempre | nem `decision_evaluation` nem `play_outcome` são `INSUFFICIENT_INFORMATION` |
| `decisive_event_established` | gate anterior passou | `decision_evaluation != UNKNOWN` E `play_outcome` não é `UNKNOWN`/`LOST_TRACK`/`NO_SHOT_DETECTED` |
| `decision_fully_compatible` | ambos os gates passaram | `decision_evaluation == COMPATIBLE` |
| `decision_partially_compatible` | ambos os gates passaram | `decision_evaluation == PARTIALLY_COMPATIBLE` |
| `decision_incompatible` | ambos os gates passaram | `decision_evaluation == INCOMPATIBLE` |
| `outcome_is_save` | ambos os gates passaram | `play_outcome == SAVE` |
| `outcome_is_neutral` | ambos os gates passaram | `play_outcome` em `{POST, BLOCKED, BALL_OUT, CROSSBAR}` |
| `outcome_is_goal` | ambos os gates passaram | `play_outcome == GOAL` |

**Agregação num veredito único (`GoalkeeperPerformanceEvaluationAnalyzer._classify`)** - duas regras de gate + uma matriz determinística 3×3 (eixo decisão × eixo resultado), sem heurística opaca:

| | `outcome_is_save` | `outcome_is_neutral` | `outcome_is_goal` |
|---|---|---|---|
| `decision_fully_compatible` | `EXCELLENT` | `GOOD` | `ADEQUATE` |
| `decision_partially_compatible` | `GOOD` | `ADEQUATE` | `POOR` |
| `decision_incompatible` | `ADEQUATE` | `POOR` | `CRITICAL` |

Antes da matriz: `actors_and_geometry_available=False` → `INSUFFICIENT_INFORMATION`; `decisive_event_established=False` (gate anterior passou) → `UNKNOWN`. A diagonal principal (decisão compatível + defesa → `EXCELLENT`; decisão incompatível + gol → `CRITICAL`) reflete o princípio central desta sprint: a decisão (processo) pesa tanto quanto o resultado (que pode ser sorte/azar) — nunca um julgamento baseado só no resultado isolado, nunca só na decisão isolada. Por exemplo, uma decisão totalmente compatível que termina em gol é `ADEQUATE` (processo correto, resultado desfavorável), não `POOR`/`CRITICAL`.

**As 8 regras da W26 (`GoalkeeperCoachingAnalyzer`), todas puras funções de `_CoachingContext` (`performance`/`decision_evaluation`/`decision`/`outcome`/`decision_evaluation_rules_passed`/`decision_evaluation_rules_failed`):**

| `Rule.id` | Aplicável quando | Verifica |
|---|---|---|
| `evaluation_available` | sempre | `performance != INSUFFICIENT_INFORMATION` (espelha o 1º gate da W25) |
| `decisive_performance_established` | gate anterior passou | `performance != UNKNOWN` (espelha o 2º gate da W25) |
| `performance_was_reinforcement` | há uma orientação a extrair | `performance` em `{EXCELLENT, GOOD}` |
| `conceded_goal_without_active_response` | desempenho não é de reforço | `outcome == GOAL` E `decision` não é uma reação ativa (`PREPARE_DIVE`/`DIVE_LEFT`/`DIVE_RIGHT`) |
| `committed_without_shot` | desempenho não é de reforço, E `no_dive_without_shot` (W23) foi aplicável | `no_dive_without_shot` está em `rules_failed` (mergulho sem chute detectado) |
| `reacted_passively_to_shot` | desempenho não é de reforço, E `shot_prompts_active_response` (W23) foi aplicável | `shot_prompts_active_response` está em `rules_failed` (chute sem reação ativa) |
| `dived_wrong_direction` | desempenho não é de reforço, E `dive_direction_matches_ball_direction` (W23) foi aplicável | `dive_direction_matches_ball_direction` está em `rules_failed` (mergulho no lado errado) |
| `recovery_was_insufficient` | desempenho não é de reforço, E `decision == RECOVER_POSITION` | sempre `True` quando aplicável (reposicionamento ocorreu, mas o desempenho final ainda não foi de reforço) |

**Agregação num veredito único (`GoalkeeperCoachingAnalyzer._classify`)** - dois gates + um caso de reforço + cinco regras específicas em ordem de PRIORIDADE fixa, sem heurística opaca:

| Prioridade | `GoalkeeperCoaching` | Condição |
|---|---|---|
| 1 (mais alta) | `INSUFFICIENT_INFORMATION` | `evaluation_available` falhou |
| 2 | `UNKNOWN` | `decisive_performance_established` falhou (gate anterior passou) |
| 3 | `NO_FEEDBACK` / `KEEP_POSITION` | `performance_was_reinforcement` satisfeita - `NO_FEEDBACK` se `performance == EXCELLENT`, `KEEP_POSITION` se `performance == GOOD` |
| 4 | `ATTACK_BALL` | `conceded_goal_without_active_response` satisfeita |
| 5 | `MOVE_LATER` | `committed_without_shot` satisfeita |
| 6 | `MOVE_EARLIER` | `reacted_passively_to_shot` satisfeita |
| 7 | `STAY_PATIENT` | `dived_wrong_direction` satisfeita |
| 8 | `RECOVER_FASTER` | `recovery_was_insufficient` satisfeita |
| 9 (mais baixa, fallback) | `IMPROVE_POSITIONING` | nenhuma regra específica satisfeita |

**Achado estrutural da W26 (não um bug, documentado honestamente):** `conceded_goal_without_active_response` (`ATTACK_BALL`) é estruturalmente GARANTIDA a nunca ser satisfeita via composição real completa - `PlayOutcome.GOAL` (W24) só é produzido quando `shot_detected=True` no mesmo frame, e `GoalkeeperDecisionAnalyzer` (W22) SEMPRE classifica uma reação ativa (`PREPARE_DIVE`/`DIVE_LEFT`/`DIVE_RIGHT`) quando `shot_detected=True`, por construção da sua própria árvore de decisão. Portanto `decision` nunca fica fora de `_ACTIVE_SHOT_RESPONSES` no mesmo frame em que `outcome == GOAL`, tornando `ATTACK_BALL` inalcançável por composição real - mesmo padrão do Risco 38 (W23, regras garantidas por construção de uma camada inferior). Testado diretamente na agregação pura (`test_classify_attack_ball_when_goal_conceded_without_active_response`), não via composição completa - honestamente documentado no código e nos testes, não escondido.

**Parâmetro parametrizável desta sprint:**

| Variável | Default | Papel |
|---|---|---|
| `WORKER_GOALKEEPER_EVALUATION_MIN_LATERAL_SIGNAL` | `2.0` (pixels/frame) | Magnitude mínima da componente lateral (goleiro OU bola) para a regra `dive_direction_matches_ball_direction` julgar uma direção - abaixo disso, `None` (não aplicável), nunca um veredito sobre ruído |

**Critérios de detecção de chute (Seção 6.5, W19) — 100% determinísticos, parametrizáveis via `WorkerSettings`, nenhuma IA/modelo treinado/heurística opaca:**

| Variável | Default | Papel |
|---|---|---|
| `WORKER_SHOT_MIN_SPEED` | `20.0` (pixels/frame) | Velocidade mínima observada — placeholder não calibrado a nenhum vídeo real (mesma limitação de escala do Risco 20/22/27) |
| `WORKER_SHOT_MAX_ANGLE_DEVIATION_DEGREES` | `25.0` | Desvio angular máximo entre a direção observada e a direção até o centro do gol para considerar "em direção ao gol" |
| `WORKER_SHOT_MIN_CONSECUTIVE_FRAMES` | `2` | Quantos frames consecutivos os dois critérios acima precisam se manter — exige "movimento contínuo", não um pico isolado |

**Parâmetro de modelagem de trajetória (Seção 6.5, W20) — mesma disciplina de determinismo/parametrização, mas não é um "critério de decisão" (o Analyzer não decide nada, só descreve):**

| Variável | Default | Papel |
|---|---|---|
| `WORKER_TRAJECTORY_DIRECTION_CHANGE_THRESHOLD_DEGREES` | `30.0` | Desvio angular mínimo entre dois segmentos consecutivos da trajetória para contar como uma "mudança de direção" (`direction_changes`) — abaixo disso, é ruído normal de detecção, não uma mudança real |

**Classificação de situação de jogada (Seção 6.5, W21) — nenhum parâmetro numérico novo, apenas uma árvore de decisão fixa sobre campos já computados; nenhuma IA/modelo treinado:**

| Prioridade | `PlaySituation` | Condição |
|---|---|---|
| 1 (mais alta) | `NO_BALL_VISIBLE` | `not ball_detected` |
| 2 | `NO_GOALKEEPER_VISIBLE` | `ball_detected and not goalkeeper_detected` |
| 3 | `SHOT_DETECTED` | `shot_detected is True` |
| 4 | `BALL_MOVING` | `motion_detected is True` (e `shot_detected` é `False`) |
| 5 | `BALL_STATIONARY` | `motion_detected is False` |
| 6 (mais baixa) | `UNKNOWN` | `motion_detected is None` (primeira observação, histórico insuficiente) |

**Classificação de decisão do goleiro (Seção 6.5, W22) — dois parâmetros novos, parametrizáveis via `WorkerSettings`, mais uma árvore de decisão fixa; nenhuma IA/modelo treinado, nenhuma estimativa de pose:**

| Variável | Default | Papel |
|---|---|---|
| `WORKER_GOALKEEPER_SHIFT_MIN_SPEED` | `3.0` (pixels/frame) | Velocidade mínima do PRÓPRIO goleiro para considerar que houve movimento intencional — abaixo disso, `STAY_ON_LINE` |
| `WORKER_GOALKEEPER_DIVE_MIN_SPEED` | `15.0` (pixels/frame) | Velocidade mínima do goleiro, durante um chute detectado, para que o movimento lateral conte como mergulho (`DIVE_LEFT`/`DIVE_RIGHT`) em vez de `PREPARE_DIVE` |

| Prioridade | `GoalkeeperDecision` | Condição |
|---|---|---|
| 1 (mais alta) | `UNKNOWN` | `not goalkeeper_detected`, ou primeira observação/descontinuidade de `track_id` |
| 2 | `DIVE_LEFT`/`DIVE_RIGHT` | `shot_detected` e `movement_speed >= WORKER_GOALKEEPER_DIVE_MIN_SPEED` e o eixo lateral domina o deslocamento |
| 3 | `PREPARE_DIVE` | `shot_detected` mas sem atingir o limiar de mergulho acima (**proxy determinístico baseado em velocidade — não há estimativa de pose/postura real neste Worker**) |
| 4 | `RECOVER_POSITION` | decisão anterior era `DIVE_LEFT`/`DIVE_RIGHT`/`PREPARE_DIVE`, `shot_detected` já é `False`, e `movement_speed >= WORKER_GOALKEEPER_SHIFT_MIN_SPEED` |
| 5 | `STAY_ON_LINE` | `movement_speed < WORKER_GOALKEEPER_SHIFT_MIN_SPEED` |
| 6 | `SHIFT_LEFT`/`SHIFT_RIGHT` | eixo lateral domina o deslocamento (empate lateral/profundidade favorece esta categoria — desempate determinístico documentado) |
| 7 (mais baixa) | `STEP_FORWARD`/`STEP_BACK` | eixo de profundidade domina; direção decidida pela variação da MAGNITUDE de `depth_offset` (não o sinal cru), agnóstica a qual dos dois gols o goleiro defende |

**Resultado observado da jogada (Seção 6.5, W24) — dois parâmetros novos, mais uma árvore de decisão fixa; nenhuma IA/modelo treinado:**

| Prioridade | `PlayOutcome` | Condição |
|---|---|---|
| 1 (mais alta) | `INSUFFICIENT_INFORMATION` | `not goal_visible` (sem geometria do gol, nenhuma classificação geométrica é possível) |
| 2 | `LOST_TRACK` | `not ball_detected` e a bola estava sendo rastreada no frame anterior |
| 3 | `INSUFFICIENT_INFORMATION` | `not ball_detected` e a bola nunca foi rastreada |
| 4 | `UNKNOWN` | bola detectada, mas sem histórico suficiente (`not trajectory_detected`, primeira observação) |
| 5 | `NO_SHOT_DETECTED` | `not shot_detected` (a jogada ainda não chegou a um chute) |
| 6 (mais baixa) | `GOAL` > `POST` > `SAVE` > `BLOCKED` > `BALL_OUT` > `UNKNOWN` (fallback) | chute detectado - classificação geométrica, nesta ordem: bola dentro de alguma zona do gol (`GOAL`) > bola a `WORKER_OUTCOME_POST_PROXIMITY_PX` de um poste (`POST`) > goleiro a `WORKER_OUTCOME_SAVE_PROXIMITY_PX` da bola (`SAVE`) > trajetória com mudança de direção (`BLOCKED`) > bola fora da região do campo (`BALL_OUT`) > nenhuma condição atingida ainda (`UNKNOWN`) |

| Variável | Default | Papel |
|---|---|---|
| `WORKER_OUTCOME_POST_PROXIMITY_PX` | `15.0` (pixels) | Distância máxima entre a última posição conhecida da bola e um poste para classificar `POST` |
| `WORKER_OUTCOME_SAVE_PROXIMITY_PX` | `30.0` (pixels) | Distância máxima entre a última posição conhecida da bola e a do goleiro, após um chute detectado, para classificar `SAVE` |

**Explicabilidade desta sprint (`supporting_evidence`) — deliberadamente mais simples que a Rule Evaluation da W23:** por instrução explícita, `PlayOutcomeAnalyzer` NÃO reutiliza `worker.analyzers.rules` — `supporting_evidence` é uma lista de frases simples, construída incrementalmente conforme a árvore de decisão é percorrida (ex.: `"chute detectado"`, `"posicao da bola dentro da zona do gol 'top_right'"`), suficiente para justificar `outcome` sem o aparato completo de `Rule`/`RuleOutcome`.

**Achado desta sprint: `GoalGeometryResult.left_post`/`right_post` (W14) não correspondem à real separação lateral dos postes (Risco 39, NOVO).** Durante a implementação do critério `POST`, descobriu-se que `left_post = Coordinate(x=region.x, y=region.y)` e `right_post = Coordinate(x=region.x+region.width, y=region.y)` (W14) têm o MESMO `y` — diferem só ao longo do eixo de PROFUNDIDADE (`region.width`, tipicamente ~20px), não ao longo do eixo LATERAL estabelecido pela W15 (`GoalkeeperPositionAnalyzer._covers_thirds`, que divide `region.height` para determinar cobertura esquerda/centro/direita). Isso significa que `left_post`/`right_post`, como computados desde a W14, **nunca foram genuinamente consumidos por nenhum Analyzer até esta sprint** — a inconsistência estava dormente. `PlayOutcomeAnalyzer` usa esses campos EXATAMENTE como produzidos (por instrução desta sprint: "nunca recalcular"), então `POST` é geometricamente coerente apenas com o que `GoalGeometryAnalyzer` já expõe, não necessariamente com a intuição de "poste lateral" de um goleiro. **Consequência direta: `CROSSBAR` é DELIBERADAMENTE NUNCA produzido** — não há, hoje, nenhuma dimensão vertical/altura real modelada (o gol é um retângulo 2D, Risco 22) nem uma fonte geométrica independente para distinguir "travessão" de "trave". Corrigir isso (a inconsistência de eixo E a ausência de modelagem vertical) exigiria uma sprint dedicada de calibração de câmera/geometria 3D, fora do escopo desta sprint - `GoalGeometryAnalyzer` (W14) não foi alterado, conforme instrução explícita de não fazer mudanças estruturais.

**Estado entre Jobs — ainda CINCO Analyzers stateful (W18/W19/W20/W22/W24), nenhum sexto na W25, W26 nem W27:** `GoalkeeperAnalysisReportAnalyzer.reset()` delega aos seis Analyzers compostos (alguns genuinamente stateful, transitivamente; os demais combinadores puros, chamados por completude), mas não introduz nenhum `AnalyzerContext` próprio — quinta confirmação (depois da W21/W23/W25/W26) de que um Analyzer pode compor múltiplos Analyzers stateful sem precisar, ele mesmo, manter estado. Nenhuma mudança na plumbing genérica de `AnalyzerProcessor.reset()`.

**Padrão de composição entre Analyzers (confirmado na W14, validado em produção W15-W26, agora escalando para SEIS Analyzers simultâneos na W27):** `GoalkeeperAnalysisReportAnalyzer` prova que o padrão de composição (instanciar internamente, chamar `.analyze(football_world)` como função pura, combinar só resultados já produzidos) escala para SEIS Analyzers ao mesmo tempo, todos usados como fonte de conteúdo direta, sem nenhum atrito arquitetural e sem precisar de nenhuma regra própria — a diferença desta sprint é que a "combinação" é agregação PURA (ecoar cada sub-resultado integralmente), não uma nova classificação sobre eles.

**Artefato JSON:** `"analysis_results"`, `"analysis_statistics"`, `"analyzer_processing_time_ms"`, `"goal_geometry_result"` (W14), `"goalkeeper_position_result"` (W15), `"ball_position_result"` (W16), `"goalkeeper_ball_alignment_result"` (W17), `"ball_motion_result"` (W18), `"shot_analysis_result"` (W19), `"ball_trajectory_result"` (W20), `"play_situation_result"` (W21), `"goalkeeper_decision_result"` (W22), `"goalkeeper_decision_evaluation_result"` (W23), `"play_outcome_result"` (W24), `"goalkeeper_performance_evaluation_result"` (W25), `"goalkeeper_coaching_result"` (W26) e, desde a W27: `"goalkeeper_analysis_report"` (alias de `analysis_results["goalkeeper_analysis_report"]` — **o CONTRATO OFICIAL**, ver acima).

**Achado necessário da W13: `bbox` em `TrackedFootballEntity`.** Ver histórico completo na Seção 15. **Nenhuma sprint desde então (W14-W27) precisou de achado equivalente de contrato.**

**Achado da W19: descompasso de coordenadas entre geometria placeholder e posições reais (Risco 34).** A validação manual da W19 revelou concretamente (não só em teoria) que `Goal`/`Field` (geometria placeholder normalizada 0.0-1.0, Seção 6.4) e as posições reais de bola/goleiro (coordenadas de pixel cru, ex. 0-640) vivem em ESCALAS DIFERENTES sem nenhuma calibração entre elas — `towards_goal` pode divergir do esperado mesmo com movimento genuinamente horizontal em direção ao gol. **Nenhuma sprint desde a W20 corrigiu o Risco 34** — `PlayOutcomeAnalyzer` (W24) herda a mesma limitação para `GOAL`/`POST`: a geometria do gol e as posições reais da bola convivem na mesma escala de pixels nos testes (por construção), mas nunca foram calibradas uma à outra em produção real.

**Achado da W20: `trajectory_points` cresce sem limite (Risco 35).** Ver Seção 15/W20. Inalterado nesta sprint.

**Achado da W21: `sub_state` pode ser `SHOT_TOWARDS_GOAL` para uma bola tecnicamente parada, se não fosse pelo guard explícito (Risco 36).** Ver Seção 15/W21. Inalterado nesta sprint.

**Achado da W22: `PREPARE_DIVE` é um proxy determinístico, não uma detecção real de postura (Risco 37).** Ver Seção 15/W22. Inalterado nesta sprint.

**Achado da W23: `shot.direction_vector` é NORMALIZADO, não um deslocamento cru (achado de implementação, não um risco novo).** Durante a implementação, a regra `dive_direction_matches_ball_direction` inicialmente leu `shot.direction_vector.dy` diretamente como se fosse a componente lateral crua do movimento da bola - mas `ShotAnalysisResult.direction_vector` é ecoado de `BallMotionResult.direction_vector`, que é `velocity.normalized()` (W18, magnitude sempre 1). Corrigido multiplicando `direction_vector.dy * ball_speed` (ambos já ecoados por `ShotAnalyzer`) para reconstruir a componente lateral na mesma escala de pixels/frame de `GoalkeeperDecisionResult.movement_direction.dy` (que É crua, `Vector.between()`, W22) - combinação de dois campos já produzidos, não uma recalculação de geometria.

**Achado da W23: maioria das regras de conteúdo são auditorias garantidas por construção (Risco 38).** Ver Seção 15/W23. Inalterado nesta sprint.

**Achado da W24: `left_post`/`right_post` não correspondem à real separação lateral dos postes (Risco 39).** Ver descrição completa acima.

**Achado da W25: nenhum achado de contrato foi necessário.** A W25 é a segunda sprint consecutiva (depois da W24) a reutilizar integralmente a infraestrutura de Rule Evaluation sem nenhuma extensão - `Rule`/`RuleOutcome`/`evaluate_rules` (`rules.py`, W23) já eram genéricos o bastante para um contexto (`_PerformanceContext`) e um conjunto de regras totalmente diferentes dos da W23, sem nenhuma mudança no módulo. Riscos 34-39 permanecem todos inalterados - `GoalkeeperPerformanceEvaluationAnalyzer` não introduz nenhuma nova comparação geométrica, lê exclusivamente dois campos categóricos já produzidos (`GoalkeeperDecisionEvaluation`/`PlayOutcome`).

**Achado da W26: nenhum achado de contrato foi necessário; um achado ESTRUTURAL foi documentado (não um Risco novo).** A W26 é a terceira sprint consecutiva (W24 não conta, W25/W26 sim) a reutilizar integralmente a infraestrutura de Rule Evaluation sem nenhuma extensão do módulo `rules.py` em si. O achado desta sprint é de outra natureza: `conceded_goal_without_active_response` (a regra que produziria `ATTACK_BALL`) é estruturalmente GARANTIDA a nunca ser satisfeita via composição real completa, pelo mesmo motivo do Risco 38 (W23) - uma regra cujo resultado é garantido pela construção de uma camada inferior (`GoalkeeperDecisionAnalyzer`, W22, sempre produz uma reação ativa quando `shot_detected=True`, e `PlayOutcome.GOAL` só existe quando `shot_detected=True`). Documentado no código (`goalkeeper_coaching.py`) e testado diretamente na agregação pura, nunca escondido. Riscos 34-39 permanecem todos inalterados - `GoalkeeperCoachingAnalyzer` não introduz nenhuma nova comparação geométrica, lê exclusivamente campos categóricos e listas de `id`s de regras já produzidos (`GoalkeeperPerformanceEvaluation`/`GoalkeeperDecisionEvaluationResult.rules_passed`/`rules_failed`/`GoalkeeperDecision`/`PlayOutcome`).

**Achado da W27: nenhum achado de contrato foi necessário; nenhum achado estrutural novo.** A W27 não introduz nenhum cálculo, nenhuma regra, nenhum `Enum` novo — é a primeira sprint cognitiva cujo `analyze()` é uma agregação pura (seis chamadas + montagem de um `dataclass`), sem nenhum `if`/árvore de decisão de conteúdo. `WorkerVersion`/`AnalysisVersion` são os únicos conceitos genuinamente novos, e nenhum dos dois é uma inferência de visão computacional — são metadados de contrato/software. Riscos 34-39 permanecem todos inalterados e não se agravam (nenhuma geometria/cinemática nova é tocada).

**Boundary Enforcement (confirmado por grep, `SPRINT_W13_REPORT.md` a `SPRINT_W27_REPORT.md`):** todo `import` real de `worker/analyzers/**/*.py` é só stdlib + `worker` (`__version__`, W27) + `worker.config.settings` + `worker.core.exceptions` + `worker.domain.football_world`/`worker.domain.types`/`worker.domain.geometry.*` + `worker.inference.processors.base` + módulos internos de `worker.analyzers.*` (incluindo, na W19-W27, `ball_motion`/`ball_position`/`goal_geometry`/`shot`/`ball_trajectory`/`goalkeeper_ball_alignment`/`goalkeeper_position`/`play_situation`/`goalkeeper_decision`/`goalkeeper_decision_evaluation`/`play_outcome`/`goalkeeper_performance_evaluation`/`goalkeeper_coaching`/`rules` importados por `shot.py`/`ball_trajectory.py`/`play_situation.py`/`goalkeeper_decision.py`/`goalkeeper_decision_evaluation.py`/`play_outcome.py`/`goalkeeper_performance_evaluation.py`/`goalkeeper_coaching.py`/`goalkeeper_analysis_report.py` — composição DENTRO da própria família). Nenhum `import` de `worker.inference.detectors`/`trackers`/`events`/`world`, `cv2`, `ultralytics`, `redis`, `worker.infrastructure.*`. Toda menção a `WorldState`/`SceneAnalysisResult`/`TrackingResult`/`DetectionResult`/YOLO/ByteTrack/OpenCV/Tracker/Detector em `analyzers/` é só docstring.

---

## 7. Abstração de GPU (Compute Backend)

Interface "Compute Backend": seleção de dispositivo, alocação/liberação de memória e política de fallback resolvidas por um adaptador de compute, nunca espalhadas pelos Plugins de modelo. Hoje: adaptador ROCm (AMD). Futuro: adaptador CUDA. Depois: adaptador CPU (fallback com degradação aceita).

Frameworks de deep learning já abstraem boa parte da diferença ROCm/CUDA no nível de tensor, mas o Worker ainda precisa de uma camada própria para: seleção de dispositivo na inicialização, tamanho de lote adaptativo conforme memória disponível, tratamento de OOM com fallback, e relato de qual dispositivo está em uso (Observabilidade). Nenhum Plugin de modelo, tracker ou Stage do Pipeline verifica diretamente "estou em ROCm ou CUDA?".

**Estado real (Sprint W8/W9):** `YOLODetector` roda sobre `torch` CPU-only (nenhuma dependência de CUDA/ROCm instalada ou usada, por proibição explícita). `ByteTrackTracker` (W9) nem depende de `torch` diretamente — é um algoritmo puro em NumPy (filtro de Kalman + associação por IoU), sem custo de GPU algum. O dispositivo não é escolhido por nenhuma camada própria, é simplesmente o único disponível. Esta seção continua **aspiracional**: nem o primeiro Detector nem o primeiro Tracker reais forçam a existência do Compute Backend, mas o Detector é o primeiro Plugin que de fato se beneficiaria dele (seleção de dispositivo, fallback de OOM) quando GPU for autorizada em sprint futura.

---

## 8. Versionamento e Rastreabilidade Completa

Toda **Analysis** produzida carrega, como metadados de proveniência, as seguintes versões — sem isso não há como auditar retroativamente o que gerou um resultado:

- **Pipeline Version** — identifica a sequência de Stages e o conjunto de Plugins ativos no momento da execução (ADR-006). Versão única por execução do Pipeline inteiro.
- **Worker Version** — versão do software do Worker que processou o Job (distinta do `worker_instance_id`, que identifica só a instância física/processo).
- **Model Versions** — mapa família → versão de cada Plugin de modelo efetivamente usado (Model Registry), persistido por hash de conteúdo dos pesos, não apenas por tag semântica (ADR-007).
- **Schema Version** — versão do contrato de submissão (Seção 9) usado para montar o payload.

Essas quatro versões são obrigatoriamente incluídas no payload da Submissão de Resultados (Seção 9) e devem ser persistidas junto da Analysis correspondente no backend — a exigência de captura é definida aqui; o campo/coluna exato de persistência é uma decisão de schema do backend, fora do escopo deste repositório, mas dependente desta arquitetura.

**Semente real já implementada (W4):** `InferenceMetadata` (Seção 6.1) já captura `engine_name`/`engine_version`/`duration_ms` a cada execução — é o embrião do que se tornará Model Versions quando a Submissão de Resultados existir. Nenhuma mudança de schema foi feita; é só o dado já sendo produzido, ainda sem para onde ser enviado.

---

## 9. Contrato Backend ↔ Worker

Autenticação inalterada: API Key existente (`X-Worker-Api-Key`).

**Submissão de Resultados em duas fases idempotentes (ADR-002, ADR-008):**

- **Fase 1 — Criar/Obter Analysis:** o Worker solicita a criação de uma Analysis para `(video_id, job_id)`. Idempotente: se uma Analysis já existir para esse `job_id` (ex.: reenvio após falha de rede na Fase 2), o backend devolve a existente em vez de criar uma duplicata. A atribuição da versão da Analysis é responsabilidade exclusiva do backend (autoridade única de escrita), eliminando corrida de versionamento mesmo sob concorrência.
- **Fase 2 — Enviar Resultados:** o Worker envia, referenciando a Analysis da Fase 1: os Events, as Metrics, os metadados de Artifact (apontando para os `r2_key` já enviados na etapa de Upload — a Fase 2 nunca envia bytes, só metadados) e o Report. Idempotente por Analysis + chave natural de cada item — seguro reenviar a Fase 2 inteira se ela falhou parcialmente.
- O Job só é marcado `COMPLETED` (Stage "Conclusão", Seção 2) **depois** que a Fase 2 é confirmada com sucesso pelo backend.
- O payload da Fase 2 carrega `schema_version`, `pipeline_version`, `worker_version`, `model_versions` e o Correlation ID (`video_id`) — ver Seção 8.

**Dependência externa explícita:** as Fases 1 e 2 não existem hoje no backend. Este documento assume que serão implementadas como uma pequena sprint do repositório backend, coordenada antes da Sprint W8 do roadmap do Worker (Seção 13) — ver ADR-008.

---

## 10. Escalabilidade

- **Distribuição de carga:** todos os Workers se inscrevem no mesmo consumer group do stream `processing_jobs`; o Redis distribui as mensagens automaticamente.
- **Exclusão mútua por vídeo** (Seção 4) é complementar à distribuição por Job — o consumer group evita que a *mesma mensagem* seja entregue duas vezes; o Lock evita que *Jobs diferentes do mesmo vídeo* rodem ao mesmo tempo.
- **Recuperação de falhas:** mensagens não confirmadas ficam pendentes; qualquer Worker vivo pode reivindicá-las via `XCLAIM` após timeout.
- **Recurso físico limitado (GPU):** hoje uma única GPU. Escalar para 10-100 Workers pressupõe múltiplas máquinas com GPU — decisão de infraestrutura/operação, não de arquitetura de código.

---

## 11. Observabilidade

- **Logs estruturados** com `video_id` (Correlation ID) e `job_id` obrigatórios em toda linha relacionada a um Job (Seção 3).
- **Métricas internas:** duração por Stage, throughput, profundidade/lag da fila, taxa de falha, contagem de retries, utilização de GPU/memória — formato compatível com Prometheus é evolução futura, não exigência agora.
- **Heartbeat:** reportado periodicamente pelo Worker, e é o mesmo sinal que sustenta a renovação do Lock de vídeo (Seção 4, ADR-003).
- **Health check:** verificação local de conectividade com Redis, backend e GPU.
- **Tracing:** o Correlation ID já cobre a reconstrução de uma execução ponta a ponta via log; OpenTelemetry é evolução possível, não bloqueante.

---

## 12. Estrutura Completa do Repositório

Árvore real, conferida arquivo a arquivo nesta revisão (W27) contra o disco:

```
IA_GK/                                # monorepo oficial - um unico Git, dois produtos independentes (ADR-009)
├── backend_fastapi/                  # Produto 1: Plataforma SaaS - fora do escopo deste documento
├── frontend_flutter/                 # Produto 1: Flutter Web - fora do escopo deste documento
├── goalkeeper_ai_worker/             # Produto 2: este documento - servico de IA, totalmente independente
│   ├── worker/
│   │   ├── orchestrator/
│   │   │   └── orchestrator.py        # WorkerOrchestrator - process_job, run_forever (Secao 1/2)
│   │   ├── pipeline/
│   │   │   └── stages/                 # os 10 Stages ativos + base.py (interface comum)
│   │   │       ├── receive_job.py, validate_job.py, acquire_lock.py, prepare_workspace.py,
│   │   │       │   download_video.py, inference.py, upload_artifact.py, update_status.py,
│   │   │       │   cleanup.py, release_lock.py
│   │   ├── inference/                  # camada de IA plugavel (Secao 6.1) - NOVO na W4
│   │   │   ├── base.py                  # InferenceEngine (ABC) - o contrato
│   │   │   ├── types.py                  # Detection, FrameMetadata, RegionOfInterest, InferenceMetadata, InferenceResult
│   │   │   ├── exceptions.py              # InferenceError, EngineInitializationError, InferenceExecutionError
│   │   │   ├── frame_ops.py                # convert_bgr_to_rgb/resize_frame/apply_roi - funcoes puras, chamadas pelos Processors (W7)
│   │   │   ├── basic_vision_engine.py       # BasicVisionEngine v2.0.0 - orquestrador fino desde a W7 (delega a PipelineProcessor)
│   │   │   ├── processors/                   # NOVO na W7 - toda transformacao de frame vive aqui, nenhuma no engine
│   │   │   │   ├── base.py                    # FrameProcessor (ABC), ProcessorContext, ProcessorStats
│   │   │   │   ├── color_processor.py          # ColorProcessor - BGR->RGB (delega a frame_ops.convert_bgr_to_rgb)
│   │   │   │   ├── resize_processor.py          # ResizeProcessor - delega a frame_ops.resize_frame
│   │   │   │   ├── roi_processor.py              # ROIProcessor - delega a frame_ops.apply_roi
│   │   │   │   ├── statistics_processor.py        # StatisticsProcessor - so observabilidade, nao transforma
│   │   │   │   ├── yolo_processor.py               # YOLOProcessor (W8) - so detecta, delega a Detector.detect()
│   │   │   │   ├── tracking_processor.py            # TrackingProcessor (W9) - so associa, delega a Tracker.track()
│   │   │   │   ├── scene_analysis_processor.py       # SceneAnalysisProcessor (W10) - so interpreta, delega a SceneAnalyzer.analyze()
│   │   │   │   ├── world_model_processor.py           # WorldModelProcessor (W11) - so mantem estado, delega a WorldModel.update()
│   │   │   │   ├── football_domain_processor.py        # FootballDomainProcessor (W12) - so delega a FootballWorldBuilder.build()
│   │   │   │   ├── registry.py                     # register_processor/get_processor_class/available_processors
│   │   │   │   │                                    # (dict ordenado - ordem de registro = ordem de execucao)
│   │   │   │   │                                    # importa AnalyzerProcessor de worker.analyzers.processor (W13) so para registrar
│   │   │   │   └── pipeline.py                       # PipelineProcessor - executa a sequencia, from_settings(), reset()
│   │   │   ├── detectors/                     # NOVO na W8 - API de Deteccao, YOLO e so a 1a implementacao
│   │   │   │   ├── base.py                     # Detector (ABC) - detect(frame) -> DetectionResult
│   │   │   │   ├── types.py                     # BoundingBox, ClassLabel, Confidence, Detection, DetectionResult
│   │   │   │   ├── exceptions.py                  # DetectorError, DetectorInitializationError, DetectorExecutionError
│   │   │   │   ├── registry.py                     # register_detector/get_detector_class/available_detectors ("yolo")
│   │   │   │   ├── factory.py                       # create_detector(nome, settings) - resolve por WORKER_DETECTOR
│   │   │   │   └── yolo_detector.py                  # YOLODetector - todo codigo Ultralytics exclusivamente aqui
│   │   │   ├── trackers/                      # NOVO na W9 - API de Tracking, ByteTrack e so a 1a implementacao
│   │   │   │   ├── base.py                     # Tracker (ABC) - track(DetectionResult) -> TrackingResult, reset()
│   │   │   │   ├── types.py                     # TrackId, ClassLabel, Confidence, BoundingBox, TrackState,
│   │   │   │   │                                 # TrackedObject, TrackingStatistics, TrackingResult
│   │   │   │   ├── exceptions.py                  # TrackerError, TrackerInitializationError, TrackerExecutionError
│   │   │   │   ├── registry.py                     # register_tracker/get_tracker_class/available_trackers ("bytetrack")
│   │   │   │   ├── factory.py                       # create_tracker(nome, settings) - resolve por WORKER_TRACKER
│   │   │   │   └── bytetrack_tracker.py               # ByteTrackTracker - reaproveita ultralytics.trackers.byte_tracker,
│   │   │   │                                           # todo codigo especifico de ByteTrack exclusivamente aqui
│   │   │   ├── events/                        # NOVO na W10 - API de Eventos de Cena (NAO e worker/events/, Secao 1)
│   │   │   │   ├── base.py                     # SceneAnalyzer (ABC) - analyze(TrackingResult) -> SceneAnalysisResult, reset()
│   │   │   │   ├── types.py                     # SceneEventType, MotionState, TrackLifecycle, SceneEvent,
│   │   │   │   │                                 # SceneObjectSnapshot (W11), SceneStatistics, SceneAnalysisResult
│   │   │   │   ├── context.py                     # SceneAnalysisContext/TrackObservation - memoria interna do analisador
│   │   │   │   ├── exceptions.py                   # SceneAnalysisError, SceneAnalysisInitializationError/ExecutionError
│   │   │   │   ├── registry.py                      # register_analyzer/get_analyzer_class/available_analyzers ("basic")
│   │   │   │   ├── factory.py                        # create_analyzer(nome, settings) - resolve por WORKER_SCENE_ANALYZER
│   │   │   │   └── scene_analyzer.py                   # BasicSceneAnalyzer - eventos genericos, zero regra de futebol;
│   │   │   │                                            # popula SceneAnalysisResult.objects (W11) a partir do TrackingResult
│   │   │   ├── world/                         # NOVO na W11 - World Model, ultima camada generica da arquitetura
│   │   │   │   ├── base.py                     # WorldModel (ABC) - update(SceneAnalysisResult) -> WorldState, reset()
│   │   │   │   ├── types.py                     # ObjectId, ClassLabel, Confidence, BoundingBox, Position, Motion
│   │   │   │   ├── history.py                    # History[T] - buffer circular generico (deque maxlen), nunca cresce infinito
│   │   │   │   ├── trajectory.py                  # Trajectory - historico limitado de posicoes, composicao sobre History
│   │   │   │   ├── motion.py                       # compute_motion() - deslocamento/velocidade/direcao/aceleracao, so matematica
│   │   │   │   ├── object_state.py                  # ObjectState - estado completo de 1 objeto, zero logica de goleiro
│   │   │   │   ├── world_state.py                    # WorldStatistics, WorldState - fotografia completa da cena
│   │   │   │   ├── context.py                         # WorldModelContext - ObjectState mais recente por track_id
│   │   │   │   ├── exceptions.py                       # WorldModelError, WorldModelInitializationError/ExecutionError
│   │   │   │   ├── registry.py                          # register_world_model/get_world_model_class/available_world_models ("basic")
│   │   │   │   ├── factory.py                            # create_world_model(nome, settings) - resolve por WORKER_WORLD_MODEL
│   │   │   │   └── world_model.py                         # BasicWorldModel - nunca muta ObjectState, sempre reconstroi
│   │   │   ├── fake_engine.py                # FakeInferenceEngine - mantido so para testes desde a W6
│   │   │   ├── registry.py                    # register_engine/get_engine_class/available_engines ("fake","basic_vision")
│   │   │   └── engine.py                       # create_engine(nome, settings) - resolve por WORKER_INFERENCE_ENGINE
│   │   ├── domain/                     # NOVO na W12 - Football Domain Model, irmao de inference/, SEM Registry/factory
│   │   │   │                            # (implementacao unica e canonica, nao uma familia de plugins)
│   │   │   ├── football_world.py        # FootballWorld - goleiro(s), bola, jogadores, gols, campo, direcao do jogo
│   │   │   ├── world_builder.py          # FootballWorldBuilder.build(WorldState) -> FootballWorld, so transforma dados
│   │   │   ├── context.py                 # FootballDomainContext - memoria interna do builder (reset() entre Jobs)
│   │   │   ├── types.py                    # EntityId, ClassLabel, Confidence, TrackedFootballEntity (base das entidades;
│   │   │   │                                # ganhou bbox: Region na W13, para GoalkeeperPresenceAnalyzer)
│   │   │   ├── exceptions.py                # FootballDomainError, FootballWorldBuildError
│   │   │   ├── entities/                      # objetos ricos, nao dicts
│   │   │   │   ├── goalkeeper.py                # Goalkeeper(TrackedFootballEntity)
│   │   │   │   ├── player.py                     # Player(TrackedFootballEntity)
│   │   │   │   ├── ball.py                        # Ball(TrackedFootballEntity)
│   │   │   │   ├── goal.py                         # Goal + default_pair(field_region) - geometria placeholder
│   │   │   │   └── field.py                        # Field + default() - geometria placeholder, nao calibrada
│   │   │   └── geometry/                      # utilitarios reusaveis por todos os futuros analisadores
│   │   │       ├── coordinate.py                 # Coordinate, Distance, distance()
│   │   │       ├── vector.py                      # Vector (magnitude/angle_degrees/normalized/from_polar/between), Angle
│   │   │       ├── direction.py                    # Direction (LEFT_TO_RIGHT/RIGHT_TO_LEFT/UNKNOWN)
│   │   │       └── region.py                       # Region (center/contains/to_dict) - reaproveitado como bbox (W13)
│   │   ├── analyzers/                  # NOVO na W13 - Analyzer API, irmao de domain/ e inference/
│   │   │   ├── base.py                  # Analyzer (ABC) - analyze(FootballWorld) -> AnalysisResult, reset()
│   │   │   ├── results.py                # AnalyzerMetadata, AnalysisResult, GoalkeeperPresenceResult (W13),
│   │   │   │                              # GoalGeometryResult (W14), GoalkeeperPositionResult (W15),
│   │   │   │                              # BallPositionResult (W16), GoalkeeperBallAlignmentResult (W17),
│   │   │   │                              # BallMotionResult (W18), ShotAnalysisResult (W19),
│   │   │   │                              # BallTrajectoryResult (W20), PlaySituationResult (W21),
│   │   │   │                              # GoalkeeperDecisionResult (W22), GoalkeeperDecisionEvaluationResult (W23),
│   │   │   │                              # PlayOutcomeResult (W24), GoalkeeperPerformanceEvaluationResult (W25),
│   │   │   │                              # GoalkeeperCoachingResult (W26), GoalkeeperAnalysisReport (W27),
│   │   │   │                              # AnalysisStatistics
│   │   │   ├── goalkeeper_presence.py     # GoalkeeperPresenceAnalyzer (W13) - 1a implementacao, so fatos deterministicos
│   │   │   ├── goal_geometry.py            # GoalGeometryAnalyzer (W14) - 2a implementacao, so geometria do gol
│   │   │   ├── goalkeeper_position.py       # GoalkeeperPositionAnalyzer (W15) - 3a implementacao, compoe GoalGeometryAnalyzer
│   │   │   ├── ball_position.py             # BallPositionAnalyzer (W16) - 4a implementacao, compoe GoalGeometryAnalyzer
│   │   │   ├── goalkeeper_ball_alignment.py  # GoalkeeperBallAlignmentAnalyzer (W17) - 5a implementacao, 1a RELACIONAL,
│   │   │   │                                  # compoe os outros tres (GoalGeometry/GoalkeeperPosition/BallPosition)
│   │   │   ├── ball_motion.py                 # BallMotionAnalyzer (W18) - 6a implementacao, 1a STATEFUL, compoe
│   │   │   │                                   # BallPositionAnalyzer; define BallMotionContext(AnalyzerContext)
│   │   │   ├── shot.py                          # ShotAnalyzer (W19) - 7a implementacao, 1o Analyzer de EVENTOS,
│   │   │   │                                     # compoe BallMotionAnalyzer/BallPositionAnalyzer/GoalGeometryAnalyzer;
│   │   │   │                                     # define ShotAnalyzerContext(AnalyzerContext)
│   │   │   ├── ball_trajectory.py                # BallTrajectoryAnalyzer (W20) - 8a implementacao, 2o STATEFUL,
│   │   │   │                                       # compoe BallMotionAnalyzer/BallPositionAnalyzer/GoalGeometryAnalyzer
│   │   │   │                                       # (este ultimo so para confidence); define BallTrajectoryContext(AnalyzerContext)
│   │   │   ├── play_situation.py                 # PlaySituationAnalyzer (W21) - 9a implementacao, 1o COGNITIVO,
│   │   │   │                                       # compoe ShotAnalyzer/BallTrajectoryAnalyzer/GoalkeeperBallAlignmentAnalyzer/
│   │   │   │                                       # GoalGeometryAnalyzer; sem AnalyzerContext proprio (combinador puro)
│   │   │   ├── goalkeeper_decision.py            # GoalkeeperDecisionAnalyzer (W22) - 10a implementacao, 1o ESPECIFICO
│   │   │   │                                       # DO GOLEIRO, 4o STATEFUL; compoe PlaySituationAnalyzer/GoalkeeperPositionAnalyzer/
│   │   │   │                                       # GoalkeeperBallAlignmentAnalyzer/BallTrajectoryAnalyzer/ShotAnalyzer;
│   │   │   │                                       # define GoalkeeperDecisionContext(AnalyzerContext)
│   │   │   ├── goalkeeper_decision_evaluation.py # GoalkeeperDecisionEvaluationAnalyzer (W23) - 11a implementacao,
│   │   │   │                                       # 1o de AVALIACAO; compoe PlaySituationAnalyzer/GoalkeeperDecisionAnalyzer/
│   │   │   │                                       # GoalkeeperPositionAnalyzer/GoalkeeperBallAlignmentAnalyzer/BallTrajectoryAnalyzer/
│   │   │   │                                       # ShotAnalyzer; usa rules.py, sem AnalyzerContext proprio (combinador puro)
│   │   │   ├── rules.py                          # Rule/RuleOutcome/evaluate_rules (W23) - mecanismo generico de Rule
│   │   │   │                                       # Evaluation; NAO reutilizado por play_outcome.py (W24) nem por
│   │   │   │                                       # goalkeeper_analysis_report.py (W27, agregacao pura); REUTILIZADO
│   │   │   │                                       # integralmente por goalkeeper_performance_evaluation.py (W25) e
│   │   │   │                                       # por goalkeeper_coaching.py (W26)
│   │   │   ├── play_outcome.py                   # PlayOutcomeAnalyzer (W24) - 12a implementacao, encerra a cadeia
│   │   │   │                                       # de observacao, 5o STATEFUL; compoe PlaySituationAnalyzer/ShotAnalyzer/
│   │   │   │                                       # BallTrajectoryAnalyzer/GoalGeometryAnalyzer/GoalkeeperDecisionAnalyzer;
│   │   │   │                                       # define PlayOutcomeContext(AnalyzerContext); le football_world.field
│   │   │   │                                       # diretamente para BALL_OUT
│   │   │   ├── goalkeeper_performance_evaluation.py # GoalkeeperPerformanceEvaluationAnalyzer (W25) - 13a implementacao,
│   │   │   │                                       # encerra a cadeia de avaliacao; compoe PlaySituationAnalyzer/
│   │   │   │                                       # GoalkeeperDecisionAnalyzer/GoalkeeperDecisionEvaluationAnalyzer/
│   │   │   │                                       # PlayOutcomeAnalyzer; usa rules.py (8 novas Rules), sem
│   │   │   │                                       # AnalyzerContext proprio (combinador puro)
│   │   │   ├── goalkeeper_coaching.py             # GoalkeeperCoachingAnalyzer (W26) - 14a implementacao,
│   │   │   │                                       # 1o Analyzer de COACHING; compoe GoalkeeperPerformanceEvaluationAnalyzer/
│   │   │   │                                       # GoalkeeperDecisionEvaluationAnalyzer/PlayOutcomeAnalyzer/
│   │   │   │                                       # GoalkeeperDecisionAnalyzer; usa rules.py (8 novas Rules, 3 delas
│   │   │   │                                       # lendo rules_passed/rules_failed da W23 diretamente), sem
│   │   │   │                                       # AnalyzerContext proprio (combinador puro)
│   │   │   ├── goalkeeper_analysis_report.py      # GoalkeeperAnalysisReportAnalyzer (W27) - 15a implementacao,
│   │   │   │                                       # ENCERRA O MVP; compoe SEIS Analyzers: PlaySituationAnalyzer/
│   │   │   │                                       # GoalkeeperDecisionAnalyzer/GoalkeeperDecisionEvaluationAnalyzer/
│   │   │   │                                       # PlayOutcomeAnalyzer/GoalkeeperPerformanceEvaluationAnalyzer/
│   │   │   │                                       # GoalkeeperCoachingAnalyzer; agregacao PURA, zero Rule nova,
│   │   │   │                                       # sem AnalyzerContext proprio (combinador puro) - produz o
│   │   │   │                                       # CONTRATO OFICIAL de saida (GoalkeeperAnalysisReport)
│   │   │   ├── field_areas.py                    # estimate_goal_and_penalty_areas() (W16) - extraido de goalkeeper_position.py,
│   │   │   │                                      # reusado por ball_position.py
│   │   │   ├── context.py                         # AnalyzerContext - base vazia, reservada para Analyzers stateful;
│   │   │   │                                       # subclassada na W18 (BallMotionContext), W19 (ShotAnalyzerContext),
│   │   │   │                                       # W20 (BallTrajectoryContext), W22 (GoalkeeperDecisionContext) e
│   │   │   │                                       # W24 (PlayOutcomeContext) - NAO subclassada na W21, W23, W25, W26
│   │   │   │                                       # nem W27 (combinadores puros, sem estado proprio)
│   │   │   ├── types.py                            # AnalyzerName, AnalyzerVersion, GoalZone (W14), PlaySituation (W21),
│   │   │   │                                        # GoalkeeperDecision (W22), GoalkeeperDecisionEvaluation (W23),
│   │   │   │                                        # PlayOutcome (W24), GoalkeeperPerformanceEvaluation (W25),
│   │   │   │                                        # GoalkeeperCoaching (W26) - SEM mudanca na W27 (nenhum Enum novo)
│   │   │   ├── exceptions.py                        # AnalyzerError, AnalyzerInitializationError/ExecutionError
│   │   │   ├── registry.py                           # register_analyzer/get_analyzer_class/available_analyzers
│   │   │   │                                           # ("goalkeeper_presence", "goal_geometry", "goalkeeper_position",
│   │   │   │                                           #  "ball_position", "goalkeeper_ball_alignment", "ball_motion", "shot",
│   │   │   │                                           #  "ball_trajectory", "play_situation", "goalkeeper_decision",
│   │   │   │                                           #  "goalkeeper_decision_evaluation", "play_outcome",
│   │   │   │                                           #  "goalkeeper_performance_evaluation", "goalkeeper_coaching",
│   │   │   │                                           #  "goalkeeper_analysis_report")
│   │   │   ├── factory.py                              # create_analyzer(nome, settings) - resolve por nome, chamado 1x por WORKER_ANALYZERS
│   │   │   └── processor.py                          # AnalyzerProcessor(FrameProcessor) - registrado em inference/processors/registry.py
│   │   ├── video/                       # infraestrutura de leitura de video (Secao 6.3) - NOVO na W5, irmao de inference/
│   │   │   ├── reader.py                 # VideoReader - abre/fecha/valida (cv2.VideoCapture, so leitura)
│   │   │   ├── provider.py                # FrameProvider - leitura sequencial
│   │   │   ├── iterator.py                 # FrameIterator - protocolo padrao de iterador
│   │   │   ├── frame.py                     # Frame (imagem + FrameMetadata)
│   │   │   ├── metadata.py                   # FrameMetadata (video/)
│   │   │   ├── types.py                       # VideoProperties
│   │   │   └── exceptions.py                   # VideoError, VideoOpenError, InvalidVideoError, FrameReadError
│   │   ├── contracts/            # tipos dos contratos publicos do Backend (REST + mensagem do Redis Stream)
│   │   │   ├── backend_api.py     # JobDetails, JobStatusUpdate, PresignedUrl, ArtifactUploadUrlRequest/Response
│   │   │   └── queue_message.py    # JobMessage (job_id, video_id, message_id)
│   │   ├── infrastructure/        # clientes de infraestrutura externa, agrupados por sistema
│   │   │   ├── redis/              # cliente Redis, consumer group (consumer.py), Lock (lock.py, ADR-001/003)
│   │   │   ├── backend_client/      # Worker API: camada HTTP generica (_BaseBackendClient) + endpoints
│   │   │   └── storage/              # r2_client.py - download_to_path/upload_file REAIS via URL assinada
│   │   ├── gpu/                 # vazio - aspiracional (Secao 7)
│   │   ├── registry/            # vazio - o PADRAO ja existe em inference/registry.py (ver Secao 1)
│   │   ├── models/               # vazio - responsabilidade convergiu para inference/ na W4 (ver Secao 1)
│   │   │   ├── detection/, pose/, segmentation/, classification/   # pastas nunca criadas
│   │   ├── tracking/              # vazio - responsabilidade convergiu para inference/trackers/ (W9), igual a models/
│   │   ├── metrics/                # vazio - aspiracional (pos-W5)
│   │   ├── events/
│   │   │   └── events.py             # JobStarted/VideoDownloaded/UploadFinished/JobCompleted/JobFailed (Secao 6.2)
│   │   │                              # NAO e inference/events/ (API de Eventos de Cena, W10) - pastas distintas
│   │   ├── artifacts/                # vazio - aspiracional (pos-W5)
│   │   ├── report/                    # vazio - aspiracional
│   │   ├── submission/                 # vazio - aspiracional (ADR-008, depende do backend)
│   │   ├── state/
│   │   │   └── pipeline_state.py         # PipelineState (Secao 6.2)
│   │   ├── workspace/
│   │   │   └── manager.py                 # WorkspaceManager - tempfile.mkdtemp (Secao 6.2)
│   │   ├── observability/
│   │   │   └── logging_setup.py            # logging estruturado (pronto); metricas/heartbeat/health check (futuro)
│   │   ├── core/
│   │   │   ├── exceptions.py                # WorkerError e toda a hierarquia (Secao 1)
│   │   │   └── lifecycle.py                  # shutdown gracioso
│   │   ├── config/
│   │   │   └── settings.py                    # WorkerSettings - todas as variaveis WORKER_*
│   │   └── main.py                              # so inicializa e delega ao WorkerOrchestrator
│   ├── requirements.txt              # dependencias proprias (opencv-python-headless/numpy - W5; ultralytics - W8,
│   │                                  # traz torch/torchvision CPU-only, sem CUDA/ROCm) - NUNCA compartilhadas com backend_fastapi/
│   ├── pyproject.toml                # metadados do projeto + configuracao do pytest
│   ├── .env.example                  # variaveis documentadas, nenhum valor sensivel real
│   ├── .gitignore                    # .venv/, __pycache__/, /weights/, /workspace/, *.pt (proprio, ancorado, nao o do IA_GK)
│   ├── README.md                     # status da sprint atual, como rodar/testar
│   ├── weights/                       # NAO versionado no git - pesos de modelo; weights/yolo11n.pt (W8) e o
│   │                                   # default de WORKER_MODEL_PATH, baixado automaticamente pela Ultralytics
│   │                                   # no primeiro uso (auto-download por hash/nome, nunca commitado)
│   ├── workspace/                     # NAO versionado no git - diretorio de dados de topo, SEM USO REAL ainda
│   │                                   # (WorkspaceManager usa tempfile.mkdtemp do SO, nao esta pasta - Secao 6.2)
│   ├── docs/                          # (ainda nao criado) documentacao especifica de implementacao do Worker
│   │   ├── architecture/
│   │   └── adr/                       # ADRs de implementacao (ex.: escolha de biblioteca de tracking) -
│   │                                  # distintas das ADRs constitucionais desta Constituicao
│   ├── scripts/                        # (ainda nao criado) utilitarios operacionais (nao Docker, nao IA)
│   └── tests/                          # tests/ (unitarios), tests/pipeline/, tests/inference/, tests/inference/processors/ (W7),
│                                        # tests/inference/detectors/ (W8), tests/inference/trackers/ (W9),
│                                        # tests/inference/events/ (W10), tests/inference/world/ (W11), tests/domain/ (W12),
│                                        # tests/analyzers/ (W13/W14/W15/W16/W17/W18/W19/W20/W21/W22/W23/W24/W25/W26/W27) - Detector/Tracker/SceneAnalyzer
│                                        # reais (YOLODetector/ByteTrackTracker/BasicSceneAnalyzer), so a inferencia de
│                                        # outros Processors e mockada quando necessario; BasicWorldModel, FootballWorldBuilder,
│                                        # GoalkeeperPresenceAnalyzer, GoalGeometryAnalyzer, GoalkeeperPositionAnalyzer,
│                                        # BallPositionAnalyzer, GoalkeeperBallAlignmentAnalyzer, BallMotionAnalyzer,
│                                        # ShotAnalyzer, BallTrajectoryAnalyzer, PlaySituationAnalyzer,
│                                        # GoalkeeperDecisionAnalyzer, GoalkeeperDecisionEvaluationAnalyzer,
│                                        # PlayOutcomeAnalyzer, GoalkeeperPerformanceEvaluationAnalyzer,
│                                        # GoalkeeperCoachingAnalyzer e GoalkeeperAnalysisReportAnalyzer testados 100%
│                                        # real (nenhuma dependencia externa a mockar);
│                                        # tests/video/ (videos reais gerados com OpenCV, nunca mockados);
│                                        # tests/infrastructure/ (Redis real, HTTP mockado)
└── docs/                             # documentacao do monorepo (AI_WORKER_ARCHITECTURE.md,
                                       # AI_WORKER_CONSTITUTION.md e DOMAIN_ARCHITECTURE.md residem
                                       # hoje na raiz do IA_GK - reorganizacao para docs/ e um
                                       # follow-up ainda nao decidido, nao executado nesta revisao)
```

**Regra de isolamento (Frozen):** nenhum `import` cruza a fronteira entre `backend_fastapi/` e `goalkeeper_ai_worker/`, em nenhuma direção. `goalkeeper_ai_worker/` tem `requirements.txt`, ambiente Python e ciclo de deploy inteiramente próprios — a única coisa que os dois compartilham é o histórico do Git.

---

## 13. Roadmap

Cada sprint é totalmente funcional. **Revisado após a conclusão da Sprint W1** — substitui o detalhamento fino original por um roadmap de sprints mais amplas, aprovado explicitamente pelo usuário.

| Sprint | Objetivo | Escopo |
|---|---|---|
| **W1 — Fundação** ✓ concluída | Esqueleto do projeto, sem IA, sem integração externa | Estrutura do projeto Python independente, configuração centralizada (`config/`), logging estruturado (`observability/`), ciclo de vida do processo com shutdown gracioso (`core/`), ambiente/dependências/testes próprios. Nenhuma conexão com Redis, API do backend, R2 ou GPU. |
| **W2 — Comunicação** ✓ concluída | Integração com os três contratos públicos do Boundary Enforcement | Redis (consumer group do stream `processing_jobs`), `backend_client` (Worker API, autenticado por API Key), acesso ao R2 via URLs assinadas de download/upload, Lock distribuído por vídeo (ADR-001/003). |
| **(W2.1 — Alinhamento arquitetural)** ✓ concluída | Sincronização documental, sem funcionalidade nova | `contracts/`/`infrastructure/`/`core/` incorporados à Constituição; `QueueConnectionError` corrigida; commit de bootstrap. |
| **W3 — Pipeline de Processamento** ✓ concluída | Orquestração completa do ciclo de um Job, ainda sem IA | `WorkerOrchestrator`, os 10 Stages (Seção 2.1), `PipelineState`, `WorkspaceManager`, eventos internos, download/upload reais. Pipeline "vazio" no lugar da Inferência (`FakeProcessingStage`, depois removida). |
| **W4 — Arquitetura da Camada de Inferência** ✓ concluída | Inferência plugável, ainda sem IA real | Módulo `inference/` completo (Seção 6.1): `InferenceEngine`, `FakeInferenceEngine`, Registry, tipos, exceções. `InferenceStage` substitui `FakeProcessingStage` e absorve `GenerateArtifactStage`. **Não** foi "primeiro modelo real" como esta linha previa originalmente — foi a arquitetura que torna o primeiro modelo real uma troca de Plugin, não uma reescrita. |
| **(W4.1 — Alinhamento arquitetural)** ✓ concluída | Sincronização documental, sem funcionalidade nova | Esta revisão: Seções 1/2/6/12 sincronizadas com W3/W4; Seção 15 (nova) define a W5. Encerra oficialmente a fase de arquitetura do Worker. |
| **W5 — Infraestrutura de Leitura de Vídeo** ✓ concluída | Apenas leitura de vídeo — sem detecção, inferência, IA ou modelos | Módulo `video/` completo (Seção 6.3): `VideoReader`, `FrameProvider`, `FrameIterator`, `FrameMetadata` real (dimensões/fps/contagem de frames/duração de verdade). `FakeInferenceEngine` passou a consumi-lo. Resolvida a questão em aberto da Seção 15 original: `video/` é módulo irmão de `inference/`, nunca dentro dele. |
| **W6 — Primeiro Motor Real de Visão (`BasicVisionEngine`)** ✓ concluída | Camada de visão computacional reutilizável — **ainda sem detecção, YOLO, tracking, classificação ou GPU** | `BasicVisionEngine` (Seção 6.1) — motor **padrão** desde esta sprint; processa frames de verdade (resize/ROI/frame-skip/conversão de cor via `inference/frame_ops.py`, reutilizável por qualquer motor futuro), produz estatísticas básicas. `FakeInferenceEngine` mantida só para testes. **A partir desta sprint, não há mais sprints de sincronização (Wx.1)** — documentação atualizada durante a própria sprint (Definition of Done). |
| **W7 — Pipeline de Processamento de Visão (Processors)** ✓ concluída | Decompor a transformação de frame em Processors independentes — **ainda sem detecção, YOLO, tracking, classificação ou GPU** | `inference/processors/` (Seção 6.1): `FrameProcessor` (contrato), `ColorProcessor`/`ResizeProcessor`/`ROIProcessor`/`StatisticsProcessor`, Registry próprio (ordem de registro = ordem de execução), `PipelineProcessor` (executa a sequência habilitada). `BasicVisionEngine` (v2.0.0) deixa de transformar frame diretamente — vira orquestrador fino que delega tudo à `PipelineProcessor`. Cada Processor habilitável por configuração (`WORKER_ENABLE_*`), sem alterar código. Métricas por Processor entram no artefato (`"processors"`/`"processor_order"`). **Não era mais "primeiro detector real" como esta linha previa originalmente** — foi a arquitetura que torna um futuro `YOLOProcessor` um novo Processor isolado, não uma reescrita do motor. |
| **W8 — Detection API + Primeiro Detector (YOLO)** ✓ concluída | Primeiro modelo real de detecção, por trás de uma abstração própria — **ainda sem tracking, classificação, pose ou GPU** | `inference/detectors/` (Seção 6.1): `Detector` (contrato `detect(frame) -> DetectionResult`), `YOLODetector` (primeira implementação, Ultralytics YOLO11n, CPU-only), Registry e `factory.py` próprios. `YOLOProcessor` (novo Processor, `inference/processors/`) só chama `Detector.detect()` e acumula o resultado no contexto — nenhuma linha de Ultralytics/pesos/modelo fora de `yolo_detector.py`. Habilitável por `WORKER_DETECTOR=yolo`, sem alterar `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator` ou `video/`. Resultados de detecção entram no artefato (`"detection_results"`). |
| **W9 — Tracking API + Primeiro Tracker (ByteTrack)** ✓ concluída | Associar detecções entre frames, por trás de uma abstração própria — **ainda sem classificação, pose ou GPU** | `inference/trackers/` (Seção 6.1): `Tracker` (contrato `track(DetectionResult) -> TrackingResult`, + `reset()`), `ByteTrackTracker` (primeira implementação, reaproveita `ultralytics.trackers.byte_tracker.BYTETracker`, CPU-only), Registry e `factory.py` próprios. `TrackingProcessor` (novo Processor, `inference/processors/`) só chama `Tracker.track()` e acumula o resultado no contexto — nenhuma linha de ByteTrack fora de `bytetrack_tracker.py`. Habilitável por `WORKER_TRACKER=bytetrack` + `WORKER_TRACKING_ENABLED=true`, sem alterar `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `YOLOProcessor` ou `video/`. Resultados de tracking entram no artefato (`"tracking_results"`/`"tracking_engine"`/`"tracking_statistics"`/`"tracking_time_ms"`). **Achado arquitetural corrigido nesta sprint:** a mesma instância de `BasicVisionEngine`/`PipelineProcessor` é reaproveitada entre Jobs sequenciais pelo `WorkerOrchestrator` (construída uma única vez) — sem um `reset()` explícito no início de cada Job, um Tracker stateful vazaria `TrackId`s de um vídeo para o próximo (ver Seção 6.1, "Estado entre Jobs"). |
| **W10 — Scene Events API** ✓ concluída | Primeira camada de interpretação de cena, por trás de uma abstração própria — **ainda sem lógica específica de futebol (defesa/gol/chute), pose, MediaPipe, classificação ou GPU** | `inference/events/` (Seção 6.1): `SceneAnalyzer` (contrato `analyze(TrackingResult) -> SceneAnalysisResult`, + `reset()`), `BasicSceneAnalyzer` (primeira implementação, 9 tipos de evento genérico: `TrackStarted`/`TrackUpdated`/`TrackLost`/`TrackRecovered`/`ObjectEnteredFrame`/`ObjectLeftFrame`/`ObjectStopped`/`ObjectMoving`/`OcclusionDetected`), Registry e `factory.py` próprios. `SceneAnalysisProcessor` (novo Processor, `inference/processors/`) só chama `SceneAnalyzer.analyze()` e acumula o resultado no contexto — zero regra de negócio. Habilitável por `WORKER_SCENE_ANALYZER=basic` + `WORKER_SCENE_ANALYSIS_ENABLED=true`, sem alterar `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker` ou `video/`. Eventos entram no artefato (`"scene_events"`/`"scene_statistics"`/`"scene_processing_time_ms"`). Reaproveitou, sem alterações, a plumbing de `reset()` entre Jobs já introduzida na W9 (mesma classe de problema: `SceneAnalyzer` também é stateful). |
| **W11 — World Model API** ✓ concluída | Última camada genérica da arquitetura - memória persistente e consistente da cena — **ainda sem lógica de futebol, sem detecção, tracking ou decisão** | `inference/world/` (Seção 6.1): `WorldModel` (contrato `update(SceneAnalysisResult) -> WorldState`, + `reset()`), `BasicWorldModel` (primeira implementação — `ObjectState` por `track_id`: posição/bbox/trajetória/velocidade/aceleração/idade/visibilidade, nunca muta em lugar, sempre reconstrói), `Trajectory`/`History` (buffers limitados, nunca crescem infinitamente), `compute_motion` (cinemática pura). `WorldModelProcessor` (novo Processor) só chama `WorldModel.update()` e acumula o resultado — zero regra de negócio. Habilitável por `WORKER_WORLD_MODEL=basic` + `WORKER_WORLD_MODEL_ENABLED=true`, sem alterar `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer` ou `video/`. `WorldState` entra no artefato (`"world_state"`/`"world_statistics"`/`"object_count"`/`"active_tracks"`/`"lost_tracks"`/`"average_speed"`/`"processing_time_ms"`). Reaproveitou, sem alterações, a plumbing de `reset()` entre Jobs já introduzida na W9 (terceira confirmação consecutiva de que o mecanismo genérico funciona). **Achado necessário desta sprint:** `SceneAnalysisResult` (W10) precisou ganhar um novo campo `objects: list[SceneObjectSnapshot]` — `SceneEvent` sozinho não carrega bbox/posição, e o World Model só pode consumir `SceneAnalysisResult` (nunca `TrackingResult` diretamente). |
| **W12 — Football Domain Model** ✓ concluída | Modelar as entidades fundamentais do domínio de futebol a partir de `WorldState` — **ainda sem análise, sem IA, sem regra de negócio, sem heurística** | `worker/domain/` (Seção 6.4): `FootballWorld` (goleiro(s)/bola/jogadores/gols/campo/direção do jogo, tudo como entidades ricas, não dicts), `FootballWorldBuilder.build(WorldState) -> FootballWorld` (só transforma dados — a única decisão é o dispatch por rótulo de classe, não uma heurística de futebol), `entities/` (`Goalkeeper`/`Player`/`Ball`/`Goal`/`Field`), `geometry/` (`Coordinate`/`Vector`/`Direction`/`Region`, reusáveis por todos os futuros analisadores). Deliberadamente **sem** Registry/factory — implementação única e canônica, não uma família de Plugin. `FootballDomainProcessor` (novo Processor) só chama `FootballWorldBuilder.build()` e acumula o resultado — zero regra de negócio. Habilitável por `WORKER_FOOTBALL_DOMAIN_ENABLED=true`, sem alterar `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel` ou `video/`. `FootballWorld` entra no artefato (`"football_world"`/`"football_domain_time_ms"`). Reaproveitou, sem alterações, a plumbing de `reset()` entre Jobs já introduzida na W9 (quarta confirmação consecutiva — desta vez aplicada proativamente, não redescoberta). |
| **W13 — Analyzer API + GoalkeeperPresenceAnalyzer** ✓ concluída | Construir a infraestrutura de Analyzers e um primeiro Analyzer extremamente simples — **ainda sem Save/Shot/Dive/Goal Analyzer, pose estimation, MediaPipe ou classificação de desempenho** | `worker/analyzers/` (Seção 6.5): `Analyzer` (contrato `analyze(FootballWorld) -> AnalysisResult`), `GoalkeeperPresenceAnalyzer` (1ª implementação — existe goleiro?/quantos?/qual track_id?/visível?/há quantos frames?/posição/bbox atuais — totalmente determinístico, zero heurística/regra de futebol/avaliação/julgamento), `results.py` (`AnalysisResult`/`AnalyzerMetadata`/`AnalysisStatistics`, tipos fortemente tipados), Registry e `factory.py` próprios (sétima família de Plugin — única em que VÁRIAS implementações coexistem simultaneamente, via `WORKER_ANALYZERS`). `AnalyzerProcessor` (`worker/analyzers/processor.py`, registrado em `inference/processors/registry.py`) só delega — zero regra de negócio. Habilitável por `WORKER_ANALYZERS=goalkeeper_presence`, sem alterar `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel`, `FootballDomainProcessor` ou `video/`. `AnalysisResult`s entram no artefato (`"analysis_results"`/`"analysis_statistics"`/`"analyzer_processing_time_ms"`). Reaproveitou, sem alterações, a plumbing de `reset()` entre Jobs já introduzida na W9 (quinta confirmação consecutiva). **Achado necessário desta sprint:** `TrackedFootballEntity` (W12) precisou ganhar `bbox: Region` — nenhuma entidade de domínio carregava bounding box até então, e `GoalkeeperPresenceAnalyzer` precisa responder "qual o bbox atual?" sem violar Boundary Enforcement (`Analyzer` só conhece `FootballWorld`). |
| **W14 — Goal Geometry Analyzer** ✓ concluída | Modelar a geometria do gol (centro, postes, dimensões, regiões/zonas de cobertura) — **ainda sem avaliação de goleiro, defesa, chute, mergulho, reação ou qualquer heurística de futebol**. **Não foi "a primeira análise com julgamento real" como esta linha previa originalmente** — continuou a série de Analyzers puramente factuais/geométricos, adiando o julgamento real (a W15 também não foi - ver linha abaixo) | `worker/analyzers/goal_geometry.py` (Seção 6.5): `GoalGeometryAnalyzer` (2ª implementação — goal_detected/centro/postes/largura/altura/regiões via grade 2×3 de zonas de cobertura, `confidence` como validade estrutural, nunca probabilidade inventada), `GoalGeometryResult`/`GoalZone` (novos tipos em `results.py`/`types.py`). Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel`, `FootballDomainProcessor` ou `video/` — confirmação prática de que a Analyzer API (W13) já suporta múltiplos Analyzers simultâneos sem qualquer extensão estrutural. Habilitável incluindo `goal_geometry` em `WORKER_ANALYZERS`. Resultado entra no artefato (`"goal_geometry_result"`, alias de `analysis_results["goal_geometry"]`). **Não precisou de nenhum achado arquitetural** — `Goal`/`Field` (W12) já carregavam tudo (`region`) que o Analyzer precisava. **Padrão de composição confirmado para W15+:** um Analyzer futuro que precise de `GoalGeometryResult` pode simplesmente instanciar `GoalGeometryAnalyzer` internamente e chamar `.analyze(football_world)` como função pura — nenhuma mudança de contrato necessária. |
| **W15 — Goalkeeper Position Analyzer** ✓ concluída | Medir a relação geométrica entre goleiro e gol (distância, deslocamentos lateral/em profundidade, ângulo, dentro de área de meta/pênalti, alinhamento com os terços do gol) — **ainda sem avaliação de qualidade, sem julgamento de posicionamento correto**. **Não foi "a primeira análise com julgamento real" como a linha da W14 previa** — terceira sprint consecutiva puramente de medição/geometria (a W16 também não foi - ver linha abaixo) | `worker/analyzers/goalkeeper_position.py` (Seção 6.5): `GoalkeeperPositionAnalyzer` (3ª implementação — primeiro Analyzer a COMPOR outro, instanciando `GoalGeometryAnalyzer` internamente), `GoalkeeperPositionResult` (novo tipo em `results.py`). Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, `GoalGeometryAnalyzer`, `Detector`, `Tracker`, `SceneAnalyzer` ou `video/` — confirmação prática (2ª vez consecutiva) de que a Analyzer API já suporta múltiplos Analyzers simultâneos, e primeira confirmação real do padrão de composição entre Analyzers definido na W14. Habilitável incluindo `goalkeeper_position` em `WORKER_ANALYZERS` (funciona mesmo sem `goal_geometry` incluído, já que a composição é interna). Resultado entra no artefato (`"goalkeeper_position_result"`, alias de `analysis_results["goalkeeper_position"]`). **Não precisou de nenhum achado arquitetural** — `Goal`/`Field`/`TrackedFootballEntity.confidence` (W12/W13) já carregavam tudo que o Analyzer precisava. |
| **W16 — Ball Position Analyzer** ✓ concluída | Medir a relação geométrica entre a bola e o gol (distância, deslocamentos, ângulo, dentro de área de meta/pênalti, zona do gol interceptada) — **ainda sem análise de chute, sem previsão de trajetória, sem avaliação de decisões do goleiro**. **Não foi "a primeira análise com julgamento real" como a linha da W15 previa** — quarta sprint consecutiva puramente de medição/geometria (a W17 também não foi - ver linha abaixo) | `worker/analyzers/ball_position.py` (Seção 6.5): `BallPositionAnalyzer` (4ª implementação — segundo Analyzer a compor `GoalGeometryAnalyzer`, mesmo padrão exato da W15 aplicado a `football_world.balls[0]`), `BallPositionResult` (novo tipo em `results.py`, com `ball_region` — novo conceito de containment na grade 2×3 do gol). `field_areas.py` (novo) extrai `estimate_goal_and_penalty_areas()` de `goalkeeper_position.py` para reuso, sem alterar comportamento (testes da W15 inalterados). Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, `GoalGeometryAnalyzer`, `Detector`, `Tracker`, `SceneAnalyzer` ou `video/` — terceira confirmação consecutiva do padrão de encaixe e segunda confirmação em produção do padrão de composição (W15 e W16). Habilitável incluindo `ball_position` em `WORKER_ANALYZERS` (funciona mesmo sem `goal_geometry` incluído). Resultado entra no artefato (`"ball_position_result"`). **Não precisou de nenhum achado arquitetural de contrato** — só uma refatoração de extração de duplicação (`field_areas.py`). |
| **W17 — Goalkeeper Ball Alignment Analyzer** ✓ concluída | Medir a relação espacial entre goleiro, bola e gol (distâncias/ângulos par-a-par, alinhamento do goleiro com a reta bola→gol) — **ainda sem avaliação de desempenho, sem julgamento de posicionamento, sem detecção de chutes**. **Não foi "a primeira análise com julgamento real" como a linha da W16 previa** — quinta sprint consecutiva puramente de medição/geometria (a W18 também não foi - ver linha abaixo) | `worker/analyzers/goalkeeper_ball_alignment.py` (Seção 6.5): `GoalkeeperBallAlignmentAnalyzer` (5ª implementação — **primeiro Analyzer RELACIONAL**, compõe TRÊS Analyzers internamente: `GoalGeometryAnalyzer`/`GoalkeeperPositionAnalyzer`/`BallPositionAnalyzer`, combinando apenas seus resultados já produzidos), `GoalkeeperBallAlignmentResult` (novo tipo em `results.py`, incluindo `alignment_line`/`alignment_offset`/`is_between_ball_and_goal` — projeção geométrica do goleiro sobre a reta bola→gol). Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou nos três Analyzers compostos — quarta confirmação consecutiva do padrão de encaixe e primeira confirmação de que o padrão de composição escala para TRÊS Analyzers simultâneos dentro de um quarto. Habilitável incluindo `goalkeeper_ball_alignment` em `WORKER_ANALYZERS` (funciona mesmo sem os três Analyzers compostos incluídos). Resultado entra no artefato (`"goalkeeper_ball_alignment_result"`). **Não precisou de nenhum achado arquitetural.** |
| **W18 — Ball Motion Analyzer** ✓ concluída | Medir o movimento OBSERVADO da bola entre frames (deslocamento, velocidade, direção, aceleração observada) — **ainda sem detecção de chute, sem previsão de trajetória futura, sem avaliação de risco**. **Não foi "a primeira análise com julgamento real" como a linha da W17 previa** — sexta sprint consecutiva puramente de medição (a W19 finalmente introduziu uma decisão, mas ainda não uma avaliação de qualidade - ver linha abaixo). **Primeiro Analyzer STATEFUL** da Analyzer API | `worker/analyzers/ball_motion.py` (Seção 6.5): `BallMotionAnalyzer` (6ª implementação — compõe `BallPositionAnalyzer` internamente, usa `AnalyzerContext` pela primeira vez de forma real via `BallMotionContext`, com `reset()` genuíno), `BallMotionResult` (novo tipo em `results.py`). Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou `BallPositionAnalyzer` — quinta confirmação consecutiva do padrão de encaixe, e primeira confirmação de que `AnalyzerProcessor.reset()` (existente e nunca modificada desde a W13) já delegava corretamente a `analyzer.reset()` de cada Analyzer, mesmo antes de qualquer Analyzer real precisar dele. Nunca extrapola através de uma lacuna: bola desaparecida OU `track_id` trocado reinicia a continuidade de observação (`frames_observed` volta a 1, sem inferir deslocamento sobre o intervalo não observado). `confidence` usa `Ball.confidence` real (não o `confidence` combinado de `BallPositionResult`, que incluiria `goal_geometry.confidence`, irrelevante para medir movimento). Resultado entra no artefato (`"ball_motion_result"`). **Não precisou de nenhum achado arquitetural de contrato** — deliberadamente NÃO reaproveitou `compute_motion()`/`Motion` do World Model (W11), reimplementando a mesma matemática localmente para não importar de `worker.inference.world` (fora do Boundary Enforcement de `analyzers/`). |
| **W19 — Shot Analyzer** ✓ concluída | Identificar se houve um evento compatível com um chute (velocidade mínima observada + direção consistente com o gol + movimento contínuo, todos parametrizáveis) — **ainda sem detecção de gol, sem avaliação de defesa, sem avaliação de qualidade**. **É a primeira vez que um Analyzer toma uma DECISÃO** (`shot_detected: bool`) em vez de só relatar fatos/geometria/movimento — mas essa decisão é 100% determinística, parametrizável e documentada (nenhuma IA, nenhum modelo treinado, nenhuma heurística opaca), então **não é ainda** a "análise com julgamento de qualidade" que a linha da W18 previa — essa continua adiada, agora para a W20 | `worker/analyzers/shot.py` (Seção 6.5): `ShotAnalyzer` (7ª implementação — primeiro Analyzer de EVENTOS, compõe TRÊS Analyzers: `BallMotionAnalyzer`/`BallPositionAnalyzer`/`GoalGeometryAnalyzer`, combinando apenas resultados já produzidos), `ShotAnalysisResult` (novo tipo em `results.py`), `ShotAnalyzerContext` (segundo Analyzer STATEFUL, mesmo padrão da W18). Critérios expostos via `WORKER_SHOT_MIN_SPEED`/`WORKER_SHOT_MAX_ANGLE_DEVIATION_DEGREES`/`WORKER_SHOT_MIN_CONSECUTIVE_FRAMES`. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou nos três Analyzers compostos — sexta confirmação consecutiva do padrão de encaixe, e confirmação de que composição múltipla + estado próprio coexistem sem atrito. Resultado entra no artefato (`"shot_analysis_result"`). **Achado desta sprint (não de contrato, mas de calibração — Risco 34):** a validação manual revelou que a geometria placeholder normalizada (`Goal`/`Field`) e as posições reais em pixel cru vivem em escalas incompatíveis, afetando `towards_goal` em produção real — o teste de integração foi ajustado para verificar apenas a FORMA do resultado, deixando a prova geométrica exata para os testes unitários com coordenadas controladas. |
| **W20 — Ball Trajectory Analyzer** ✓ concluída | Modelar a FORMA da trajetória observada da bola ao longo de múltiplos frames (sequência de posições, comprimento do caminho, direção dominante, consistência de direção, mudanças de direção, linearidade) — **ainda sem detecção de gol, sem avaliação de defesa, sem julgamento de decisões do goleiro, sem previsão de posições futuras**. **Não foi "a primeira análise com avaliação de qualidade" como a linha da W19 previa** — volta a ser puramente descritiva (nem sequer um evento binário como o `ShotAnalyzer`, W19); a avaliação de qualidade continua adiada, agora para a W21. **Segundo Analyzer STATEFUL** (depois de `BallMotionAnalyzer`, W18) | `worker/analyzers/ball_trajectory.py` (Seção 6.5): `BallTrajectoryAnalyzer` (8ª implementação — compõe TRÊS Analyzers: `BallMotionAnalyzer`/`BallPositionAnalyzer`/`GoalGeometryAnalyzer`, este último usado só para `confidence`, nenhum campo de trajetória depende de geometria do gol), `BallTrajectoryResult` (novo tipo em `results.py`), `BallTrajectoryContext` (terceiro `AnalyzerContext` real, acumula `points: list[Coordinate]` da mesma bola continuamente observada). Parâmetro exposto via `WORKER_TRAJECTORY_DIRECTION_CHANGE_THRESHOLD_DEGREES`. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou nos três Analyzers compostos — sétima confirmação consecutiva do padrão de encaixe. Resultado entra no artefato (`"ball_trajectory_result"`). **Achado desta sprint:** confirmado que compor um Analyzer sem usar NENHUM de seus campos diretamente (`GoalGeometryAnalyzer`, aqui) continua sendo uma composição legítima, desde que documentado por quê; Risco 34 (W19) deliberadamente não corrigido, por instrução explícita, mas confirmado que não se agrava (nenhum cálculo de trajetória referencia geometria do gol). **Novo risco (35):** `trajectory_points` cresce sem limite para observações contínuas longas — aceito nesta sprint, sem `WORKER_TRAJECTORY_MAX_POINTS` (nenhum limite foi pedido). |
| **W21 — Play Situation Analyzer** ✓ concluída | Classificar o estado OBSERVADO da jogada atual (`PlaySituation`, fortemente tipado) combinando quatro Analyzers já existentes — **ainda sem avaliar o goleiro, sem avaliar defesa, sem julgar decisões, sem emitir nota de qualidade**. **Não foi "a primeira análise com avaliação de qualidade" como a linha da W20 previa** — é uma CLASSIFICAÇÃO determinística do estado observável, não um julgamento de mérito; a avaliação de qualidade continua adiada, agora para a W22. **Primeiro Analyzer COGNITIVO** (marca a transição da percepção para a cognição), mas sem estado próprio — não é um quarto `AnalyzerContext` | `worker/analyzers/play_situation.py` (Seção 6.5): `PlaySituationAnalyzer` (9ª implementação — compõe QUATRO Analyzers: `ShotAnalyzer`/`BallTrajectoryAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`GoalGeometryAnalyzer`, todos ecoados, nunca recalculados), `PlaySituationResult` (novo tipo em `results.py`), `PlaySituation(str, Enum)` (novo tipo em `types.py` — `UNKNOWN`/`NO_BALL_VISIBLE`/`NO_GOALKEEPER_VISIBLE`/`BALL_STATIONARY`/`BALL_MOVING`/`SHOT_DETECTED`/`SHOT_TOWARDS_GOAL`/`SHOT_AWAY_FROM_GOAL`). `situation` (primário) vem de uma árvore de decisão priorizada de 6 níveis; `sub_state` (refinamento opcional sobre direção) exige `motion_detected is True` para evitar um falso-positivo herdado de `ShotAnalyzer.towards_goal` (Risco 36, novo). Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou nos quatro Analyzers compostos — oitava confirmação consecutiva do padrão de encaixe, e confirmação de que compor Analyzers que já compõem outros Analyzers (dois níveis de profundidade) não exige nenhum tratamento especial. Resultado entra no artefato (`"play_situation_result"`). Riscos 34/35 (W19/W20) permanecem inalterados por instrução explícita. **Deliberadamente NÃO implementados nesta sprint:** `BALL_INSIDE_GOAL_AREA`/`BALL_INSIDE_PENALTY_AREA` (exemplos da especificação) — exigiriam compor `BallPositionAnalyzer` diretamente, fora da lista explícita de quatro Analyzers a reutilizar. |
| **W22 — Goalkeeper Decision Analyzer** ✓ concluída | Classificar a decisão OBSERVADA que o goleiro aparenta estar executando (`GoalkeeperDecision`, fortemente tipado) combinando cinco Analyzers já existentes — **ainda sem afirmar se a decisão foi correta, sem avaliar desempenho, sem avaliar resultado da jogada**. **Não foi "a primeira análise com avaliação de qualidade" como a linha da W21 previa** — é uma CLASSIFICAÇÃO de comportamento, não um julgamento de mérito; a avaliação de qualidade continua adiada, agora para a W23. **Primeiro Analyzer ESPECÍFICO DO GOLEIRO, quarto STATEFUL** | `worker/analyzers/goalkeeper_decision.py` (Seção 6.5): `GoalkeeperDecisionAnalyzer` (10ª implementação — compõe CINCO Analyzers: `PlaySituationAnalyzer`/`GoalkeeperPositionAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`BallTrajectoryAnalyzer`/`ShotAnalyzer`, todos ecoados, nunca recalculados), `GoalkeeperDecisionResult` (novo tipo em `results.py`), `GoalkeeperDecision(str, Enum)` (novo tipo em `types.py` — `UNKNOWN`/`STAY_ON_LINE`/`STEP_FORWARD`/`STEP_BACK`/`SHIFT_LEFT`/`SHIFT_RIGHT`/`PREPARE_DIVE`/`DIVE_LEFT`/`DIVE_RIGHT`/`RECOVER_POSITION`), `GoalkeeperDecisionContext` (quarto `AnalyzerContext` real — `movement_direction`/`movement_speed` do PRÓPRIO goleiro são a única informação genuinamente nova, já que nenhum Analyzer composto rastreia isso). Dois parâmetros novos: `WORKER_GOALKEEPER_SHIFT_MIN_SPEED`/`WORKER_GOALKEEPER_DIVE_MIN_SPEED`. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou nos cinco Analyzers compostos — nona confirmação consecutiva do padrão de encaixe, e confirmação de que a profundidade de composição (seis camadas via `PlaySituationAnalyzer`) não exige nenhum tratamento especial. Resultado entra no artefato (`"goalkeeper_decision_result"`). Riscos 34/35/36 (W19/W20/W21) permanecem inalterados. **Novo risco (37):** `PREPARE_DIVE` é um proxy determinístico baseado em velocidade, não uma detecção real de postura (o Worker não tem estimativa de pose) — documentado honestamente. |
| **W23 — Goalkeeper Decision Evaluation Analyzer** ✓ concluída | Responder se a decisão observada do goleiro (W22) foi COMPATÍVEL com a situação observada (W21), via um mecanismo explícito e auditável de Rule Evaluation — **ainda sem avaliar o resultado da jogada, sem afirmar se houve defesa ou gol**. **Não foi "a primeira análise com avaliação de qualidade" como a linha da W22 previa** — é uma verificação de CONSISTÊNCIA lógica entre camadas já produzidas (Observação→Situação→Decisão→Avaliação), não um julgamento de mérito sobre o RESULTADO; a avaliação de qualidade do resultado continua adiada, agora para a W24. **Primeiro Analyzer de AVALIAÇÃO, primeira peça da camada de Explainability** | `worker/analyzers/goalkeeper_decision_evaluation.py` + novo módulo genérico `worker/analyzers/rules.py` (Seção 6.5): `GoalkeeperDecisionEvaluationAnalyzer` (11ª implementação — compõe SEIS Analyzers: `PlaySituationAnalyzer`/`GoalkeeperDecisionAnalyzer`/`GoalkeeperPositionAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`BallTrajectoryAnalyzer`/`ShotAnalyzer`, três deles só para `confidence`), `GoalkeeperDecisionEvaluationResult` (novo tipo em `results.py`), `GoalkeeperDecisionEvaluation(str, Enum)` (novo tipo em `types.py` — `UNKNOWN`/`INSUFFICIENT_INFORMATION`/`COMPATIBLE`/`PARTIALLY_COMPATIBLE`/`INCOMPATIBLE`), `Rule`/`RuleOutcome`/`evaluate_rules` (`rules.py`, genérico e reutilizável). 6 regras determinísticas, cada `RuleOutcome` sempre com `rule_id`/descrição/resultado/explicação textual — nenhum veredito sem justificativa. Um parâmetro novo: `WORKER_GOALKEEPER_EVALUATION_MIN_LATERAL_SIGNAL`. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, ou nos seis Analyzers compostos — décima confirmação consecutiva do padrão de encaixe. Resultado entra no artefato (`"goalkeeper_decision_evaluation_result"`). Riscos 34/35/36/37 (W19/W20/W21/W22) permanecem inalterados. Corrigiu, via teste falho, uma leitura incorreta de `direction_vector` (normalizado, não cru). **Novo risco (38):** a maioria das regras de conteúdo são auditorias garantidas por construção — `INCOMPATIBLE` puro não é alcançável hoje só via composição real, testado diretamente na função de agregação. |
| **W24 — Play Outcome Analyzer** ✓ concluída | Responder qual foi o resultado OBSERVADO da jogada (`PlayOutcome`, fortemente tipado), combinando cinco Analyzers já existentes — **ainda sem avaliar o goleiro, sem atribuir nota, sem julgar a qualidade da decisão**. **Não foi "a primeira análise com avaliação de qualidade" como a linha da W23 previa** — encerra a cadeia de OBSERVAÇÃO (Observação→Situação→Decisão→Avaliação→Resultado), não introduz nenhum julgamento de mérito; a avaliação COMPLETA de desempenho continua adiada, agora para a W25, que combinará os quatro resultados desta cadeia. **Quinto Analyzer STATEFUL** | `worker/analyzers/play_outcome.py` (Seção 6.5): `PlayOutcomeAnalyzer` (12ª implementação — compõe CINCO Analyzers: `PlaySituationAnalyzer`/`ShotAnalyzer`/`BallTrajectoryAnalyzer`/`GoalGeometryAnalyzer`/`GoalkeeperDecisionAnalyzer`, todos ecoados, nunca recalculados; usa `Region.contains()`/`distance()` como utilitários puros, não lógica de outro Analyzer), `PlayOutcomeResult` (novo tipo em `results.py`), `PlayOutcome(str, Enum)` (novo tipo em `types.py` — `UNKNOWN`/`INSUFFICIENT_INFORMATION`/`SAVE`/`GOAL`/`BALL_OUT`/`BLOCKED`/`POST`/`CROSSBAR`/`LOST_TRACK`/`NO_SHOT_DETECTED`), `PlayOutcomeContext` (quinto `AnalyzerContext` real — memoriza a última posição conhecida da bola/do goleiro e se a bola estava sendo rastreada, para reconhecer `LOST_TRACK`). Dois parâmetros novos: `WORKER_OUTCOME_POST_PROXIMITY_PX`/`WORKER_OUTCOME_SAVE_PROXIMITY_PX`. Por instrução explícita, NÃO reutiliza a Rule Evaluation da W23 — `supporting_evidence` é uma lista simples de frases. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, `Rule Evaluation`, ou nos cinco Analyzers compostos — décima primeira confirmação consecutiva do padrão de encaixe. Resultado entra no artefato (`"play_outcome_result"`). Riscos 34/35/36/37/38 permanecem inalterados. **Novo risco (39):** `GoalGeometryResult.left_post`/`right_post` (W14) nunca haviam sido consumidos até agora e não correspondem à real separação lateral dos postes - `CROSSBAR` permanece deliberadamente nunca produzido. Validação manual confirmou, com DOIS Jobs consecutivos no mesmo processo, resultados idênticos e independentes (nenhum estado vazado). |
| **W25 — Goalkeeper Performance Evaluation Analyzer** ✓ concluída | Responder como foi o desempenho OBSERVADO do goleiro (`GoalkeeperPerformanceEvaluation`, fortemente tipado), combinando EXCLUSIVAMENTE `GoalkeeperDecisionEvaluation` (W23) e `PlayOutcome` (W24) via Rule Evaluation (W23) — **ainda sem gerar recomendação, sem coaching, sem sugerir melhoria**. Encerra a cadeia de AVALIAÇÃO (Situação→Decisão→Avaliação da Decisão→Resultado→Avaliação de Desempenho) — é a classificação FINAL da jogada, não ainda uma recomendação. **Primeiro reaproveitamento genuíno da infraestrutura de Rule Evaluation** (W23) por um Analyzer completamente diferente | `worker/analyzers/goalkeeper_performance_evaluation.py` (Seção 6.5): `GoalkeeperPerformanceEvaluationAnalyzer` (13ª implementação — compõe QUATRO Analyzers: `PlaySituationAnalyzer`/`GoalkeeperDecisionAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer`, dois deles só para `confidence`), `GoalkeeperPerformanceEvaluationResult` (novo tipo em `results.py`), `GoalkeeperPerformanceEvaluation(str, Enum)` (novo tipo em `types.py` — `UNKNOWN`/`INSUFFICIENT_INFORMATION`/`EXCELLENT`/`GOOD`/`ADEQUATE`/`POOR`/`CRITICAL`). 8 novas `Rule`s (duas de gate + 3+3 de eixo decisão/resultado), agregadas numa matriz determinística 3x3 (ver Seção 6.5). **Nenhum parâmetro numérico novo** — a matriz é puramente categórica. `summary` é uma string ESTRUTURADA, não linguagem natural, por instrução explícita. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, `worker.analyzers.rules`, ou nos quatro Analyzers compostos — décima segunda confirmação consecutiva do padrão de encaixe. Resultado entra no artefato (`"goalkeeper_performance_evaluation_result"`). Riscos 34-39 permanecem inalterados; nenhum achado de contrato foi necessário. |
| **W26 — Goalkeeper Coaching Analyzer** ✓ concluída | Responder qual orientação técnica pode ser extraída da jogada (`GoalkeeperCoaching`, fortemente tipado), interpretando `GoalkeeperPerformanceEvaluation` (W25) + `GoalkeeperDecisionEvaluationResult.rules_failed` (W23) + `GoalkeeperDecision` (W22) + `PlayOutcome` (W24) via Rule Evaluation — **ainda sem gerar linguagem natural, sem produzir relatório final, sem sugerir plano de treino**. **Primeiro Analyzer de COACHING**, marca a transição de AVALIAÇÃO (descreve/classifica/avalia o que já aconteceu) para ORIENTAÇÃO (o que fazer diferente). **Segundo reaproveitamento consecutivo da infraestrutura de Rule Evaluation** (W23/W25) por um terceiro Analyzer completamente diferente | `worker/analyzers/goalkeeper_coaching.py` (Seção 6.5): `GoalkeeperCoachingAnalyzer` (14ª implementação — compõe QUATRO Analyzers: `GoalkeeperPerformanceEvaluationAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer`/`GoalkeeperDecisionAnalyzer`, todos usados como fonte de conteúdo), `GoalkeeperCoachingResult` (novo tipo em `results.py`), `GoalkeeperCoaching(str, Enum)` (novo tipo em `types.py` — `UNKNOWN`/`INSUFFICIENT_INFORMATION`/`NO_FEEDBACK`/`KEEP_POSITION`/`IMPROVE_POSITIONING`/`MOVE_EARLIER`/`MOVE_LATER`/`ATTACK_BALL`/`STAY_PATIENT`/`RECOVER_FASTER`). 8 novas `Rule`s (dois gates + reforço + cinco regras específicas em ordem de prioridade + fallback residual), três delas lendo `rules_passed`/`rules_failed` de `GoalkeeperDecisionEvaluationResult` (Explainability da W23) diretamente. **Nenhum parâmetro numérico novo.** `summary` é uma string ESTRUTURADA, não linguagem natural, mesmo princípio da W25. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, `worker.analyzers.rules`, ou nos quatro Analyzers compostos — décima terceira confirmação consecutiva do padrão de encaixe. Resultado entra no artefato (`"goalkeeper_coaching_result"`). Riscos 34-39 permanecem inalterados; nenhum achado de contrato foi necessário (só um achado estrutural documentado — `ATTACK_BALL` inalcançável via composição real, ver Seção 6.5). |
| **W27 — Goalkeeper Analysis Report Analyzer** ✓ concluída — **ENCERRA O MVP ARQUITETURAL** | Consolidar a cadeia cognitiva completa (`PlaySituationResult`/`GoalkeeperDecisionResult`/`GoalkeeperDecisionEvaluationResult`/`PlayOutcomeResult`/`GoalkeeperPerformanceEvaluationResult`/`GoalkeeperCoachingResult`, via composição direta) num único `GoalkeeperAnalysisReport` — **o CONTRATO OFICIAL de saída do Worker**. **Não cria nenhuma inteligência nova** — agregação pura, zero recálculo, zero regra nova | `worker/analyzers/goalkeeper_analysis_report.py` (Seção 6.5): `GoalkeeperAnalysisReportAnalyzer` (15ª implementação — compõe SEIS Analyzers: `PlaySituationAnalyzer`/`GoalkeeperDecisionAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer`/`GoalkeeperPerformanceEvaluationAnalyzer`/`GoalkeeperCoachingAnalyzer`, todos ecoados integralmente), `GoalkeeperAnalysisReport` (novo tipo em `results.py` — seis sub-resultados tipados + `confidence_summary`/`artifacts`/`analysis_version`/`worker_version`/`generated_at`). **Nenhum `Enum` novo** — primeira sprint cognitiva sem nenhuma classificação nova a tipar. `confidence_summary` CONSOLIDA (nunca recalcula) as seis `confidence`s + `overall`. `artifacts` espelha os seis sub-resultados indexados por nome de Analyzer. Registrado em `analyzers/registry.py` **sem nenhuma mudança** em `AnalyzerProcessor`, `PipelineProcessor`, `BasicVisionEngine`, `FootballDomainProcessor`, `WorldModel`, `worker.analyzers.rules`, ou nos seis Analyzers compostos — décima quarta confirmação consecutiva do padrão de encaixe, e primeira vez que o padrão escala para SEIS Analyzers simultâneos. Resultado entra no artefato (`"goalkeeper_analysis_report"`, agora o CONTRATO OFICIAL — ver Seção 6.5). Riscos 34-39 permanecem inalterados; nenhum achado de contrato foi necessário. Validação manual confirmou a cadeia completa Backend→Redis→R2→Worker→Report, com os quinze Analyzers ativos e o relatório consolidado idêntico ao seu alias no artefato. |

**MVP arquitetural encerrado oficialmente na W27.** Itens registrados para evolução PÓS-MVP, sem sprint definida (ver Seção 16 para a lista completa e justificativa de cada um): Pose Estimation, calibração automática, múltiplas câmeras, tracking 3D, física avançada da bola, Machine Learning para avaliação, feedback em linguagem natural, métricas estatísticas entre sessões. Itens que continuam arquiteturalmente definidos e congelados (ADRs e Seções correspondentes), sem sequenciamento definido: Contrato Backend↔Worker (Seção 9, ADR-002/008), Versionamento completo (Seção 8, ADR-006/007), Metric/Artifact/Report Registry (Seção 6), Observabilidade avançada — métricas internas/heartbeat/health check (Seção 11), Escala horizontal (Seção 10).

---

## 14. Riscos Arquiteturais

1. **Checkpoint local não é durável entre máquinas.** Se outra máquina reivindica o Job via `XCLAIM`, não há checkpoint compartilhado — recomeça do zero nessa máquina. Aceito por simplicidade; reavaliar se crashes frequentes tornarem isso caro na prática.
2. **Redis como único ponto de coordenação** (fila, Lock e heartbeat). Se o Redis cair, publicação, consumo, exclusão mútua e renovação de lock param juntos. Decisão herdada do backend (Sprint 7); reconhecida como ponto único de falha.
3. **A promessa de "trocar Plugin sem alterar o Pipeline" é arquitetural, não física** — frameworks nem sempre têm suporte estável a ROCm para toda arquitetura de modelo recente (ex. SAM2, Grounding DINO).
4. **Padrão de Registry em todo lugar é investimento antecipado** para o dia 1 (1 modelo, 1 tracker) — deliberado, dado o horizonte de 2-3 anos, mas reconhecidamente mais estrutura do que o mínimo necessário na Sprint W1.
5. **Heartbeat como base da renovação do Lock (ADR-003)** cria acoplamento: um bug de heartbeat (sem crash real) liberaria o Lock indevidamente enquanto o Job ainda roda — requer monitoramento dedicado desse caminho.
6. **Armazenamento/versionamento de pesos de modelo** — o princípio está definido (fora do Git, versionado por hash), mas o mecanismo concreto (bucket dedicado vs. volume compartilhado) segue em aberto, a fechar antes da W3.
7. **Uso de disco local sob carga** — múltiplos Jobs concorrentes na mesma máquina exigem disciplina de quota/limpeza do workspace; sem isso, risco real de esgotamento de disco.
8. **Granularidade de campos de proveniência no backend** (Seção 8) depende de uma mudança de schema ainda não desenhada em detalhe no backend — a obrigação de existir está definida aqui, o desenho exato da coluna/tabela não.
9. **Isolamento entre `backend_fastapi/` e `goalkeeper_ai_worker/` depende de disciplina manual, não de imposição automatizada** (ADR-009) — não há, nesta fase, lint ou verificação de CI que bloqueie um `import` cruzado entre as duas pastas do monorepo. O risco de acoplamento silencioso é real e deve ser checado ativamente a cada sprint, não apenas assumido como resolvido pela documentação.
10. **`worker/registry/`, `worker/models/` e `worker/gpu/` seguem vazios enquanto `inference/` concentra tudo** (Seção 1/6.1) — decisão deliberada para não generalizar a partir de uma única família (evita abstração prematura), mas significa que, se uma segunda família de Plugin chegar antes de uma refatoração consciente, há risco de duplicar o padrão em vez de extrair o genérico. Vale revisar quando o Tracker Registry (pós-W7) for implementado.
11. **`create_engine(nome, settings)` (Sprint W6) assume que todo motor registrado aceita `settings` no construtor** — convenção, não imposta por `InferenceEngine` (a ABC não declara `__init__`). Um motor futuro que esqueça esse parâmetro falha só em runtime, na primeira tentativa de `create_engine`, não em tempo de import/definição da classe.
12. **`PipelineProcessor.from_settings()` (Sprint W7) confia que `is_enabled(settings)` de cada Processor não tem efeito colateral e é barata de chamar a cada construção da pipeline** — é um classmethod, não imposto a ser puro pela ABC `FrameProcessor`. Um Processor futuro que faça I/O ou lance exceção dentro de `is_enabled` quebraria a montagem de toda a pipeline, não só a si mesmo. Mesmo risco de convenção-não-imposta do item 11, agora replicado numa segunda família de Plugin (Processors).
13. **`ultralytics` (Sprint W8) traz uma dependência transitiva pesada** (`torch`/`torchvision`, ~700MB, CPU-only nesta sprint) — aceitável para o primeiro Detector real, mas cada Detector futuro (RT-DETR, GroundingDINO, OWLv2) pode trazer seu próprio framework pesado (Transformers, JAX, etc.), sem um mecanismo de dependências opcionais por Detector (`extras_require`) ainda definido. Hoje `requirements.txt` é monolítico — revisitar se o número de Detectors registrados crescer.
14. **`opencv-python-headless` e `opencv-python` (instalado transitivamente por `ultralytics`) colidem no mesmo diretório `cv2/` do site-packages** — ambos os pacotes pip distintos instalam para o mesmo caminho de import. Resolvido nesta sprint fixando `opencv-python-headless==4.10.0.84` (primeira versão com suporte a NumPy 2.x) e reinstalando-a por cima após `pip install ultralytics`; `pip check` reporta permanentemente "ultralytics requires opencv-python, which is not installed" — **falso positivo conhecido e aceito**, não indica quebra real (cv2 importa e funciona; só o metadado do pip não reflete a substituição deliberada por headless). Reprovisionar o ambiente do zero exige repetir esse passo manualmente (documentado em `SPRINT_W8_REPORT.md`) até que um mecanismo mais robusto (ex.: constraints file, ou trocar para `opencv-python` puro) seja adotado.
15. **NumPy 2.x (trazido por `ultralytics`) é uma mudança de versão maior (1.26 → 2.2.6)** — testada e aprovada nesta sprint (117→130 testes, incluindo os de `video/`/`frame_ops.py`, todos passando), mas nenhuma auditoria linha-a-linha de APIs NumPy depreciadas foi feita; risco residual de comportamento sutilmente diferente em código futuro que dependa de detalhes de NumPy 1.x.
16. **`ByteTrackTracker` (Sprint W9) acopla-se a uma API interna, não pública, do pacote `ultralytics`** (`ultralytics.trackers.byte_tracker.BYTETracker`, incluindo o protocolo "results-like" que `_DetectionsAdapter` precisa imitar - `xywh`/`conf`/`cls` + indexação booleana) — decisão pragmática para evitar trazer um pacote `bytetrack`/`lap`/`cython_bbox` separado (historicamente difícil de instalar no Windows), já que `ultralytics` é dependência transitiva desde a W8. Uma atualização de versão do `ultralytics` que renomeie/reestruture esse módulo interno quebraria `bytetrack_tracker.py` silenciosamente até a próxima execução da suíte de testes (não há pin de versão exata além da já existente em `requirements.txt`, e a API interna não segue o mesmo compromisso de estabilidade da API pública `model.track()`).
17. **`WorkerOrchestrator` constrói o `InferenceEngine` (e, por extensão, toda a `PipelineProcessor` e cada `FrameProcessor`) uma única vez, reaproveitando a mesma instância entre Jobs sequenciais** (`orchestrator.py`, Seção 6.1, "Estado entre Jobs") — inofensivo para Processors sem estado, mas qualquer Processor futuro com estado próprio (ex.: um `PoseProcessor` com suavização temporal) precisa lembrar de implementar `reset()` corretamente; nada na ABC `FrameProcessor` força isso além do default no-op, e o próprio `BasicVisionEngine` precisa lembrar de chamar `self._pipeline.reset()` no início de `process()` — convenção, não imposta pelo compilador, mesma classe de risco dos itens 11/12.
18. **`BasicSceneAnalyzer.analyze()` (Sprint W10) recebe apenas `TrackingResult`, sem dimensões do frame** — por isso `OBJECT_ENTERED_FRAME`/`OBJECT_LEFT_FRAME` são emitidos como sinônimos exatos de `TRACK_STARTED`/`TRACK_LOST` (mesma condição, dois nomes), não como eventos geométricos independentes baseados em proximidade das bordas do frame real. Documentado honestamente no código e nesta Constituição (Seção 6.1) - se uma sprint futura precisar da semântica geométrica genuína, `analyze()` precisará receber também a resolução do frame (quebra de contrato, ou um parâmetro opcional adicional).
19. **`SceneStatistics`/`TrackingStatistics` são cumulativas por instância de Analyzer/Tracker, não por Job** — como a mesma instância é reaproveitada entre Jobs (Risco 17) mas `reset()` zera os contadores no início de cada Job, a cumulatividade É por-Job na prática (correta), mas depende inteiramente do `reset()` rodar antes de cada novo vídeo; qualquer falha nesse encadeamento (ex.: uma exceção não tratada entre Jobs que pule o reset) inflaria silenciosamente as estatísticas do próximo vídeo.
20. **`ObjectState.time_in_scene_frames` (Sprint W11) é expresso em frames, não em segundos** — o World Model nunca recebe `fps` (só `frame_index`, via `SceneAnalysisResult`/`TrackingResult`), então não há como converter para tempo real nesta camada sem estender o contrato de entrada (mesma classe de limitação do Risco 18, agora na camada de World Model). `Motion.speed` tem a mesma limitação — "pixels por frame", não "pixels por segundo".
21. **`BasicWorldModel._evict_if_over_capacity()` (Sprint W11) remove os objetos LOST mais antigos quando `WORKER_WORLD_MAX_OBJECTS` é excedido** — em vídeos com MUITOS objetos efêmeros (ex.: falsos positivos de detecção), um objeto genuíno mas momentaneamente perdido por mais tempo que outros pode ser evictado antes de conseguir se recuperar (`TRACK_RECOVERED`), perdendo sua identidade permanentemente (um novo `track_id` apareceria como objeto novo, não recuperado). Aceito por simplicidade — o limite existe para não crescer infinitamente em vídeos longos/ruidosos; ajustar `WORKER_WORLD_MAX_OBJECTS` para cima se isso se mostrar um problema real em vídeos de produção.
22. **`Field.default()`/`Goal.default_pair()` (Sprint W12) produzem geometria placeholder, não calibrada a nenhum vídeo real** — como o Worker nunca recebe calibração de câmera nem coordenadas reais de campo/gols, `FootballWorldBuilder` constrói `Field`/`Goal` com valores fixos arbitrários a cada Job. Qualquer analisador futuro que dependa de posição real relativa ao gol/linhas de campo (ex.: "a bola cruzou a linha") precisará de uma fonte de calibração ainda não desenhada — documentado honestamente no código e validado na prática (artefato real da W12 confirma os mesmos valores fixos independente do conteúdo do vídeo).
23. **`FootballWorldBuilder._build_entity()` (Sprint W12) decide `Goalkeeper` vs. `Player` vs. `Ball` só por comparação de string contra `ObjectState.label.lower()`** (`_GOALKEEPER_LABELS`/`_BALL_LABELS`/`_PLAYER_LABELS`) — como o `YOLODetector` padrão (YOLO11n/COCO, W8) nunca emite um rótulo "goalkeeper" (não existe essa classe em COCO), `FootballWorld.goalkeepers` permanece vazio em qualquer execução real com o modelo atual, mesmo quando um goleiro está de fato na cena; confirmado na validação manual da W12 (um "frisbee" real virou `Ball`, mas nenhum `Player`/`Goalkeeper` foi produzido). Resolver isso exige um Detector/modelo customizado com uma classe de goleiro própria (fora do escopo desta sprint) — não é um bug do builder, é uma limitação honesta e documentada do modelo de detecção montante. **Consequência direta na W13:** `GoalkeeperPresenceAnalyzer` herda esta mesma limitação — `exists`/`goalkeeper_count` refletirão fielmente o que `FootballWorldBuilder` classificou como goleiro, então continuarão `False`/`0` em qualquer execução real com YOLO11n padrão, mesmo com um goleiro genuíno em cena.
24. **`GoalkeeperPresenceAnalyzer` (Sprint W13) escolhe o PRIMEIRO candidato de `FootballWorld.goalkeepers` quando há mais de um** — não é uma heurística de "qual É o goleiro de verdade" (o Football Domain Model, W12, nunca desambigua isso), é apenas a regra determinística mais simples possível para reportar um único `track_id` quando pedido. Se um cenário real produzir múltiplos candidatos rotulados goleiro (ex.: dois "person" mal classificados por engano futuro), o `track_id`/`age`/`current_position`/`current_bbox` reportados podem "trocar" de frame para frame conforme a ORDEM de `active_objects` variar — nenhuma lógica de continuidade de identidade entre frames existe nesta camada (isso pertence ao Tracker, W9, não ao Analyzer).
25. **`WORKER_ANALYZERS` (Sprint W13) é uma lista simples separada por vírgula, sem validação de nomes desconhecidos em tempo de configuração** — um nome inválido só falha em runtime, na primeira chamada a `create_analyzer` dentro de `AnalyzerProcessor.__init__` (mesma classe de risco dos itens 11/12/17: convenção não imposta pelo compilador/schema, só descoberta ao construir a pipeline).
26. **`GoalGeometryAnalyzer` (Sprint W14) escolhe o PRIMEIRO gol de `FootballWorld.goals` quando há mais de um** — mesma regra de ordem determinística do Risco 24, mesma limitação: como `Goal.default_pair()` (W12) sempre cria exatamente 2 gols nas extremidades do campo placeholder, o "primeiro" hoje é sempre o gol esquerdo por construção, não porque foi identificado como o gol relevante para o goleiro em cena. Um `GoalkeeperPositionAnalyzer` futuro que precise saber qual gol o goleiro está defendendo precisará de uma lógica de seleção própria (fora do escopo puramente geométrico desta sprint).
27. **`GoalGeometryAnalyzer.confidence` (Sprint W14) reflete só a validade ESTRUTURAL do retângulo do gol (largura/altura positivas), não a precisão real da geometria** — como `Goal`/`Field` seguem sendo geometria placeholder não calibrada (Risco 22), `confidence=1.0` hoje significa apenas "o retângulo é bem formado", nunca "esta é a posição real do gol no vídeo". Uma sprint futura de calibração de câmera precisará introduzir uma fonte de confiança genuína (ex.: detecção visual do gol) antes que `confidence` possa refletir precisão real.
28. **`GoalkeeperPositionAnalyzer` (Sprint W15) herda a mesma limitação do Risco 26 sem resolvê-la** — ele também lê `football_world.goals[0]` (o primeiro gol, sempre o esquerdo por construção de `Goal.default_pair()`), não uma lógica de "qual gol o goleiro está defendendo". Se um goleiro real estiver posicionado no gol DIREITO, todas as medidas (`distance_to_goal_center`/`lateral_offset`/`depth_offset`/`angle_to_goal`) serão calculadas contra o gol ERRADO. Resolver isso exige uma sprint dedicada de seleção de gol (ex.: escolher o gol mais próximo do goleiro), fora do escopo puramente geométrico da W15.
29. **`GoalkeeperPositionAnalyzer._covers_thirds()` (Sprint W15) e `estimate_goal_and_penalty_areas()` (extraída para `field_areas.py` na W16) usam proporções fixas de campo oficial (5.5m/16.5m) e uma convenção arbitrária de "y menor = poste esquerdo"** — nenhuma das duas é medida do vídeo real: as proporções são uma convenção padrão de futebol aplicada deterministicamente à geometria placeholder do gol (mesma disciplina do Risco 22/27), e a correspondência y→poste é arbitrária sem calibração de câmera/direção de jogo conhecida (`Field.direction` permanece `UNKNOWN`, Risco 22). `inside_goal_area`/`inside_penalty_area`/`covers_left_post`/`covers_right_post` são, portanto, medições geometricamente corretas EM RELAÇÃO à geometria placeholder atual, mas não correspondem necessariamente à realidade física do vídeo até que uma sprint de calibração de câmera exista.
30. **`BallPositionAnalyzer` (Sprint W16) herda a mesma limitação dos Riscos 26/28 sem resolvê-la** — também lê `football_world.goals[0]` (o gol esquerdo por construção), não uma lógica de "para qual gol a bola se dirige". Uma bola real perto do gol direito teria todas as medidas calculadas contra o gol errado — mesma limitação, agora replicada num terceiro Analyzer consecutivo (W14/W15/W16).
31. **`BallPositionResult.ball_region` (Sprint W16) quase sempre será `None` na prática** — a grade 2×3 de `GoalGeometryResult.goal_regions` (W14) subdivide a faixa FINA do próprio gol (a profundidade `goal_width`, tipicamente ~2% da largura do campo), não a área de meta/pênalti (muito mais larga). Uma bola precisaria estar literalmente sobre a linha do gol para que `ball_region` seja diferente de `None` — útil para o caso raro "bola cruzou a linha", mas não para medir aproximação geral da bola ao gol (isso já é coberto por `inside_goal_area`/`inside_penalty_area`/`distance_to_goal_center`).
32. **`GoalkeeperBallAlignmentAnalyzer` (Sprint W17) executa `GoalGeometryAnalyzer.analyze()` efetivamente TRÊS vezes por frame** — uma vez diretamente e uma vez dentro de cada um dos dois sub-Analyzers que compõe (`GoalkeeperPositionAnalyzer`/`BallPositionAnalyzer`, que por sua vez também instanciam seu próprio `GoalGeometryAnalyzer`). Seguro (função pura, sem estado, sem efeito colateral), mas gasta CPU redundante — cresce combinatoriamente se um Analyzer futuro compor `GoalkeeperBallAlignmentAnalyzer` (que já compõe três). Aceito por simplicidade nesta sprint; se o custo de CPU se tornar relevante em produção, uma futura otimização poderia passar `FootballWorld` já acompanhado de um cache de `GoalGeometryResult` por frame (fora do escopo desta sprint, que segue estritamente "nenhum canal entre Analyzers").
33. **`BallMotionAnalyzer` (Sprint W18) decide continuidade comparando `track_id` entre frames consecutivos, mas `BallPositionAnalyzer`/`FootballWorldBuilder` sempre escolhem `balls[0]` (o primeiro da lista, mesma limitação dos Riscos 26/28/30)** — se o ByteTrack perder e recuperar o mesmo objeto físico com um NOVO `track_id` (comportamento real e possível de qualquer Tracker por reidentificação), `BallMotionAnalyzer` tratará isso corretamente como "não é a mesma bola" e reiniciará a contagem — tecnicamente correto dado o sinal disponível (não há garantia de que o track_id realmente representa a mesma bola física), mas significa que uma reidentificação do Tracker é indistinguível de uma bola genuinamente nova para este Analyzer. Nenhuma sprint futura de Tracking (fora do escopo de `analyzers/`) resolve isso sem mudar o Boundary Enforcement.
34. **`ShotAnalyzer.towards_goal` (Sprint W19) compara a direção observada do movimento (coordenadas de pixel cru) contra a direção até `Goal`/`Field` (geometria placeholder normalizada 0.0-1.0, Risco 22)** — as duas escalas não são calibradas entre si. Confirmado na validação manual: um movimento genuinamente horizontal em direção ao gol pode produzir `towards_goal=False` porque a componente y do vetor até o gol (posição em pixel menos ~0.5) domina o ângulo calculado quando a posição em pixel é ordens de magnitude maior que 1.0. `shot_detected` herda essa mesma limitação — em produção real (sem calibração de câmera), pode nunca se tornar `True` mesmo com um chute genuíno na direção do gol, dependendo da escala de pixels do vídeo. Resolver isso exige uma sprint de calibração de câmera (escala pixel↔metro e origem compartilhada entre `domain/` e as detecções reais), fora do escopo desta sprint. **Confirmado na W20 que este risco não se agrava nem se resolve** — por instrução explícita, a W20 não tentou corrigi-lo; como nenhum cálculo de `BallTrajectoryAnalyzer` referencia `goal_center`, ele simplesmente não é exercitado por essa sprint.
35. **`BallTrajectoryContext.points` (Sprint W20) cresce sem limite** — diferente de `Trajectory`/`History` do World Model (W11, buffer circular com `WORKER_WORLD_MAX_TRAJECTORY`), a lista de posições acumuladas por `BallTrajectoryAnalyzer` não tem teto: para uma bola continuamente rastreada (mesmo `track_id`, sem lacuna de frames) ao longo de um vídeo longo, `trajectory_points` cresce proporcionalmente à duração da observação contínua, inflando o tamanho do artefato JSON. Aceito nesta sprint (nenhum limite foi pedido, e o vídeo real de validação é curto); uma sprint futura pode introduzir um `WORKER_TRAJECTORY_MAX_POINTS` análogo, se isso se mostrar um problema real em vídeos de produção longos.
36. **`ShotAnalyzer.towards_goal` (Sprint W19) pode ser `True` para uma bola PARADA, não só em movimento** — quando `direction_vector` é o vetor nulo (`Vector(0,0)`, bola sem deslocamento), `angle_between()` (geometria pura, definida desde antes da W19) devolve 0° por convenção matemática para vetores degenerados, o que faz `towards_goal=True` mesmo sem nenhum movimento real em direção a lugar nenhum. Não é um bug de `ShotAnalyzer` (o campo é calculado corretamente segundo sua própria definição), mas é uma armadilha para qualquer consumidor que interprete `towards_goal=True` como "há movimento real em direção ao gol" sem primeiro checar `motion_detected`. Confirmado concretamente na Sprint W21: `PlaySituationAnalyzer._classify_direction()` precisou de um guard explícito (`if motion_detected is not True: return None`) para não expor `sub_state=SHOT_TOWARDS_GOAL` junto de `situation=BALL_STATIONARY`. Qualquer Analyzer futuro que consuma `ShotAnalysisResult.towards_goal` diretamente precisa aplicar o mesmo guard.
37. **`GoalkeeperDecisionAnalyzer.PREPARE_DIVE` (Sprint W22) é um proxy determinístico baseado em VELOCIDADE observada, não uma detecção real de postura/pose do goleiro** — este Worker não tem estimativa de pose/keypoints (nenhuma sprint até aqui introduziu isso, por decisão consciente de escopo). `PREPARE_DIVE` é inferido apenas por "chute detectado + velocidade do goleiro abaixo de `WORKER_GOALKEEPER_DIVE_MIN_SPEED`", não por reconhecer uma postura de preparação real (joelhos flexionados, peso baixo, etc.). Um goleiro genuinamente estático por INDECISÃO (não por estar se preparando) seria classificado da mesma forma - `GoalkeeperDecision` não distingue as duas situações, e não poderia distingui-las sem dados de pose. Documentado honestamente no docstring de `GoalkeeperDecision`; corrigir exigiria uma sprint de estimativa de pose, fora do escopo desta arquitetura atual.
38. **A maioria das regras de conteúdo de `GoalkeeperDecisionEvaluationAnalyzer` (Sprint W23) são auditorias garantidas por construção, não checagens independentes** — `shot_prompts_active_response`/`no_dive_without_shot`/`recover_follows_shot_ending` não podem falhar sozinhas com os dados hoje disponíveis, porque `GoalkeeperDecisionAnalyzer` (W22) já só produz `DIVE_LEFT`/`DIVE_RIGHT`/`PREPARE_DIVE` quando `shot_detected=True` e só produz `RECOVER_POSITION` quando `shot_detected=False` - essas três regras hoje só CONFIRMAM garantias já asseguradas pela própria W22, não capturam inconsistências genuinamente independentes. Consequência prática: `GoalkeeperDecisionEvaluation.INCOMPATIBLE` (todas as regras aplicáveis violadas) não é alcançável hoje só via composição real dos seis Analyzers - o pior veredito realista é `PARTIALLY_COMPATIBLE` (quando `dive_direction_matches_ball_direction`, a única regra genuinamente independente, diverge das demais). A lógica de agregação (`_classify`) foi testada diretamente com um `RuleOutcome` sintético para confirmar que o caminho `INCOMPATIBLE` está correto quando alcançado - documentado honestamente, não escondido. Resolver isso (tornar `INCOMPATIBLE` alcançável via dados reais) exigiria adicionar uma regra de conteúdo genuinamente independente numa sprint futura, sem remover as três atuais (que continuam úteis como auditoria de regressão entre W21/W22).
39. **`GoalGeometryResult.left_post`/`right_post` (Sprint W14) não correspondem à real separação lateral dos postes, e ficaram dormentes até a Sprint W24** — `left_post = Coordinate(x=region.x, y=region.y)` e `right_post = Coordinate(x=region.x+region.width, y=region.y)` têm o MESMO `y`, diferindo só ao longo do eixo de PROFUNDIDADE (`region.width`, tipicamente ~20px) - não ao longo do eixo LATERAL estabelecido pela W15 (`GoalkeeperPositionAnalyzer._covers_thirds`, que divide `region.height` para cobertura esquerda/centro/direita). Nenhum Analyzer entre a W14 e a W23 consumiu `left_post`/`right_post` genuinamente (só carregavam o valor adiante) - a inconsistência de eixo nunca foi exercitada até `PlayOutcomeAnalyzer` (W24) precisar deles para o critério `POST`. Por instrução da W24 ("nunca recalcular"), `PlayOutcomeAnalyzer` usa esses campos EXATAMENTE como produzidos, então `POST` é geometricamente coerente apenas com o que já existe, não necessariamente com a intuição de "poste lateral" de um goleiro real. **Consequência direta:** `PlayOutcome.CROSSBAR` é DELIBERADAMENTE NUNCA produzido - o gol é modelado como um retângulo 2D (Risco 22), sem nenhuma dimensão vertical/altura, e não há hoje fonte geométrica independente para distinguir "travessão" de "trave". Corrigir isso (o eixo de `left_post`/`right_post` E a ausência de modelagem vertical) exigiria uma sprint dedicada de calibração de câmera/geometria 3D - `GoalGeometryAnalyzer` (W14) não foi alterado nesta sprint, por instrução explícita de não fazer mudanças estruturais.

---

## 15. Infraestrutura de Visão — Histórico e Decisões (Sprints W5/W6/W7/W8/W9/W10/W11/W12/W13/W14/W15/W16/W17/W18/W19/W20/W21/W22/W23/W24/W25/W26/W27, concluídas — MVP arquitetural encerrado na W27)

Esta seção documentava originalmente o escopo *futuro* da W5 (redigida na revisão W4.1); mantida agora como registro das decisões que W5-W19 realmente tomaram, seguindo a regra vigente desde a W6 (sem mais revisões `Wx.1` — o histórico fica aqui, atualizado no lugar).

- **`VideoReader`/`FrameProvider`/`FrameIterator`/`FrameMetadata` real** — implementados na W5 exatamente como planejado, sem desvio (Seção 6.3).
- **Onde o código vive:** decisão fechada na W5 — `worker/video/`, módulo **irmão** de `inference/`, nunca dentro dele (Seção 1, Seção 6.3).
- **Primeiro motor real (`BasicVisionEngine`)** — implementado na W6, consumindo `video/` e introduzindo `inference/frame_ops.py` como camada reutilizável por qualquer motor futuro (Seção 6.1).
- **Decomposição em Processors independentes** — implementada na W7: `inference/processors/` (`FrameProcessor`, `ColorProcessor`/`ResizeProcessor`/`ROIProcessor`/`StatisticsProcessor`, Registry próprio, `PipelineProcessor`). `BasicVisionEngine` deixou de transformar frame diretamente (v1.0.0 → v2.0.0) e passou a orquestrar a execução da pipeline de Processors, mantendo `frame_ops.py` como as únicas funções que de fato tocam pixels (Seção 6.1).
- **Detection API + primeiro Detector real** — implementada na W8: `inference/detectors/` (`Detector`, `YOLODetector`, Registry e `factory.py` próprios) e `YOLOProcessor`. Confirmado na prática o desenho previsto: adicionar detecção real exigiu **apenas** um novo Processor + uma nova família de Registry — zero alteração em `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `video/`, Redis, Backend ou R2.
- **Tracking API + primeiro Tracker real** — implementada na W9: `inference/trackers/` (`Tracker`, `ByteTrackTracker`, Registry e `factory.py` próprios) e `TrackingProcessor`. **Achado não previsto originalmente:** a W9 revelou que `WorkerOrchestrator` reaproveita a mesma instância de `BasicVisionEngine`/`PipelineProcessor` entre Jobs sequenciais — exigiu introduzir `FrameProcessor.reset()`/`Tracker.reset()`/`PipelineProcessor.reset()` (Seção 6.1, "Estado entre Jobs"; Risco 17).
- **Scene Events API + primeiro SceneAnalyzer real** — implementada na W10: `inference/events/` (`SceneAnalyzer`, `BasicSceneAnalyzer`, Registry e `factory.py` próprios) e `SceneAnalysisProcessor`. Reaproveitou diretamente a plumbing `reset()` da W9, sem redescobrir o problema. Também corrigiu de curso uma expectativa antiga (W4.1): o "Event Registry técnico" aspiracional, antes previsto como submódulo de `worker/events/`, na prática se tornou a API de Eventos de Cena em `worker/inference/events/` (Seção 1).
- **World Model + primeiro WorldModel real** — implementado na W11: `inference/world/` (`WorldModel`, `BasicWorldModel`, `Trajectory`/`History`/`compute_motion`, Registry e `factory.py` próprios) e `WorldModelProcessor`. Quarta repetição consecutiva do mesmo desenho: adicionar a última camada genérica exigiu **apenas** um novo Processor + uma nova família de Registry — zero alteração em `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `video/`, Redis, Backend ou R2. Reaproveitou a plumbing `reset()` da W9 pela terceira vez sem modificá-la — confirmação definitiva de que o mecanismo genérico estava corretamente desenhado desde o início. **Achado necessário desta sprint:** `SceneAnalysisResult` precisou ganhar `objects: list[SceneObjectSnapshot]` (extensão aditiva) — `SceneEvent` sozinho não carrega bbox/posição, e a regra "WorldModel só conhece SceneAnalysisResult" tornou esse canal indispensável.
- **Football Domain Model + primeiro FootballWorldBuilder real** — implementado na W12: `worker/domain/` (`FootballWorld`, `FootballWorldBuilder`, `entities/`, `geometry/`, sem Registry/factory — implementação única e canônica, não uma família de Plugin) e `FootballDomainProcessor`. Quinta repetição consecutiva do padrão de encaixe: adicionar a primeira camada de domínio exigiu **apenas** um novo Processor — zero alteração em `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel` ou `video/`. Reaproveitou a plumbing `reset()` da W9 pela quarta vez, desta vez **aplicada proativamente** (`FootballWorldBuilder.reset()` escrito desde a primeira versão, não redescoberto via bug ou teste falho, diferente das W9/W10/W11). Introduziu `Vector.from_polar(magnitude, angle_degrees)` para converter `Motion.speed`/`direction_degrees` (camada World, polar) em `Vector` (camada Football Domain, cartesiano) — conversão de representação, não nova inferência. A única decisão dentro do builder é o dispatch por rótulo de classe (`_GOALKEEPER_LABELS`/`_BALL_LABELS`/`_PLAYER_LABELS`), explicitamente **não** tratada como heurística de negócio (é apenas roteamento para o construtor certo) — mas com limitação honesta documentada (Risco 23): o modelo YOLO11n padrão nunca produz rótulo "goalkeeper".
- **Analyzer API + primeiro Analyzer real** — implementado na W13: `worker/analyzers/` (`Analyzer`, `GoalkeeperPresenceAnalyzer`, `results.py`, Registry e `factory.py` próprios) e `AnalyzerProcessor`. Sexta repetição consecutiva do padrão de encaixe: adicionar a primeira camada de análise exigiu **apenas** um novo Processor + uma nova família de Registry — zero alteração em `BasicVisionEngine`, `PipelineProcessor`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel`, `FootballDomainProcessor` ou `video/`. **Diferença arquitetural desta família:** é a primeira em que várias implementações coexistem simultaneamente (`WORKER_ANALYZERS`, lista), em vez de uma única ativa por vez. Reaproveitou a plumbing `reset()` da W9 pela quinta vez. **Achado necessário desta sprint:** `TrackedFootballEntity` (W12) precisou ganhar `bbox: Region` (extensão aditiva, reaproveitando `Region` já existente para `Field`/`Goal` em vez de um tipo `BoundingBox` próprio do domínio) — nenhuma entidade carregava bounding box até então, e `GoalkeeperPresenceAnalyzer` precisa responder "qual o bbox atual?" sem violar Boundary Enforcement. `GoalkeeperPresenceAnalyzer` é deliberadamente **não** a primeira análise com julgamento real — responde só fatos determinísticos, mantendo a promessa "nenhuma regra de negócio ainda" por mais uma sprint.
- **Goal Geometry Analyzer** — implementado na W14: `worker/analyzers/goal_geometry.py` (`GoalGeometryAnalyzer`, segunda implementação concreta) e extensões de `results.py`/`types.py` (`GoalGeometryResult`/`GoalZone`). **Confirmação mais forte até aqui do padrão de encaixe:** adicionar um SEGUNDO Analyzer simultâneo exigiu **zero** mudança em `AnalyzerProcessor`, `registry.py` (além da linha de registro), `PipelineProcessor`, `BasicVisionEngine`, `Orchestrator`, `Detector`, `Tracker`, `SceneAnalyzer`, `WorldModel`, `FootballDomainProcessor` ou `video/` — a primeira vez que a arquitetura roda DOIS Analyzers reais ao mesmo tempo (`WORKER_ANALYZERS=goalkeeper_presence,goal_geometry`), validando a promessa central da Seção 6.5 (múltiplas implementações coexistindo, diferente de todas as famílias anteriores). **Sem achado arquitetural necessário** — diferente de W11/W13, `Goal`/`Field` (W12) já carregavam tudo que o Analyzer precisava (`region`), nenhuma extensão de contrato de camada inferior foi necessária. **Descoberta de composição:** um Analyzer futuro que precise do resultado de outro (`GoalkeeperPositionAnalyzer` consumindo `GoalGeometryResult`, W15) não exige nenhuma mudança no contrato `Analyzer.analyze(football_world)` — basta instanciar o Analyzer geométrico internamente e chamar `.analyze()` como função pura, já que é determinístico e sem estado.
- **Goalkeeper Position Analyzer** — implementado na W15: `worker/analyzers/goalkeeper_position.py` (`GoalkeeperPositionAnalyzer`, terceira implementação concreta) e extensão de `results.py` (`GoalkeeperPositionResult`). **Primeira confirmação em produção do padrão de composição** (previsto na W14): instancia `GoalGeometryAnalyzer` internamente e chama `.analyze(football_world)` diretamente, sem exigir NENHUM canal de comunicação novo no `AnalyzerProcessor`/`ProcessorContext` — a composição é invisível ao Processor. Validado que funciona mesmo com `WORKER_ANALYZERS=goalkeeper_position` sozinho (sem `goal_geometry` na lista), confirmando que a composição é totalmente independente do Registry/configuração. **Sem achado arquitetural necessário** — `Goal`/`Field`/`TrackedFootballEntity.confidence` (W12/W13) já carregavam tudo. Introduziu, de forma DELIBERADAMENTE documentada como convenção-não-calibração, retângulos de área de meta/pênalti derivados de proporções oficiais fixas (5.5m/16.5m, como múltiplos de `goal_height`) e uma correspondência arbitrária "y menor = poste esquerdo" (Riscos 28/29) — nenhuma delas é uma medida do vídeo real. `GoalkeeperPositionAnalyzer` é deliberadamente **não** a primeira análise com julgamento real — mede, não avalia, mantendo a promessa "nenhuma regra de negócio ainda" pela terceira sprint consecutiva.
- **Ball Position Analyzer** — implementado na W16: `worker/analyzers/ball_position.py` (`BallPositionAnalyzer`, quarta implementação concreta, segundo Analyzer a compor `GoalGeometryAnalyzer`) e extensão de `results.py` (`BallPositionResult`, incluindo `ball_region` — novo conceito de containment na grade 2×3 de `GoalGeometryResult.goal_regions`). **Segunda confirmação em produção do padrão de composição** (mesmo desenho exato da W15, aplicado a `football_world.balls[0]`) e terceira confirmação consecutiva de que um novo Analyzer não exige mudança em `AnalyzerProcessor`/pipeline/famílias de Plugin existentes. **Refatoração desta sprint (não achado de contrato):** `estimate_goal_and_penalty_areas()` extraída de `goalkeeper_position.py` para o novo módulo `field_areas.py`, reutilizada por `ball_position.py` — comportamento idêntico, testes da W15 inalterados. `BallPositionAnalyzer` é deliberadamente **não** a primeira análise com julgamento real — mede, não avalia (nenhuma previsão de trajetória, nenhuma avaliação de risco, nenhum conceito de "bola perigosa"), mantendo a promessa "nenhuma regra de negócio ainda" pela quarta sprint consecutiva.
- **Goalkeeper Ball Alignment Analyzer** — implementado na W17: `worker/analyzers/goalkeeper_ball_alignment.py` (`GoalkeeperBallAlignmentAnalyzer`, quinta implementação concreta, **primeiro Analyzer RELACIONAL**) e extensão de `results.py` (`GoalkeeperBallAlignmentResult`). **Primeira confirmação de que o padrão de composição escala para TRÊS Analyzers simultâneos** (`GoalGeometryAnalyzer` + `GoalkeeperPositionAnalyzer` + `BallPositionAnalyzer`, todos instanciados dentro de um quarto) — validado por teste e em produção que funciona mesmo sem nenhum dos três estar ativo em `WORKER_ANALYZERS`. **Princípio central da implementação:** combinar APENAS os resultados já produzidos pelos três Analyzers (ecoar `distance_to_goal_center`/`angle_to_goal` de `GoalkeeperPositionResult`/`BallPositionResult` em vez de recalculá-los), calculando como genuinamente NOVO só a relação goleiro↔bola e o "alinhamento" (projeção geométrica do goleiro sobre a reta bola→gol — distância perpendicular + se cai dentro do segmento). **Sem achado arquitetural necessário.** **Nota de eficiência não resolvida (Risco 32):** `GoalGeometryAnalyzer.analyze()` roda três vezes por frame (uma direta, duas dentro dos sub-Analyzers) — aceito por simplicidade, já que é uma função pura e sem estado. `GoalkeeperBallAlignmentAnalyzer` é deliberadamente **não** a primeira análise com julgamento real — mede a relação espacial entre os três elementos, nunca avalia se essa relação é boa ou ruim, mantendo a promessa "nenhuma regra de negócio ainda" pela quinta sprint consecutiva.
- **Ball Motion Analyzer** — implementado na W18: `worker/analyzers/ball_motion.py` (`BallMotionAnalyzer`, sexta implementação concreta, **primeiro Analyzer STATEFUL**) e extensão de `results.py` (`BallMotionResult`). **Primeiro uso real de `AnalyzerContext`** (reservada desde a W13, Seção 6.5): `BallMotionContext(AnalyzerContext)` guarda posição/velocidade/track_id/contagem de frames do frame anterior. **Confirmação definitiva de que a plumbing genérica de `reset()` estava correta desde o início:** `AnalyzerProcessor.reset()` já delegava a `analyzer.reset()` de cada Analyzer ativo desde a W13, mas até aqui isso sempre fora um no-op herdado da base (nenhum Analyzer anterior tinha estado); a W18 é a primeira vez que esse `reset()` realmente limpa algo, e funcionou sem NENHUMA mudança na plumbing. Compõe `BallPositionAnalyzer` (mesmo padrão das W15-W17), mas lê `Ball.confidence`/`.track_id` diretamente de `football_world.balls[0]` em vez de só via `BallPositionResult` (que não os expõe, e cujo `confidence` combinado incluiria `goal_geometry.confidence` — irrelevante para medir movimento). **Acha deliberadamente NÃO fazer** um achado que pareceria natural: reaproveitar `compute_motion()`/`Motion` do World Model (W11), que já calcula exatamente esta mesma cinemática — rejeitado porque `worker.inference.world` está fora do Boundary Enforcement de `analyzers/`; a pequena duplicação de matemática (distância/vetor/normalização) foi aceita, mesma disciplina já praticada entre `detectors`/`trackers`/`events`/`world`. Nunca extrapola através de uma lacuna de observação: bola desaparecida ou `track_id` trocado reinicia a continuidade (Risco 33). `BallMotionAnalyzer` é deliberadamente **não** a primeira análise com julgamento real — mede movimento já observado, nunca prevê nem avalia, mantendo a promessa "nenhuma regra de negócio ainda" pela sexta sprint consecutiva.
- **Shot Analyzer** — implementado na W19: `worker/analyzers/shot.py` (`ShotAnalyzer`, sétima implementação concreta, **primeiro Analyzer de EVENTOS**) e extensão de `results.py` (`ShotAnalysisResult`). **Primeiro Analyzer a tomar uma DECISÃO** (`shot_detected: bool`), mas 100% determinística e parametrizável (`WORKER_SHOT_MIN_SPEED`/`WORKER_SHOT_MAX_ANGLE_DEVIATION_DEGREES`/`WORKER_SHOT_MIN_CONSECUTIVE_FRAMES`) — não é ainda "julgamento de qualidade". Compõe TRÊS Analyzers (`BallMotionAnalyzer`/`BallPositionAnalyzer`/`GoalGeometryAnalyzer`), combinando apenas resultados já produzidos (ecoando `speed`/`direction_vector`/`direction_angle`/`frames_observed`/`distance_to_goal_center`, nunca recalculando); o único cálculo novo é `towards_goal` (`angle_between()` entre a direção observada e a direção até o gol). **Segundo Analyzer STATEFUL** (`ShotAnalyzerContext`, mesmo padrão exato da W18) — precisa lembrar por quantos frames consecutivos os critérios instantâneos vêm sendo satisfeitos, para que `shot_start_frame` aponte para onde a sequência qualificante realmente começou, não para onde `shot_detected` foi confirmado. **Confirmação de que composição múltipla + estado próprio + Analyzer composto que já compõe outro (`BallMotionAnalyzer` já compõe `BallPositionAnalyzer`) coexistem sem nenhum atrito arquitetural.** **Achado desta sprint (calibração, não contrato — Risco 34):** a validação manual revelou que a geometria placeholder normalizada e as posições reais em pixel cru vivem em escalas incompatíveis, fazendo `towards_goal` divergir do esperado em produção real — documentado honestamente; a prova geométrica exata ficou nos testes unitários com coordenadas controladas.
- **Ball Trajectory Analyzer** — implementado na W20: `worker/analyzers/ball_trajectory.py` (`BallTrajectoryAnalyzer`, oitava implementação concreta, **segundo Analyzer STATEFUL**) e extensão de `results.py` (`BallTrajectoryResult`). **Volta a ser puramente descritivo** — não é um evento binário como o `ShotAnalyzer` (W19), nem uma decisão: modela a FORMA da sequência de posições observadas (comprimento do caminho, direção dominante/média, consistência de direção, mudanças de direção, linearidade), tudo derivado exclusivamente dos frames já observados, nunca extrapolado. Compõe TRÊS Analyzers (`BallMotionAnalyzer`/`BallPositionAnalyzer`/`GoalGeometryAnalyzer`) — mas, diferente do `ShotAnalyzer`, `GoalGeometryAnalyzer` é usado apenas como um terceiro sinal real de `confidence`, nenhum campo de trajetória depende de geometria do gol. **Terceiro `AnalyzerContext` real** (`BallTrajectoryContext`, `points: list[Coordinate]`) — mesma disciplina de continuidade de `BallMotionAnalyzer`: qualquer lacuna de frames ou mudança de `track_id` (detectada via `ball_motion.previous_position is None`) reinicia a trajetória do zero, nunca emenda com a anterior. `linearity_score` (distância em linha reta ÷ comprimento do caminho, sempre em [0,1]) e `direction_consistency` (magnitude do vetor resultante médio dos segmentos normalizados, também em [0,1]) são as duas únicas grandezas genuinamente novas desta sprint — nenhuma das duas representa qualidade/perigo/precisão, só a FORMA geométrica do caminho. **Achado desta sprint:** confirmado que compor um Analyzer sem usar NENHUM de seus campos diretamente continua sendo composição legítima, desde que documentado por quê (`GoalGeometryAnalyzer` aqui). **Risco 34 (W19) deliberadamente não corrigido** por instrução explícita — confirmado que não se agrava, já que nenhum cálculo de trajetória referencia `goal_center`. **Novo risco (35, não corrigido):** `trajectory_points` cresce sem limite para observações contínuas longas, diferente de `Trajectory`/`History` do World Model (W11, com teto via `WORKER_WORLD_MAX_TRAJECTORY`) — aceito nesta sprint, sem `WORKER_TRAJECTORY_MAX_POINTS`. `BallTrajectoryAnalyzer` é deliberadamente **não** a primeira análise com julgamento de qualidade — descreve a forma do movimento, nunca avalia se ela foi boa/perigosa/eficiente, mantendo a promessa "nenhuma regra de negócio ainda" pela oitava sprint consecutiva (a nona, se contar a decisão puramente determinística da W19).
- **Play Situation Analyzer** — implementado na W21: `worker/analyzers/play_situation.py` (`PlaySituationAnalyzer`, nona implementação concreta, **primeiro Analyzer COGNITIVO**) e extensão de `results.py`/`types.py` (`PlaySituationResult`/`PlaySituation`). **Marca a transição da percepção para a cognição**, mas continua sendo uma CLASSIFICAÇÃO determinística, não um julgamento: `situation` vem de uma árvore de decisão fixa e priorizada (ausência de bola > ausência de goleiro > chute detectado > bola em movimento > bola parada > desconhecido) sobre campos já computados por `ShotAnalyzer`/`BallTrajectoryAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`GoalGeometryAnalyzer` (QUATRO Analyzers compostos, dois deles — `ShotAnalyzer`/`BallTrajectoryAnalyzer` — já compõem outros três cada). **Não introduz nenhum `AnalyzerContext` próprio** — é a primeira confirmação de que um Analyzer pode compor Analyzers STATEFUL sem precisar, ele mesmo, manter estado. `sub_state` (refinamento opcional sobre a direção observada) precisou de um guard explícito (`motion_detected is True`) para não herdar um falso-positivo de `ShotAnalyzer.towards_goal` para bolas paradas (**Risco 36** — documentado honestamente, não corrigido em `ShotAnalyzer` em si, só evitado aqui). **Deliberadamente NÃO implementados nesta sprint:** `BALL_INSIDE_GOAL_AREA`/`BALL_INSIDE_PENALTY_AREA` (mencionados como exemplos na especificação) — exigiriam compor `BallPositionAnalyzer` diretamente, fora da lista explícita de quatro Analyzers a reutilizar; documentado no docstring de `PlaySituation` em vez de forçar uma composição não pedida. `PlaySituationAnalyzer` é deliberadamente **não** a primeira análise com julgamento de qualidade — classifica o estado observável, nunca avalia se ele é bom/ruim/perigoso, mantendo a promessa "nenhuma regra de negócio ainda" pela nona sprint consecutiva (contando a partir da W13).
- **Goalkeeper Decision Analyzer** — implementado na W22: `worker/analyzers/goalkeeper_decision.py` (`GoalkeeperDecisionAnalyzer`, décima implementação concreta, **primeiro Analyzer ESPECÍFICO DO GOLEIRO, quarto STATEFUL**) e extensão de `results.py`/`types.py` (`GoalkeeperDecisionResult`/`GoalkeeperDecision`). Compõe CINCO Analyzers — `PlaySituationAnalyzer`/`GoalkeeperPositionAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`BallTrajectoryAnalyzer`/`ShotAnalyzer` — combinando apenas resultados já produzidos; a única informação GENUINAMENTE NOVA é `movement_direction`/`movement_speed` do PRÓPRIO goleiro (nenhum Analyzer composto rastreia isso, já que `GoalkeeperPositionAnalyzer` é uma fotografia geométrica por frame), calculada via `GoalkeeperDecisionContext` (quarto `AnalyzerContext` real) com a mesma disciplina de continuidade de `BallMotionAnalyzer` (`track_id` lido diretamente de `football_world.goalkeepers[0]`). `decision` (sempre um valor, nunca `None`) vem de uma árvore de decisão priorizada de 7 níveis, com dois parâmetros novos (`WORKER_GOALKEEPER_SHIFT_MIN_SPEED`/`WORKER_GOALKEEPER_DIVE_MIN_SPEED`) e um desempate determinístico documentado (empate lateral/profundidade favorece a classificação lateral). **Confirmação de que a profundidade de composição não importa** — `GoalkeeperDecisionAnalyzer` → `PlaySituationAnalyzer` → `ShotAnalyzer` → `BallMotionAnalyzer` → `BallPositionAnalyzer` → `GoalGeometryAnalyzer` são seis camadas de composição sem nenhum atrito. **Novo risco (37):** `PREPARE_DIVE` é um proxy determinístico baseado em velocidade, não uma detecção real de postura corporal (o Worker não tem estimativa de pose) — documentado honestamente no docstring de `GoalkeeperDecision`. `GoalkeeperDecisionAnalyzer` é deliberadamente **não** a primeira análise com julgamento de qualidade — classifica o comportamento observável do goleiro, nunca avalia se a decisão foi correta, mantendo a promessa "nenhuma regra de negócio ainda" pela décima sprint consecutiva (contando a partir da W13).
- **Goalkeeper Decision Evaluation Analyzer** — implementado na W23: `worker/analyzers/goalkeeper_decision_evaluation.py` (`GoalkeeperDecisionEvaluationAnalyzer`, décima primeira implementação concreta, **primeiro Analyzer de AVALIAÇÃO**) + novo módulo genérico `worker/analyzers/rules.py` (`Rule`/`RuleOutcome`/`evaluate_rules`, mecanismo de Rule Evaluation reutilizável por qualquer Analyzer de avaliação futuro). Responde apenas "a decisão observada do goleiro (W22) foi COMPATÍVEL com a situação observada (W21)?" - NUNCA avalia o resultado da jogada. Compõe SEIS Analyzers - três (`GoalkeeperPositionAnalyzer`/`GoalkeeperBallAlignmentAnalyzer`/`BallTrajectoryAnalyzer`) contribuem só para `confidence` (mesmo princípio da W20). Aplica 6 regras determinísticas (`actors_visible`/`decision_established`/`shot_prompts_active_response`/`no_dive_without_shot`/`recover_follows_shot_ending`/`dive_direction_matches_ball_direction`) a um contexto imutável, agregando o resultado numa árvore de prioridade de 5 níveis. **Inaugura a camada de Explainability** - `rules_evaluated`/`rules_passed`/`rules_failed`/`explanations` tornam toda conclusão auditável, nunca um estado isolado sem justificativa. **Achado durante a implementação (corrigido antes da validação manual):** leitura inicial incorreta de `ShotAnalysisResult.direction_vector` como deslocamento cru - na verdade normalizado (W18); corrigido multiplicando por `ball_speed` (ambos já ecoados). **Novo risco (38):** a maioria das regras de conteúdo são auditorias garantidas por construção do W22 - `INCOMPATIBLE` puro só é alcançável hoje via teste direto da agregação, não via composição real. `GoalkeeperDecisionEvaluationAnalyzer` é deliberadamente **não** a primeira análise com julgamento de qualidade sobre o RESULTADO da jogada - verifica só CONSISTÊNCIA lógica entre camadas já produzidas, mantendo a promessa "nenhuma regra de negócio sobre o resultado ainda" pela décima primeira sprint consecutiva (contando a partir da W13).
- **Play Outcome Analyzer** — implementado na W24: `worker/analyzers/play_outcome.py` (`PlayOutcomeAnalyzer`, décima segunda implementação concreta, **encerra a cadeia de observação**, **quinto Analyzer STATEFUL**) e extensão de `results.py`/`types.py` (`PlayOutcomeResult`/`PlayOutcome`). Responde apenas "qual foi o resultado observado da jogada?" - NUNCA avalia o goleiro, NUNCA atribui nota, NUNCA julga a qualidade da decisão. Compõe CINCO Analyzers - `PlaySituationAnalyzer`/`ShotAnalyzer`/`BallTrajectoryAnalyzer`/`GoalGeometryAnalyzer`/`GoalkeeperDecisionAnalyzer` - combinando apenas resultados já produzidos, e lê `football_world.field.region` DIRETAMENTE (permitido, todo Analyzer conhece `FootballWorld`) para o critério `BALL_OUT`. `PlayOutcomeContext` (quinto `AnalyzerContext` real) memoriza a última posição conhecida da bola/do goleiro (sobrevive a um frame de ausência momentânea - essencial para `LOST_TRACK`) e se a bola estava sendo rastreada no frame anterior. Árvore de decisão priorizada de 6 níveis, com dois parâmetros novos (`WORKER_OUTCOME_POST_PROXIMITY_PX`/`WORKER_OUTCOME_SAVE_PROXIMITY_PX`). **Por instrução explícita, NÃO reutiliza a Rule Evaluation da W23** - `supporting_evidence` é uma lista simples de frases, suficiente para esta sprint. **Achado durante a implementação:** `GoalGeometryResult.left_post`/`right_post` (W14) nunca haviam sido genuinamente consumidos até agora e não correspondem à real separação lateral dos postes estabelecida pela W15 - `POST` usa esses campos exatamente como produzidos (por instrução, "nunca recalcular"), e `CROSSBAR` permanece deliberadamente nunca produzido, documentado como **Risco 39, novo**. Validação manual confirmou, com DOIS Jobs consecutivos no mesmo processo do Worker, resultados idênticos e independentes - nenhum estado vazado entre vídeos diferentes. `PlayOutcomeAnalyzer` é deliberadamente **não** a primeira análise com julgamento de qualidade - identifica o resultado observável, nunca avalia se o goleiro foi bem ou mal, mantendo a promessa "nenhuma regra de negócio sobre desempenho ainda" pela décima segunda sprint consecutiva (contando a partir da W13).
- **Goalkeeper Performance Evaluation Analyzer** — implementado na W25: `worker/analyzers/goalkeeper_performance_evaluation.py` (`GoalkeeperPerformanceEvaluationAnalyzer`, décima terceira implementação concreta, **encerra a cadeia de avaliação**) e extensão de `results.py`/`types.py` (`GoalkeeperPerformanceEvaluationResult`/`GoalkeeperPerformanceEvaluation`). Responde apenas "qual foi o desempenho observado do goleiro nesta jogada?", cruzando EXCLUSIVAMENTE `GoalkeeperDecisionEvaluationResult` (W23, a decisão foi compatível com a situação?) e `PlayOutcomeResult` (W24, qual foi o resultado observado?) numa matriz determinística 3×3 — nunca reavalia decisão ou situação por conta própria, nunca introduz um terceiro eixo de julgamento. Compõe QUATRO Analyzers — `PlaySituationAnalyzer`/`GoalkeeperDecisionAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer` — mas usa apenas os DOIS últimos como fonte de conteúdo; os dois primeiros existem só porque `GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer` já os compõem internamente, mantendo o princípio "nunca recalcular" também no nível de instanciação. **Reutiliza, pela primeira vez fora da W23, a MESMA infraestrutura de Rule Evaluation** (`worker.analyzers.rules.Rule`/`RuleOutcome`/`evaluate_rules`) sem nenhuma mudança no mecanismo genérico — 8 regras novas e específicas de desempenho (`actors_and_geometry_available`/`decisive_event_established`/`decision_fully_compatible`/`decision_partially_compatible`/`decision_incompatible`/`outcome_is_save`/`outcome_is_neutral`/`outcome_is_goal`), provando que o design da W23 nunca esteve acoplado ao seu caso de uso original. **Nenhum `AnalyzerContext` próprio** — terceiro combinador puro sem estado (depois de `PlaySituationAnalyzer`/W21 e `GoalkeeperDecisionEvaluationAnalyzer`/W23). `summary` é deliberadamente uma string ESTRUTURADA (`performance=...; decision_evaluation=...; play_outcome=...; contributing_rules=...`), nunca linguagem natural, por instrução explícita desta sprint. **Sem achado de contrato necessário** — `GoalkeeperDecisionEvaluation`/`PlayOutcome` (W23/W24) já continham tudo que a matriz precisava; nenhum Risco novo foi introduzido. Validação manual confirmou a cadeia completa `insufficient_information`/`lost_track` (mesmo vídeo/detecção real da W24, YOLO perdendo o rastreamento da bola) propagando corretamente até o artefato final no R2. `GoalkeeperPerformanceEvaluationAnalyzer` é deliberadamente **não** uma sprint de recomendação/coaching — produz só a avaliação final da jogada, sem sugerir nada ao goleiro, mantendo a promessa "nenhuma recomendação ainda" pela primeira sprint em que essa promessa passa a fazer sentido (a cadeia de avaliação está encerrada).
- **Goalkeeper Coaching Analyzer** — implementado na W26: `worker/analyzers/goalkeeper_coaching.py` (`GoalkeeperCoachingAnalyzer`, décima quarta implementação concreta, **primeiro Analyzer de COACHING**) e extensão de `results.py`/`types.py` (`GoalkeeperCoachingResult`/`GoalkeeperCoaching`). Responde apenas "qual orientação técnica pode ser extraída desta jogada?", interpretando `GoalkeeperPerformanceEvaluation` (W25, o veredito final), `GoalkeeperDecision` (W22, o comportamento observado) e `PlayOutcome` (W24, o resultado observado) — nunca reavalia nenhum deles, nunca introduz uma quarta camada de julgamento. Compõe QUATRO Analyzers — `GoalkeeperPerformanceEvaluationAnalyzer`/`GoalkeeperDecisionEvaluationAnalyzer`/`PlayOutcomeAnalyzer`/`GoalkeeperDecisionAnalyzer` — todos usados como fonte de conteúdo (diferente da W25, onde dois dos quatro compostos só contribuíam para `confidence`). **Reutiliza, pela SEGUNDA vez consecutiva, a MESMA infraestrutura de Rule Evaluation** (`worker.analyzers.rules.Rule`/`RuleOutcome`/`evaluate_rules`) sem nenhuma mudança no mecanismo genérico — 8 regras novas e específicas de coaching (`evaluation_available`/`decisive_performance_established`/`performance_was_reinforcement`/`conceded_goal_without_active_response`/`committed_without_shot`/`reacted_passively_to_shot`/`dived_wrong_direction`/`recovery_was_insufficient`), três delas lendo `GoalkeeperDecisionEvaluationResult.rules_passed`/`rules_failed` (Explainability da W23) DIRETAMENTE como parte da própria entrada — pela primeira vez, um Analyzer usa a Explainability de outro Analyzer composto como dado de entrada, não só o veredito agregado, provando que o design da W23 é reutilizável em múltiplas dimensões. **Nenhum `AnalyzerContext` próprio** — quarto combinador puro sem estado (depois de `PlaySituationAnalyzer`/W21, `GoalkeeperDecisionEvaluationAnalyzer`/W23 e `GoalkeeperPerformanceEvaluationAnalyzer`/W25). `summary` é deliberadamente uma string ESTRUTURADA (`coaching=...; performance=...; decision=...; outcome=...; contributing_rules=...`), nunca linguagem natural, mesmo princípio da W25. **Sem achado de contrato necessário** — mas um achado ESTRUTURAL foi documentado honestamente: `conceded_goal_without_active_response` (`ATTACK_BALL`) é estruturalmente garantida a nunca ser satisfeita via composição real completa, pelo mesmo motivo do Risco 38 (W23) — testado diretamente na agregação pura. Validação manual confirmou a cadeia completa `insufficient_information`/`lost_track` propagando corretamente por todas as 14 camadas até o artefato final no R2. `GoalkeeperCoachingAnalyzer` é deliberadamente **não** uma sprint de linguagem natural/relatório final — produz só uma orientação técnica fortemente tipada, sem texto de coaching, mantendo a promessa "nenhuma linguagem natural ainda" pela primeira sprint em que essa promessa passa a fazer sentido (a cadeia de COACHING está encerrada).
- **Goalkeeper Analysis Report Analyzer** — implementado na W27: `worker/analyzers/goalkeeper_analysis_report.py` (`GoalkeeperAnalysisReportAnalyzer`, décima quinta implementação concreta, **ENCERRA OFICIALMENTE O MVP ARQUITETURAL**) e extensão de `results.py` (`GoalkeeperAnalysisReport`). Responde apenas "qual é a análise COMPLETA e consolidada desta jogada?", agregando — sem recalcular nada, sem executar nenhuma regra nova — os SEIS resultados cognitivos já produzidos por `PlaySituationAnalyzer` (W21)/`GoalkeeperDecisionAnalyzer` (W22)/`GoalkeeperDecisionEvaluationAnalyzer` (W23)/`PlayOutcomeAnalyzer` (W24)/`GoalkeeperPerformanceEvaluationAnalyzer` (W25)/`GoalkeeperCoachingAnalyzer` (W26). **Primeira vez que o padrão de composição escala para SEIS Analyzers simultâneos**, todos usados como fonte de conteúdo direta — sem nenhum atrito arquitetural. **Nenhum `Enum` novo** — primeira sprint da cadeia cognitiva (W21-W27) sem nenhuma classificação nova a tipar, só consolidação do que já existe. `confidence_summary` consolida (nunca recalcula) as seis `confidence`s + `overall` (mesmo princípio de "nunca fabricar" desde a W17); `artifacts` espelha os mesmos seis sub-resultados indexados por nome de Analyzer (mesma convenção de `analysis_results` desde a W13); `analysis_version`/`worker_version`/`generated_at` são os primeiros metadados de CONTRATO/software (não de visão computacional) produzidos por um Analyzer. **`GoalkeeperAnalysisReport` passa a ser o CONTRATO OFICIAL de saída do Worker** (ver Seção 6.5, "Contrato Oficial de Saída") — decisão de governança, não uma restrição técnica nova; nenhum resultado individual anterior foi removido ou descontinuado. **Sem achado de contrato necessário, sem achado estrutural novo** — é a primeira sprint cognitiva cujo `analyze()` é agregação pura, sem nenhuma árvore de decisão de conteúdo. Validação manual confirmou a cadeia completa Backend→Redis→R2→Worker→Report, com os quinze Analyzers ativos e o relatório consolidado idêntico ao seu alias no artefato. `GoalkeeperAnalysisReportAnalyzer` **encerra oficialmente o MVP arquitetural do Goalkeeper AI Worker** — toda inteligência cognitiva planejada (Observação→Situação→Decisão→Avaliação→Resultado→Desempenho→Coaching→Relatório) está implementada, testada e validada.

## 16. Pós-MVP — Itens Registrados para Versões Futuras (sem sprint definida)

**O MVP arquitetural do Goalkeeper AI Worker foi encerrado oficialmente na Sprint W27.** Diferente de todas as seções "Preparação para a próxima sprint" anteriores (W2 a W26), esta seção NÃO aponta para uma sprint numerada específica — por instrução explícita da W27, os itens abaixo são registrados como candidatos de evolução PÓS-MVP, sem compromisso de ordem ou data. Qualquer um deles, quando (e se) especificado no futuro, deve seguir a MESMA disciplina arquitetural confirmada nas dezenove sprints deste projeto (W8-W27): nenhuma alteração em `AnalyzerProcessor`/`PipelineProcessor`/`FootballDomain`/`WorldModel`/Rule Evaluation sem justificativa explícita, nenhuma heurística opaca, nenhum modelo treinado sem documentação honesta de suas limitações.

| Item pós-MVP | Por que foi deixado de fora do MVP | O que exigiria |
|---|---|---|
| **Pose Estimation** | O Worker nunca teve keypoints/postura do goleiro (documentado como limitação honesta desde o Risco 37, W22 — `PREPARE_DIVE` é um proxy de velocidade, não postura real) | Um novo Detector/Processor de pose (ex.: MediaPipe/OpenPose) - uma família de Plugin nova, não uma extensão de `analyzers/` |
| **Calibração automática de câmera** | `Goal`/`Field` (W12) são geometria PLACEHOLDER normalizada, nunca calibrada contra pixels reais (Risco 20/22/27/34) | Um processo de calibração real (ex.: homografia a partir de pontos de referência do campo) - fora do escopo de qualquer Analyzer |
| **Múltiplas câmeras** | Toda a arquitetura (Detector→Tracker→SceneAnalyzer→WorldModel→FootballDomain→Analyzers) assume um único fluxo de frames de uma câmera | Fusão de múltiplas fontes de vídeo/tracking - mudança estrutural no Pipeline, não só um novo Analyzer |
| **Tracking 3D** | `Coordinate`/`Region` (domain/geometry) são inteiramente 2D; `Goal` não tem dimensão de altura real (Risco 22, reafirmado no achado da W24/Risco 39 sobre `CROSSBAR`) | Um novo sistema de coordenadas 3D em `domain/geometry/` - extensão de contrato ampla, não aditiva |
| **Física avançada da bola** | `BallMotionAnalyzer`/`BallTrajectoryAnalyzer` (W18/W20) usam cinemática simples (deslocamento/velocidade/direção), nunca modelam spin, arrasto aerodinâmico ou quique | Um modelo físico da bola - mudaria a natureza de `BallMotionAnalyzer`, hoje deliberadamente só matemática de posições observadas |
| **Machine Learning para avaliação** | Toda avaliação/coaching (W22-W27) é 100% determinística, parametrizável e documentada — por instrução explícita de TODAS as sprints ("nenhuma heurística opaca, nenhum modelo treinado") | Uma mudança de princípio arquitetural explícita, não uma extensão - substituiria árvores de decisão auditáveis por um modelo estatístico |
| **Feedback em linguagem natural** | `summary`/`supporting_evidence`/`explanations` são deliberadamente ESTRUTURADOS desde a W23 (nunca prosa), confirmado até na W26 (Analyzer de COACHING) e na W27 (o próprio contrato oficial) | Uma sprint que autorize explicitamente texto livre voltado ao usuário final — a promessa "nenhuma linguagem natural ainda" só pode ser quebrada por instrução explícita, nunca por omissão |
| **Métricas estatísticas entre sessões** | Todo `AnalysisResult` (incluindo `GoalkeeperAnalysisReport`, W27) é uma fotografia de UM frame de UMA jogada — nenhuma agregação estatística entre Jobs/sessões/vídeos existe hoje | Uma nova camada fora de `analyzers/` (que só conhece `FootballWorld` de um Job por vez) - provavelmente do lado do Backend, que já persiste múltiplos Jobs/Analyses |

**O que permanece confirmado e reutilizável para qualquer um destes itens, quando especificados:**

1. Um novo Analyzer que consome `FootballWorld` só precisa: (a) escrever `XAnalyzer(Analyzer)` em `worker/analyzers/`, (b) registrá-lo em `analyzers/registry.py`, (c) incluir seu nome em `WORKER_ANALYZERS` — nenhuma abstração nova, `AnalyzerProcessor`/Registry/`factory.py` já existem e são genéricos o bastante (Seção 6.5). Confirmado quinze vezes seguidas (W13 a W27).
2. Se precisar de estado entre frames — `AnalyzerContext` (W13, usada W18/W19/W20/W22/W24). Se for um combinador puro — nenhum estado próprio necessário (W21/W23/W25/W26/W27, agora CINCO confirmações).
3. Se precisar de uma árvore de regras auditável — reutilizar `worker.analyzers.rules` (W23), confirmado reutilizável por DOIS Analyzers completamente diferentes (W25/W26) sem nenhuma mudança no mecanismo, incluindo leitura direta de `rules_passed`/`rules_failed` de outro Analyzer (W26) — NUNCA reinventar. Se for agregação pura sem avaliação — nenhuma Rule Evaluation é necessária (W27).
4. Riscos 34-39 (Constituição, Seção 14) continuam a fonte da verdade sobre limitações de geometria/cinemática/postura conhecidas — qualquer item pós-MVP que as toque deve revisá-las primeiro.
5. Nenhum item pós-MVP deve produzir linguagem natural, usar ML/modelo treinado, ou introduzir heurística opaca sem uma autorização explícita e documentada — a mesma disciplina que vigorou em todas as 27 sprints deste projeto.

**Confirmação explícita:** o MVP arquitetural do Goalkeeper AI Worker está **completo** — `FootballWorld`/todos os quinze `AnalysisResult`s existentes, incluindo a cadeia COMPLETA e agora CONSOLIDADA Situação→Decisão→Avaliação da Decisão→Resultado→Avaliação de Desempenho→Coaching→Relatório (`PlaySituationResult`/`GoalkeeperDecisionResult`/`GoalkeeperDecisionEvaluationResult`/`PlayOutcomeResult`/`GoalkeeperPerformanceEvaluationResult`/`GoalkeeperCoachingResult`/`GoalkeeperAnalysisReport`), respondem a toda pergunta que o MVP se propôs a responder. Evoluções futuras (lista acima) são aditivas e não têm sprint definida.

---

# Architecture Decision Records (ADR)

### ADR-001 — Estratégia de lock distribuído por vídeo

- **Contexto:** múltiplos Workers podem reivindicar Jobs distintos do mesmo Video (reprocessamento) sem nenhuma coordenação hoje.
- **Problema:** como impedir dois Workers processando o mesmo Video simultaneamente, evitando corrida de versionamento na Analysis e desperdício de GPU.
- **Alternativas consideradas:** (a) lock distribuído no Redis por `video_id`; (b) serialização no backend, rejeitando criação de novo Job enquanto há um ativo; (c) nenhuma proteção, confiando na unicidade de versão do banco para rejeitar depois do fato.
- **Vantagens:** (a) não exige mudança de backend, reaproveita dependência já existente do Worker no Redis, reação imediata (antes de gastar GPU). (b) defesa na origem, evita até criar o segundo Job. (c) mais simples, zero código novo.
- **Desvantagens:** (a) mais um componente de coordenação para manter correto (TTL, renovação). (b) exige mudança de backend, fora do escopo deste repositório. (c) descobre o conflito tarde demais, depois de gastar processamento inteiro.
- **Decisão recomendada:** (a) como mecanismo primário, com (b) recomendada como defesa complementar futura, coordenada com o backend.
- **Impacto futuro:** introduz dependência do Worker numa primitiva de lock do Redis; exige TTL e renovação (ADR-003).

### ADR-002 — Payload incremental versus payload único de submissão

- **Contexto:** o resultado de um Job pode incluir dezenas de eventos, várias métricas e múltiplos artefatos.
- **Problema:** como estruturar o envio dos resultados ao backend.
- **Alternativas consideradas:** (a) payload único atômico ao final de tudo; (b) incremental por tipo de dado, enviado assim que calculado; (c) duas fases — criar a Analysis, depois enviar tudo relacionado de uma vez.
- **Vantagens:** (a) simples de implementar. (b) resiliente a falhas parciais, não perde trabalho já enviado. (c) equilibra simplicidade com resiliência, sem multiplicar chamadas.
- **Desvantagens:** (a) uma falha de rede no final descarta todo o trabalho e exige reenviar um payload potencialmente grande. (b) multiplica chamadas HTTP e a complexidade de idempotência por tipo de dado. (c) ainda depende de reenvio integral da Fase 2 em caso de falha parcial dela.
- **Decisão recomendada:** (c) — duas fases idempotentes (Seção 9), equilibrando simplicidade operacional com resiliência a retry.
- **Impacto futuro:** define o formato do Contrato Backend↔Worker; qualquer novo tipo de dado (ex. novo tipo de evento) se encaixa na Fase 2 sem alterar a Fase 1.

### ADR-003 — TTL e renovação automática do lock

- **Contexto:** o Lock de vídeo (ADR-001) precisa expirar se o Worker morrer, mas não pode expirar durante um Job legítimo e longo.
- **Problema:** qual duração de TTL adotar e como renová-lo.
- **Alternativas consideradas:** (a) TTL fixo, longo o suficiente para o pior caso, sem renovação; (b) TTL curto com renovação periódica atrelada ao heartbeat; (c) sem TTL, liberação só manual/por conclusão explícita.
- **Vantagens:** (a) simples, sem lógica de renovação. (b) reage rápido a um Worker morto (TTL curto), sem risco de expirar durante um Job vivo (renovação contínua). (c) nunca expira por engano.
- **Desvantagens:** (a) TTL longo o suficiente para o pior caso significa esperar muito tempo para liberar o lock de um Worker morto no caso comum. (b) acopla o Lock à confiabilidade do heartbeat. (c) um Worker morto sem liberação explícita trava o vídeo para sempre.
- **Decisão recomendada:** (b) — TTL curto (ordem de minutos) com renovação a cada fração do TTL, usando o mesmo heartbeat da Seção 11.
- **Impacto futuro:** falha no heartbeat (bug, não crash) libera o Lock indevidamente enquanto o Job ainda roda — risco já registrado na Seção 14, item 5, a monitorar ativamente.

### ADR-004 — Cancelamento de processamento

- **Contexto:** `CANCELLED` já existe como status oficial no backend, mas não havia mecanismo do Worker perceber isso durante o processamento.
- **Problema:** como propagar um pedido de cancelamento (originado por humano no backend) até um Worker no meio de um Job.
- **Alternativas consideradas:** (a) polling do status do Job entre Stages via `backend_client`; (b) canal pub/sub dedicado no Redis para cancelamento; (c) sem suporte a cancelamento em andamento, só preventivo (antes do Job ser reivindicado).
- **Vantagens:** (a) reaproveita infraestrutura já existente, sem canal adicional. (b) reação mais imediata, não depende de o Worker estar entre Stages. (c) mais simples de implementar.
- **Desvantagens:** (a) latência de reação limitada à duração do Stage atual. (b) exige um segundo canal de comunicação e sua própria confiabilidade. (c) não atende ao requisito de cancelamento em andamento pedido nesta revisão.
- **Decisão recomendada:** (a) — polling entre Stages, com latência aceitável dado que Stages têm timeout curto (Seção 5).
- **Impacto futuro:** todo Stage do orchestrator precisa de um ponto de checagem de cancelamento antes de iniciar.

### ADR-005 — Estratégia de descoberta de plugins

- **Contexto:** múltiplas famílias de Plugins precisam ser conhecidas pelo Plugin Registry.
- **Problema:** como o Registry sabe quais Plugins existem e quais estão disponíveis para uso.
- **Alternativas consideradas:** (a) registro explícito em configuração (lista curada); (b) descoberta automática por convenção de diretório/entrypoints; (c) híbrido — descoberta automática, ativação por configuração.
- **Vantagens:** (a) totalmente previsível e seguro. (b) nenhum Plugin escrito é esquecido. (c) combina os dois benefícios.
- **Desvantagens:** (a) fácil esquecer de registrar um Plugin novo. (b) qualquer código na convenção de pastas roda automaticamente — risco de segurança/previsibilidade. (c) exige manter duas listas conceituais (o que existe vs. o que está ativo).
- **Decisão recomendada:** (c) — descoberta automática + ativação explícita por configuração; nenhum Plugin roda sem estar habilitado.
- **Impacto futuro:** exige convenção estável de estrutura de diretórios (Seção 12); mudar essa convenção exige atualizar o mecanismo de descoberta.

### ADR-006 — Versionamento do pipeline

- **Contexto:** a lógica do Pipeline (sequência de Stages, Plugins ativos) evolui; sem versionamento não há como saber o que gerou uma Analysis antiga.
- **Problema:** qual granularidade de versionamento adotar.
- **Alternativas consideradas:** (a) versão única para o Pipeline inteiro; (b) versão independente por família de Stage; (c) sem versionamento.
- **Vantagens:** (a) simples de raciocinar e comparar entre execuções. (b) granularidade fina, permite saber exatamente o que mudou.
- **Desvantagens:** (a) uma mudança pequena em qualquer parte incrementa a versão inteira. (b) explosão combinatória de versões a rastrear e comparar.
- **Decisão recomendada:** (a) — Pipeline Version única como identificador primário de reprodutibilidade, complementada pelo detalhe fino já capturado em Model Versions (Seção 8) — evita a complexidade combinatória de (b) sem perder rastreabilidade.
- **Impacto futuro:** exige disciplina da equipe para incrementar a Pipeline Version a cada mudança de Stage ou Plugin ativo, por menor que seja.

### ADR-007 — Versionamento dos modelos

- **Contexto:** Plugins de modelo precisam ser versionados, armazenados e permitir rollback, sem acoplar o Pipeline a uma implementação específica.
- **Problema:** como versionar e persistir qual versão de modelo gerou um resultado.
- **Alternativas consideradas:** (a) versionamento por hash de conteúdo dos pesos; (b) versionamento por tag semântica manual; (c) ambos.
- **Vantagens:** (a) integridade garantida, impossível ambiguidade. (b) legível e fácil de configurar/selecionar. (c) soma os dois benefícios.
- **Desvantagens:** (a) sozinho, pouco amigável para configuração humana. (b) sozinho, uma tag pode ser reapontada para pesos diferentes por engano, quebrando reprodutibilidade silenciosamente.
- **Decisão recomendada:** (c) — tag semântica como identificador de configuração no Model Registry, hash de conteúdo como o que é efetivamente persistido na proveniência da Analysis (Seção 8).
- **Impacto futuro:** o processo de publicação de novos pesos deve sempre calcular e registrar o hash; hash já publicado nunca é sobrescrito — mesma disciplina de imutabilidade já aplicada à Analysis.

### ADR-008 — Contrato Backend ↔ Worker

- **Contexto:** o Worker precisa entregar resultados estruturados ao backend; esse endpoint não existe hoje.
- **Problema:** qual formato/protocolo adotar, dado que o backend não é alterado nesta sprint mas a decisão precisa existir agora para não bloquear o desenho do Worker.
- **Alternativas consideradas:** (a) contrato síncrono HTTP em duas fases (Seção 9); (b) contrato assíncrono via um segundo stream Redis, consumido pelo backend; (c) escrita direta do Worker no banco — **rejeitada permanentemente**, viola a regra de que o Worker nunca acessa o banco diretamente (ver Frozen Architecture).
- **Vantagens:** (a) reaproveita o mesmo `backend_client`/API Key já usados para todo o resto, um único canal de comunicação. (b) desacopla temporalmente Worker e backend.
- **Desvantagens:** (a) submissão síncrona depende da disponibilidade do backend no momento da conclusão. (b) introduz um segundo canal de comunicação só para este caso, mais complexidade operacional.
- **Decisão recomendada:** (a) — HTTP síncrono em duas fases, mantendo um único canal de comunicação Worker→backend.
- **Impacto futuro:** é a decisão que mais bloqueia trabalho real do Worker (Sprint W8) — deve estar implementada no backend antes dessa sprint; atraso nessa dependência atrasa diretamente o roadmap a partir da W8.

### ADR-009 — Repositório separado versus monorepo

- **Contexto:** a versão original de `AI_WORKER_ARCHITECTURE.md` (Seção 8) recomendava um repositório Git separado (`goalkeeper-ai-worker`) para o Worker, com a justificativa de reforçar organizacionalmente sua independência e evitar a tentação de acoplamento com o backend. Essa recomendação foi revisada após avaliação do fluxo real de desenvolvimento.
- **Problema:** onde o código do Worker deve residir — em um repositório Git próprio, ou dentro do mesmo repositório do backend (`IA_GK`), como uma pasta de topo independente.
- **Alternativas consideradas:** (a) repositório separado (`goalkeeper-ai-worker`) — decisão original; (b) monorepo, com `goalkeeper_ai_worker/` como pasta de topo dentro de `IA_GK`, mantendo independência total de runtime/dependências/código como regra arquitetural explícita; (c) monorepo sem nenhuma barreira formal entre as pastas — rejeitada de imediato, pois abriria caminho direto para acoplamento silencioso.
- **Vantagens:** (a) reforça o isolamento no nível organizacional, dificulta fisicamente qualquer `import` cruzado, permite ciclos de release completamente independentes desde o primeiro commit. (b) preserva contexto entre backend e Worker durante o desenvolvimento (revisões arquiteturais completas do sistema, histórico de decisões num lugar só, sem perda de contexto entre sessões), evita a sobrecarga de coordenar dois repositórios/PRs para mudanças que afetam o contrato entre os dois lados, simplifica a operação para uma equipe pequena.
- **Desvantagens:** (a) exige coordenar mudanças de contrato entre dois repositórios/PRs, duplica configuração inicial (lint, CI, README). (b) exige disciplina ativa — não apenas documental — para que a barreira de independência não seja violada só porque o código está fisicamente acessível no mesmo working tree; o risco de acoplamento silencioso (import cruzado, dependência compartilhada por engano) precisa ser mitigado por regra explícita e revisão manual a cada sprint, já que a estrutura de pastas sozinha não impede fisicamente um `import`.
- **Decisão recomendada:** (b) monorepo, com a barreira de independência tratada como regra arquitetural obrigatória (ver Frozen Architecture) em vez de uma barreira física de repositórios. Nenhuma responsabilidade de componente muda — apenas a organização de versionamento.
- **Impacto futuro:** qualquer `import` entre `backend_fastapi/` e `goalkeeper_ai_worker/`, em qualquer direção, passa a ser tratado como violação arquitetural, verificada manualmente a cada sprint (Seção "Forma de Trabalhar" do processo do projeto) — não há, nesta fase, imposição automatizada (lint/CI) dessa regra, o que é registrado como risco na Seção 14. Se essa disciplina falhar na prática de forma recorrente, a decisão pode ser revertida para (a) sem impacto em nenhuma responsabilidade já definida.

---

# Boundary Enforcement

Backend (`backend_fastapi/`) e Goalkeeper AI Worker (`goalkeeper_ai_worker/`) são sistemas independentes. A presença de ambos no mesmo repositório Git (monorepo, ADR-009) **não altera essa independência em nenhum grau**.

## Proibições absolutas

Nenhum dos itens abaixo é permitido, em qualquer direção, entre `backend_fastapi/` e `goalkeeper_ai_worker/`:

| Proibido | Detalhe |
|---|---|
| Imports cruzados | Nenhum módulo de um lado importa código do outro, direta ou indiretamente |
| Compartilhamento de Models | SQLAlchemy models do backend nunca são importados ou reimplementados por referência no Worker |
| Compartilhamento de Schemas | Pydantic schemas do backend nunca são importados pelo Worker — se o Worker precisa de uma estrutura de dados equivalente (ex.: o payload da Submissão de Resultados, Seção 9), ela é definida de forma independente do lado do Worker, mesmo que isso duplique campos |
| Compartilhamento de Services | Nenhuma lógica de `app/services/*` do backend é chamada ou importada pelo Worker |
| Compartilhamento de configurações | `Settings`/`.env`/arquivos de configuração não são compartilhados — cada lado define e lê sua própria configuração |
| Compartilhamento de banco de dados | O Worker nunca abre conexão com o PostgreSQL do backend, nunca usa o SQLAlchemy do backend, nunca lê ou escreve tabelas diretamente |
| Compartilhamento de ambiente virtual | `goalkeeper_ai_worker/` tem `requirements.txt` e ambiente Python próprios, nunca instalados no mesmo venv do backend |

## Contratos públicos (únicos canais de comunicação permitidos)

Toda comunicação entre Backend e Worker ocorre exclusivamente por:
- **REST API** — Worker API do backend, autenticada por API Key (Seção 9)
- **Redis** — fila `processing_jobs` e Lock distribuído por vídeo (Seções 2 e 4)
- **Cloudflare R2** — via URLs assinadas de download/upload, nunca credenciais mestras (Seção 9)

Nenhum outro canal é permitido: sem arquivo compartilhado em disco, sem variável de ambiente compartilhada, sem IPC local, sem acesso direto a processo.

## Processo de exceção

Nenhuma exceção às proibições acima é permitida sem uma ADR aprovada, seguindo o mesmo formato desta Constituição (Contexto/Problema/Alternativas/Vantagens/Desvantagens/Decisão/Impacto). Uma exceção implementada sem ADR correspondente é, por definição, uma violação arquitetural — nunca uma decisão válida por si só.

---

# Frozen Architecture

As regras abaixo são consideradas oficiais e não devem ser reabertas sem uma nova revisão arquitetural formal:

- ✓ Worker nunca acessa o banco diretamente.
- ✓ Backend é a única autoridade sobre regras de negócio e sobre a atribuição de versão da Analysis.
- ✓ Toda comunicação Worker↔Backend acontece via API (HTTP + API Key) — nunca acesso direto a Postgres ou uso de credenciais mestras do R2.
- ✓ Redis é a fila oficial (Streams, consumer group) e também a base do Lock de vídeo e do heartbeat.
- ✓ Plugins seguem o Plugin Registry único — nenhuma família tem mecanismo próprio divergente.
- ✓ Modelos seguem o Model Registry, especialização do Plugin Registry.
- ✓ `video_id` é o Correlation ID oficial; `job_id` representa apenas uma tentativa de processamento.
- ✓ Analysis nunca é sobrescrita — cada execução bem-sucedida gera uma nova versão.
- ✓ Toda Analysis carrega proveniência completa: Pipeline Version, Worker Version, Model Versions, Schema Version.
- ✓ Toda submissão de resultados ao backend é idempotente (duas fases).
- ✓ Worker é stateless entre Jobs — único estado local é o checkpoint efêmero do Job em andamento.
- ✓ Nenhum Job inicia processamento sem antes adquirir o Lock do vídeo correspondente.
- ✓ Todo Stage possui timeout e classificação de retry (transitório/permanente) definidos.
- ✓ Cancelamento é sempre gracioso — nunca ocorre no meio de uma operação não interrompível com segurança.
- ✓ Nenhum Plugin roda sem estar explicitamente habilitado em configuração.
- ✓ **Compartilhar o mesmo repositório Git não autoriza compartilhamento de runtime, dependências, código Python, banco de dados ou lógica de negócio** (ADR-009).
- ✓ Qualquer `import` entre `backend_fastapi/` e `goalkeeper_ai_worker/`, em qualquer direção, é uma violação arquitetural — o Worker é tratado como se estivesse em outro repositório, mesmo residindo no mesmo Git.
- ✓ Toda comunicação Backend↔Worker passa exclusivamente pelos três contratos públicos (REST API, Redis, Cloudflare R2) — ver seção "Boundary Enforcement" para a lista completa de proibições e o processo obrigatório de exceção via ADR.
