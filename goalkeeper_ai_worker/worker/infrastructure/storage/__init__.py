"""Reservado para o download/upload REAL de bytes via URL assinada (Sprint W3).

Nesta sprint (W2) o canal R2 e validado apenas na geracao das URLs
assinadas (`BackendClient.get_download_url`/`get_artifact_upload_url`) -
nenhum GET/PUT de bytes contra o R2 acontece ainda.
"""
