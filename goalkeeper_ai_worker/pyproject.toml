[project]
name = "goalkeeper-ai-worker"
version = "0.1.0"
description = "Servico independente de visao computacional do Goalkeeper AI"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
